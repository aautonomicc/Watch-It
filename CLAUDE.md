# Watch-It

## Description
Cross-platform media player (Android, Android TV, iOS, Linux, Windows, macOS) for the Autonomi network. Client-only — no server side at all, and no Plex/Emby/Jellyfin server compatibility (stripped by design). Plex-style poster-wall UI.

## Tech Stack
Flutter 3.44.6 (SDK at ~/flutter) + media_kit (libmpv); SQLite via drift (media lists; codegen: `dart run build_runner build` in app/); Riverpod planned; Autonomi access via embedded Rust library `native/watchit_core` (ant-core, dart:ffi + in-process localhost streaming server) — no gateway/sidecar. `media_kit_video` is vendored at `app/third_party/media_kit_video` (pubspec dependency_overrides) carrying a Linux H/W-rendering patch — see Architecture Notes; drop when upstream fixes media-kit/media-kit#1404.

## Test Commands
In `app/`: `flutter analyze`, `flutter test` (PATH needs `~/flutter/bin`; `source ~/Android/env.sh` for Android builds). Live TMDB tests (test/tmdb_live_test.dart) need the credentials from the gitignored repo-root `.env`: `set -a; source ../.env; set +a; flutter test` — skipped automatically when TMDB_API_KEY is unset (CI needs no secrets). Dev build with the key bundled: `flutter run -d linux --dart-define-from-file=../.env` (dev/live-test only). Releases are KEYLESS since 2026-07-25 (user decision, reversing the alpha.24 bundling): release_build.sh no longer sources .env or passes any --dart-define, and `AppSettings.bundledTmdbApiKey` is deleted — users add their own key via Settings → Metadata (TmdbKeySource is now user|none). ⚠️ the old key IS extractable from published alpha.24–.33 binaries — rotate it on TMDB after the first keyless release ships. Release builds: run `scripts/release_build.sh` — it re-execs itself as transient systemd user unit `watchit-build` (survives session death; linger is on), builds APK then AppImage, logs to /tmp/watchit-build.log, refuses to double-launch. Follow with `tail -f /tmp/watchit-build.log`, status via `systemctl --user status watchit-build`; "ALL BUILDS DONE" in the log = success. Pure-Dart packages: `dart test` in `packages/watchit_naming/` (shared name parser/generator) and `cli/` (upload CLI, docs/UPLOAD-CLI.md). Rust: `cargo test` in `native/watchit_core/` (needs `~/.cargo/bin` in PATH). Before `flutter build apk`: run `native/build-android.sh` (cargo-ndk → jniLibs). Host streaming harness: `cargo run --release --bin devserver` then curl `/health` and `/xor/<addr>`.

## Dev Server
`flutter run -d linux` in `app/` — Linux desktop toolchain installed (clang/ninja/libgtk-3-dev/libmpv-dev); Android also fully working. AppImage: `scripts/build_appimage.sh` (needs linuxdeploy at ~/tools/, output in dist/, gitignored).

## Current Status
⚠️ GIT HISTORY REWRITTEN 2026-08-11 (legal purge — see Recent Changes): every commit SHA in this file older than that date is stale; current main HEAD is 1411f2d. Old clones must re-clone or hard-reset.
v0.1.0-alpha.83 "ALBUM MERGING & UPLOAD POLISH" RELEASED (2026-09-02, Latest: AppImage + Windows zip + APK; tag on bump commit 05bce46, feature commits f46dfe5 + 4859fde + fe9e1c8): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.83 — ships album/year identity edits (Save renames the whole album fold, merges loose singles into one card / VA compilation, drops stale mbids, refuses number collisions), the batch-prepare step line (per-file Fingerprinting · NN% bar + explanation, then media-info / MB-or-TMDB lookup steps), auto-deleted fully-successful batches (Show toggle gone; Save bundle to… before leaving) and the crash-recovery "Finish an interrupted upload?" launch prompt. See Recent Changes 2026-09-02.
Previously: v0.1.0-alpha.82 "RESUME & FORGET" RELEASED (2026-09-02, Latest: AppImage + Windows zip + APK; tag on bump commit 0e3e6f6, feature commits d1b9144 + d890809): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.82 — ships resumable interrupted batches (Previous-uploads Continue + Show toggle for finished batches), free library re-add for deduped files, the forget-uploads opt-in on Delete batch (drops the batch's ledger hashes — re-upload pays again), and the adaptive artist-card collage. See Recent Changes 2026-09-02.
Previously: v0.1.0-alpha.81 "EDITABLE TRACK NUMBERS & TIDY BATCHES" RELEASED (2026-09-02, Latest: AppImage + Windows zip + APK; tag on bump commit dae75ca, feature commits 260d1e0 + 1f08dc0): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.81 — ships track-number + disc editing in the track editor (Save renames the entry in every list, migrates per-track overrides, refuses collisions), per-batch upload isolation (fresh slug+timestamp work dir per batch; "Previous uploads" management list with Review/Dismiss/Delete; clear() on a fully-successful batch removes encodes/+art/) and the artist-agnostic album fold (hand-renamed compilations share ONE Various Artists card). See Recent Changes 2026-09-02.
Previously: v0.1.0-alpha.80 "ARTIST CARDS & CLEARER UPLOAD DOORWAY" RELEASED (2026-09-02): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.80 — artist fold (2×2 collage cards + artist page), state-aware upload doorway, visible auto-accept cards, batch-free single-file copy, live needs-attention refresh.
Previously: v0.1.0-alpha.79 "ONE UPLOAD FLOW" RELEASED (2026-09-02, Latest: AppImage + Windows zip + APK): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.79 — the batch uploader absorbs the tier flow (QUALITY on its review page, per-tier uploads folding into the version picker), match-review carousel with back/forward re-decision, needs-attention pointers from earlier batches, manual details seed the local cache, list-choice buttons + auto-add + Done→home, system Downloads folder default, and full-field music editing (schema v12 artist + per-track title rows). See Recent Changes 2026-09-02. NOT live-driven: the paid upload leg (no funded wallet on Ella — standing gap; pinned by fake-server tests incl. tier expansion and auto-add).
Previously: v0.1.0-alpha.78 "ALBUM-AT-A-TIME REVIEW" RELEASED (2026-09-01, Latest: AppImage + Windows zip + APK): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.78 — batch upload reviews music one whole album at a time (album grouping + one release for every track + single album confirm card) and ships the alpha.77-followup quartet (album inline player, merged Upload entry, batch list dropdown, no-match manual entry). See Recent Changes 2026-09-01. NOT live-driven: the paid upload leg (no funded wallet on Ella — standing gap).
Previously: v0.1.0-alpha.77 "BATCH UPLOAD" RELEASED (2026-09-01, Latest: AppImage + Windows zip + APK): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.77 — ships the upload CLI's prepare/upload pipeline as a desktop screen (Upload → Batch upload with auto-matching: MB/TMDB matching + canonical renames + shared content-hash ledger dedup + unattended paid batch + .watch-list bundle). See Recent Changes 2026-09-01. NOT live-driven: the paid upload leg through the new screen (no funded wallet on Ella — machinery is the user-verified alpha.55+ /upload path).
Previously: v0.1.0-alpha.76 "ALBUM WALL" RELEASED (2026-09-01): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.76 — album grid (music → square CAA-cover album cards) + upload CLI in the repo.
Previously: v0.1.0-alpha.75 "QUIET NETWORK" RELEASED (2026-08-30, Latest: AppImage + Windows zip + APK; tag on 9297049, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.75 — ships 79b7f94 (x0x 0.39.10→0.40.4 + saorsa-gossip 0.5.75 idle-bandwidth campaign: leaf mode, slim announces, replay-storm validators; x0x_tune.rs stops the 30s presence beacons on both agents; status JSONs expose gossip_mode). See Recent Changes 2026-08-30.
Previously: v0.1.0-alpha.74 "MOBILE DATA" RELEASED (2026-08-30, AppImage + Windows zip + APK; tag on dcea88c, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.74 — ships 4e157e2 (Settings → Mobile data screen consolidating all four cellular consumers + X0xCellularGate pausing the x0x agents on cellular when set Wi-Fi-only). See Recent Changes 2026-08-30.
Previously: v0.1.0-alpha.73 "HONEST SYNC" RELEASED (2026-08-30, AppImage + Windows zip + APK; tag on e55951f, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.73 — ships the post-alpha.72 commits: d8e829e (My W@tch map-import retry backoff clears when connectivity returns; Sync now clears it first; pending data maps surfaced in the sync activity card) and 2ba56aa (Channels gets the same body Sync now button as My W@tch; manual check waits out an in-flight cycle then runs a fresh full pass). See Recent Changes 2026-08-30.
Previously: v0.1.0-alpha.72 "RESILIENT UPLOADS" RELEASED (2026-08-30, Latest: AppImage + Windows zip + APK; tag on 2101287, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.72 — ships the post-alpha.71 commits: 26057fe (drawer status rows on top; Settings LIBRARY → CONTENT with Downloads merged in; Buffer size leads NETWORK; APPEARANCE below METADATA), 97ceaba (upload session survives navigation via services/publish_session.dart; Try again/Skip/Stop on upload errors; deferred channel announce when Channels x0x is off; x0x screen Channels-first) and 792693b (release builds start clean + fail loud on stale Dart snapshots). See Recent Changes 2026-08-30.
Previously: v0.1.0-alpha.71 "STATUS IN THE DRAWER" RELEASED (2026-08-29, Latest: AppImage + Windows zip + APK; tag on 60c09c1, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.71 — ships the post-alpha.70 commits: e1738f6 (home NetworkStatusBar deleted; library drawer gains Autonomi peers + My W@tch + Channels dot-status rows below Settings, tap-to-navigate) and f642f71 (Settings LIBRARY reorder Channels · My W@tch · My Media · Upload; NETWORK leads with Built-in Autonomi client + NEW Built-in x0x client tile with independent My W@tch / Channels switches that stop the agent's traffic without unlinking). See Recent Changes 2026-08-29.
Previously: v0.1.0-alpha.70 "CHANNEL PROFILES" RELEASED (2026-08-29, Latest: AppImage + Windows zip + APK; tag on 9044554, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.70 — ships the post-alpha.69 commits: af01b00 (channel profile: circular avatar + optional author, info card, mini avatars) plus the channels connection bar, softened update-fetch message, category-tag removal and Settings LIBRARY reshuffle. See Recent Changes 2026-08-29.
Previously: v0.1.0-alpha.68 "TIDIER DRAWER" RELEASED (2026-08-28, Latest: AppImage + Windows zip + APK; tag on 118174e, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.68 — ships the post-alpha.67 commits: 56dd444 (Media + Channels moved out of the home drawer into Settings → LIBRARY; drawer is now list navigation + Upload + Settings), 5207460 (library UX quartet: version nesting, copy-to-list, one Media menu, colour schemes) and ebfe8e0 (delta-aware channel-update import). See Recent Changes 2026-08-28.
Previously: v0.1.0-alpha.67 "MEDIA CURATION" RELEASED (2026-08-28, Latest: AppImage + Windows zip + APK; tag on 18e6d41, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.67 — ships the two post-alpha.66 Dart-only commits: 58ff09e (Auto-by-type removed, list-editor show→season→episode tree with move/remove, category picker at publish + no Uncategorised chip for category-free lists) and 50d52fa (drawer mirrors home-screen row order/visibility, fresh channels surface at the top). See Recent Changes 2026-08-28.
Previously: v0.1.0-alpha.66 "CHANNELS, ROUNDED OUT" RELEASED (2026-08-28, AppImage + Windows zip + APK; tag on d520ebf, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.66 — ships the three post-alpha.65 channel refinements: 86997ba (Publish an item starts from a local file, Upload-shaped flow + optional add-to-library), 988b879 (Check TMDB on Describe-this-item), 04cff7a (own channel on the creator's home wall + channel subscriptions sync over My W@tch as amber channels, not personal lists). See Recent Changes 2026-08-28/27. The paid public publish leg is now USER-VERIFIED on this release (2026-08-28): a real funded channel publish from the Linux AppImage was received on Android — no open channel verification gaps.
Previously: v0.1.0-alpha.65 "CHANNELS" RELEASED (2026-08-27, AppImage + Windows zip + APK; tag on 907c86c, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.65 — ships the public content space from docs/PLAN-personal-vs-channels.md Parts 2+3 — Ed25519 channel identity from its own 12-word phrase, publicly-uploaded signed manifests, x0x-gossiped signature-verified heads, subscribe-everywhere read-only amber channel lists, Describe-this-item + attestation + first-publish gate + cost preview, Terms v2. See Recent Changes 2026-08-27. Every leg live-verified except the paid public upload (needs a funded wallet) — do a real publish test before releasing as alpha.65.
v0.1.0-alpha.64 "UPLOAD" RELEASED (2026-08-27, AppImage + Windows zip + APK; tag on 0605e2e, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.64 — ships 6a172ad (Publish → Upload rename, Part 1 of docs/PLAN-personal-vs-channels.md: private upload flow drops the word "publish", Settings PUBLISHING → WALLET; naming/copy only, no functional changes; see Recent Changes 2026-08-27). Windows zip from CI run 33094207338 on the v-tag (sha256 7e11feab…, 53 files incl. ffmpeg.exe+ffprobe.exe, watchit_core.dll 47.4MB); verified APK versionCode 64/cert 79:11:66:99; the alpha.64 AppImage was GUI-verified live (Settings shows "0.1.0 (build 64)" + WALLET section, /tmp/wi64shot-*.png); published with /tmp/wi64-release-notes.md (no --prerelease → Latest, confirmed via releases/latest). Previously: v0.1.0-alpha.63 "FULL KEYLESS SYNC" RELEASED (2026-08-27, tag on 023919b): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.63 — ships 26ab96f (TMDB metadata + posters sync to keyless devices) + 3a06199 (edit-sync fixes, sync activity card, simplified home bar). Previously: v0.1.0-alpha.62 "EDIT SYNC" RELEASED (2026-08-27, AppImage + Windows zip + APK; tag on e7968a5, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.62 — ships 7fcb27d (My W@tch syncs user edits between linked devices: custom titles/years/descriptions/episode names inline in the sync doc, artwork at FULL QUALITY via sha256 manifest + pull-based x0x chunk transfer from any online owner; see Recent Changes 2026-08-26). Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 06:56; APK 82.6MB / AppImage 182MB), Windows zip from CI run 33047195805 on the v-tag itself (sha256 5ebb39f6…, 53 files incl. ffmpeg.exe+ffprobe.exe, watchit_core.dll 47.4MB with the art-transfer code); verified APK versionCode 62/minSdk 24/cert 79:11:66:99 (SHA-256); clean-HOME Xvfb :96 smoke of the exact dist AppImage (wi62-xvfb/wi62-app units): Terms gate → I agree → seeded home, connected · 5 peers, drawer → My W@tch screen renders (/tmp/wi62-smoke-1-launch.png … -4-mywatch.png; the sync feature itself was GUI-verified end-to-end pre-merge, see Recent Changes 2026-08-26); published with /tmp/wi62-release-notes.md (no --prerelease → Latest, confirmed via releases/latest; notes: edit-details sync + full-quality artwork + standing SmartScreen/hot-wallet notes). Previously: v0.1.0-alpha.61 "MY W@TCH SYNC" RELEASED (2026-08-26, Latest: AppImage + Windows zip + APK; tag on 684f718, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.61 — ships e2afcea (watch-list + viewpoint sync between linked devices over the My W@tch CRDT store; shrunk data maps travel so synced entries are playable), e10b249 (x0x agent builds into the Android core — My W@tch on Android, minSdk-24 getifaddrs link fix), ffb946d (Scan QR code option in the join dialog on Android/iOS via mobile_scanner); see Recent Changes 2026-08-26. Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 17:52; APK now 81MB / AppImage 181MB — the x0x .so grew the binaries), Windows zip from CI run 32996012761 on the bump commit (sha256 11415dbb…, 53 files incl. ffmpeg.exe+ffprobe.exe, watchit_core.dll 45.2MB with x0x+sync); verified APK versionCode 61/minSdk 24/cert 79:11:66:99 (SHA-256); clean-HOME Xvfb :97 smoke of the exact dist AppImage (wi61-xvfb/wi61-app units): Terms gate → I agree → seeded home, connected · 5 peers, drawer → My W@tch screen shows the NEW sync copy ("keep your watch lists and viewing positions in sync automatically") (/tmp/wi61-smoke-1-launch.png … -4-mywatch.png); published with /tmp/wi61-release-notes.md (no --prerelease → Latest, confirmed via releases/latest; notes: sync/Android/QR-scan + standing SmartScreen + hot-wallet notes; CORRECTED the zip run instruction to `watchit.exe` — earlier releases' notes said watch_it.exe but the zip has always shipped watchit.exe). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless). Previously: v0.1.0-alpha.60 "TERMS OF USE" RELEASED (2026-08-26, AppImage + Windows zip + APK; tag on bb4fb65, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.60 — ships abc1789 (Terms of Use & Disclaimer: first-launch accept gate + Settings → About page), 352014b (Publish QUALITY section explains why multiple qualities are encoded + ⓘ dialog), ca9cb2e (universal Original-tier uploads get their real resolution tag); see Recent Changes 2026-08-26. Local watchit-build unit built APK+AppImage, Windows zip from CI run 32956666254 on the bump commit (sha256 c0ff41ce…, 53 files incl. ffmpeg.exe+ffprobe.exe, watchit_core.dll 27.4MB); verified APK versionCode 60/cert 79:11:66:99 (SHA-256); clean-HOME Xvfb :96 smoke of the exact dist AppImage (wi60-xvfb/wi60-app units): first launch gates on the Terms page (Exit / I agree bottom bar), I agree → seeded home, connected · 5 peers (/tmp/wi60-smoke-terms.png, /tmp/wi60-smoke-home.png); published with /tmp/wi60-release-notes.md (no --prerelease → Latest, confirmed via releases/latest). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless). Previously: v0.1.0-alpha.59 "PUBLISH FIXED, POSTER CROP" RELEASED (2026-08-26, Latest: AppImage + Windows zip + APK; tag on f8db301, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.59 — ships a223b3a (Publish finishes for files over ~12 MiB: the shrunk-child-map finalize fix; anyone who hit the error can re-publish the same file for free) + 4dea923 (poster crop/zoom selector after picking a video frame in Edit details); see Recent Changes 2026-08-26. Previously: v0.1.0-alpha.58 "EDIT DETAILS FOR TV, FIXED" RELEASED (2026-08-25, AppImage + Windows zip + APK; tag on ceb0936, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.58 — ships 2d25f5c (Edit details show/season/episode scopes for TV) + 7e4b527 (episode artwork no longer leaks onto season surfaces); see Recent Changes 2026-08-25. Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 21:21), Windows zip from CI run 32900368258 on the bump commit (6m52s, 114MB with ffmpeg.exe+ffprobe.exe+av* DLLs, watchit_core.dll 27.4MB; sha256 dbe85bf6…); verified APK versionCode 58/cert 79:11:66:99 (SHA-256), AppImage sha256 111e6d38…, APK 55f098be…; clean-HOME Xvfb :97 smoke of the exact dist AppImage (wi58-xvfb/wi58-app units — connected · 5 bootstrap peers, DHT found 85 peers, seed NOTLD wall on fresh HOME; /tmp/wi58-smoke.png; the TV-scoping fixes themselves were live-verified in dedicated Xvfb runs pre-merge, see Recent Changes); published with /tmp/wi58-release-notes.md (no --prerelease → Latest, confirmed via releases/latest). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless). Previously: v0.1.0-alpha.57 "EDIT DETAILS" RELEASED (2026-08-25, Latest: AppImage + Windows zip + APK; tag on adcced6, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.57 — ships 5c92318 (Edit details: user metadata + artwork from image file or video frame; see Recent Changes 2026-08-25). Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 19:34), Windows zip from CI run 32890130676 on the bump commit (success, 114MB zip with ffmpeg.exe+ffprobe.exe+av* DLLs, watchit_core.dll 27.4MB; sha256 96ca5ee8…); verified APK versionCode 57/cert 79:11:66:99 (SHA-256), AppImage sha256 62359b8f…, APK f074e4c1…; clean-HOME Xvfb :97 smoke of the exact dist AppImage (wi57-xvfb/wi57-app units — connected · 5 peers, health state=ready stored_maps=2 on dynamic port 28467, AND the new feature live in the shipped binary: detail-page pencil → Edit details editor with From image file / From video frame / Remove artwork + title/year/description/Save; /tmp/wi57-smoke.png + /tmp/wi57-smoke-detail.png + /tmp/wi57-smoke-editor.png); published with /tmp/wi57-release-notes.md (no --prerelease → Latest, confirmed via releases/latest; notes: frame-picker dialog is desktop-only (bundled ffmpeg), player camera button all platforms, standing SmartScreen + hot-wallet notes). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless). Previously: v0.1.0-alpha.56 "PUBLISH, IN BULK" RELEASED (2026-08-25, Latest: AppImage + Windows zip + APK — Android returns after the desktop-only alpha.55; tag on 38e2a2d, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.56 — ships c235d68 (multi-file Publish batches for full series, quality tiers via bundled ffmpeg, desktop update check-and-notify; see Recent Changes 2026-08-25). Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 17:05; AppImage now 176MB with static ffmpeg/ffprobe in usr/bin), Windows zip from CI run 32875464920 on the bump commit (12m52s, 118MB with BtbN shared ffmpeg + av* DLLs, sha256 a74c113f…); verified APK versionCode 56/W@tch/cert 79:11:66:99, zip contents complete (watchit_core.dll 27.4MB, ffmpeg.exe+ffprobe.exe+7 av DLLs), AppImage bundles ffmpeg+ffprobe + all 7 audio-fallback libs intact; clean-HOME Xvfb :98 smoke of the exact dist AppImage (wi56-xvfb/wi56-app units — connected · 5 peers, health state=ready stored_maps=2 on dynamic port, Publish tile in drawer; /tmp/wi56-smoke.png + /tmp/wi56-smoke-drawer.png); published with /tmp/wi56-release-notes.md (no --prerelease → Latest; notes: APK is functionally the alpha.54 set since Publish is desktop-only). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless). Previously: v0.1.0-alpha.55 "PUBLISH" RELEASED (2026-08-25, Latest: AppImage + Windows zip — FIRST desktop-only release, no APK; tag on 88bf042 — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.55 — ships the Publish feature (f6f217a: internal ANT wallet in OS keychain + plain upload from desktop, see Recent Changes 2026-08-25). Local AppImage build via detached unit (ALL BUILDS DONE 14:19), Windows zip from CI run 32858290866 on the bump commit (11m42s; watchit_core.dll now 27.4MB with the wallet/upload code, zip 47MB sha256 31015733…); verified version 0.1.0+55; clean-HOME Xvfb :97 smoke of the exact dist AppImage (transient units wi55-xvfb/wi55-app — connected · 5 peers, health state=ready stored_maps=2 on dynamic port, Publish tile in drawer between Media and Settings, /tmp/wi55-smoke.png + /tmp/wi55-smoke-drawer.png); artifacts renamed into dist/ (AppImage mv + cp of the CI zip as Watch-It-0.1.0-alpha.55-windows-x64.zip), published with /tmp/wi55-release-notes.md (no --prerelease → Latest; notes carry the SmartScreen unzip-and-run note + hot-wallet warning). NOTE: the tag push also triggered windows.yml's new v* trigger (a second, unused zip artifact — expected). Paid upload path CONFIRMED WORKING end-to-end by the user (2026-08-25, same day as release): release AppImage tested on their box, small MP4 uploaded successfully to mainnet with a funded wallet — every Publish leg is now exercised, no open verification gaps on alpha.55. alpha.56 (c235d68) RELEASED 2026-08-25 — see the alpha.56 paragraph above. Previously: v0.1.0-alpha.54 RELEASED (2026-08-12, Latest: APK + AppImage, tag on ce5788e — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.54 — ships the PLAYER CURSOR-HIDE + LINUX SCREEN-LOCK INHIBIT (cdf7e15, see Recent Changes 2026-08-12). Build via watchit-build (ALL BUILDS DONE 09:42, fast — only Dart changed so cargo/Gradle caches were warm); verified versionCode 54, label W@tch, cert 79:11:66:99, AppImage build_number 54 with all 7 audio fallback libs parked (alpha.53 layout intact); clean-HOME AppImage smoke green on Xvfb :98 -ac via transient units wi54-xvfb/wi54-app (/tmp/wi54-smoke.png — connected · 5 peers, health state=ready stored_maps=2 on dynamic port via ss -tlnp by PID, ONE NOTLD card "2 versions" in both rows) + org.freedesktop.ScreenSaver strings confirmed in the shipped libapp.so; artifacts renamed into dist/ as usual, published with /tmp/wi54-release-notes.md (no --prerelease → Latest). Previously: v0.1.0-alpha.53 RELEASED (2026-08-11, tag on b1bf83e — Linux audio fix, see Recent Changes). Previously: v0.1.0-alpha.52 RELEASED (2026-08-10, APK + AppImage, tag on 9a6bd12 — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.52 — ships the ANT-CORE 0.5.1 BUMP (e2a6ea6: pin 629b87f → 81a0a24, rc-2026.7.4 — typed NotFound, payment/quote fixes, saorsa-core 0.26.4) and the FAVOURITES HEART BESIDE DOWNLOAD (56f0b68: heart moved from DetailScreen's app bar to right of the Download button). Build via watchit-build (ALL BUILDS DONE 2026-08-10 15:24); verified versionCode 52, label W@tch, cert 79:11:66:99; clean-HOME AppImage smoke green on Xvfb :98 -ac via transient units wi52-xvfb/wi52-app (/tmp/wi52-smoke.png — connected · 5 peers on ant-core 0.5.1, health state=ready stored_maps=2, ONE NOTLD card "2 versions" in both rows; /tmp/wi52-smoke-detail.png — heart right of Download, live in the shipped binary); artifacts renamed into dist/ as usual, published with /tmp/wi52-release-notes.md (no --prerelease → Latest). Previously: v0.1.0-alpha.51 RELEASED (2026-08-10, Latest: APK + AppImage, tag on c21ba3e — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.51 — ships FAVOURITES (de8f750: detail-page app-bar heart toggles the shown version into a new Favourites home row — fourth special row kSectionFavourites, default order continue→favourites→downloads→recent, hidden while empty, reorder/hide on Settings → Home screen; lib/services/favourites.dart FavouritesStore, favourites_v1 prefs StringList, deliberately NOT a MediaList) and the SEED-CATALOG TRIM (ba616db: bundle trimmed to the two NOTLD versions only — fresh installs/factory resets seed just NOTLD 480p+1080p, existing alpha.48–.50 installs keep everything already seeded, Option A per user decision). Build via watchit-build (ALL BUILDS DONE 2026-08-09 21:17); verified versionCode 51, label W@tch, cert SHA-256 79:11:66:99 (note: that fingerprint is the SHA-256, apksigner's SHA-1 is 0bab6e44); clean-HOME AppImage smoke green on Xvfb :98 -ac via transient units wi51-xvfb/wi51-app (/tmp/wi51-smoke.png — ONE NOTLD card with "2 versions" in both Recently Added and Movies, no other seeded titles, bundled poster offline, connected · 5 peers, alpha.50 app bar layout intact); artifacts renamed into dist/ as usual (unversioned build outputs), published with /tmp/wi51-release-notes.md (no --prerelease → Latest). Previously: v0.1.0-alpha.50 RELEASED (2026-08-08, Latest: APK + AppImage, tag on a6bd5d3 — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.50 — ships the HOME APP BAR RELAYOUT (ae8276f): search icon in the leading (far left) slot, settings icon removed from the bar (drawer's Settings tile covers it), drawer hamburger on the far right ('Browse lists'); home reloads via wiRouteObserver/RouteAware didPopNext after any pushed page pops (fixes drawer-pushed Settings/Media changes not applying on return) — see Recent Changes. Build via watchit-build (ALL BUILDS DONE 19:55); verified versionCode 50, cert 79:11:66:99; clean-HOME AppImage smoke green on Xvfb :98 -ac via transient units wi50-xvfb/wi50-app (/tmp/wi50-smoke.png — new bar layout visible, connected · 5 peers, full seeded wall, NOTLD '2 versions'); notes /tmp/wi50-release-notes.md (no --prerelease → Latest). Previously: v0.1.0-alpha.49 RELEASED (2026-08-08, Latest: APK + AppImage, tag on cc56138 — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.49 — ships the two unreleased items in one release: SAME-TITLE VERSION PICKER (52eb9b6 — same-title uploads share ONE wall card ("2 versions" info line) and the detail page grows a dropdown ("480p H.264 · 570 MB" / "1080p H.264 · 5.29 GB") that switches play/resume/download/FILE to the picked version; groupSeasons folds non-episode entries by parsed lookupKey so every surface gets the fold for free; DetailScreen self-discovers sibling versions from the library) and BOTH NOTLD UPLOADS BUNDLED (a19d9a6 — see Recent Changes). Pre-build: 403 Dart tests + analyze clean. Build via watchit-build transient unit (ALL BUILDS DONE 18:35); verified versionCode 49, label W@tch, cert 79:11:66:99; clean-HOME AppImage smoke green on fresh Xvfb :98 -ac via transient units wi49-xvfb/wi49-app (/tmp/wi49-smoke.png — connected · 5 peers, health stored_maps=49, ONE NOTLD card with "2 versions" in both Recently Added and Movies rows, bundled posters + size/format lines); artifacts renamed into dist/ (build outputs are unversioned: cp app/build/app/outputs/flutter-apk/app-release.apk + mv dist/Watch-It-0.1.0-x86_64.AppImage), published with /tmp/wi49-release-notes.md (no --prerelease so it lands as Latest). Previously: BOTH NOTLD UPLOADS BUNDLED (a19d9a6): the genuine-1080p 5.68GB re-encode (66cacd06…, default movie up to alpha.47) is back in the seed catalog as a second Movies entry beside the 480p upload — identical network file name, told apart by sizeBytes/videoInfo (the 6dc11fb feature); root-map asset restored from git history and verified offline (PUT /rootmap → resolve {size:5682464056, chunks:1357} == stat of the source still at ~/movie-convert/); 66cacd06 REMOVED from kLegacyDefaultMovieAddresses (pre-v4 holders keep it in place, no longer rewritten to the 480p address); already-v4-seeded installs (alpha.48) gain it via new LibraryStore.ensureSeedAdditions behind seed_additions_v1 (kSeedAdditionAddresses in seed_catalog.dart — skips held addresses, inserts next to the sibling, never recreates a deleted list; defaults_seeded_v4 NOT bumped, so user deletions stay deleted); no metadata re-harvest needed (same lookupKey → shared TMDB row/poster); 393 Dart tests (+7) + analyze clean; clean-HOME Xvfb smoke green (/tmp/wi-notld2-wall.png — both NOTLD cards side by side "480p H.264 · 570 MB" / "1080p H.264 · 5.29 GB", stored_maps=49) + live network stream of the 1080p upload (HTTP 206, ftyp isom header, 4.7s). Previously: v0.1.0-alpha.48 RELEASED (2026-08-08, Latest: APK + AppImage, tag on 91cf58f — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.48 — ships all four previously-unreleased items: PER-ENTRY FILE SIZE + VIDEO FORMAT (6dc11fb, 2026-08-08 — MediaEntry.sizeBytes/videoInfo differentiate same-title uploads; drift schema v8; size free from the datamap at import + /resolve backfill, resolution learned from mpv on first play; seed catalog regenerated with exact sizes + ffprobed formats for all 48 PD uploads — NOTLD and Lady Vanishes say [1080p] in their names but are really 480p; see Recent Changes), SEED-CATALOG TMDB METADATA BUNDLED (2026-08-08 — assets/seed_metadata/ metadata.json + 53 images (~1.5MB) harvested with the app's own matcher via tool/harvest_seed_metadata.dart; metadata_seeder.dart gap-fills the cache + posters dir at startup behind seed_metadata_v1, so fresh keyless installs show posters/descriptions/episode names offline; see Recent Changes), FULL PD SEED CATALOG (b11b1df, 2026-08-08 — first-run library seeds all 48 verified-PD uploads as three lists: 10 Movies + Petticoat Junction S1 21 eps + One Step Beyond 17 eps, root maps bundled as assets, NOTLD upload is the new default movie; see Recent Changes + docs/SEED-CATALOG.md) and IMPORT/EXPORT UX FIXES (b2d2e97, 2026-08-08 — Android bundle export via SAF Create-Document dialog, offline import pre-flight warning, real import-error surfacing, File/Bundle X-of-Y progress dialogs; see Recent Changes). Previously: v0.1.0-alpha.47 RELEASED (2026-08-04, Latest: APK + AppImage from a663a5c) — ships the ANDROID .BIN PICKER-RENAME FIX (5e1d145 — `.bin` accepted as the Android-mangled alias of `.datamap`: file_selector_android copies picked files to cache and swaps unknown extensions for the MIME-derived `.bin`, so every `X.datamap` reached Dart as `X.bin` and was name-rejected; mediaNameFromDatamapFileName now strips `.bin` like `.datamap`, recovering the exact original media name, and `*.watch-list.bin` routes as a bundle map for free; content verification unchanged — the name was never a safety check). 362 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi47-smoke.png — connected · 5 peers, NOTLD tile). Previously: v0.1.0-alpha.46 RELEASED (2026-08-04, APK + AppImage from 1689a53) — ships the SHRUNK-DATAMAP IMPORT FIX (bd5a87b — child/shrunk maps from real ant-cli uploads now expand over the network at import, so files >12MiB finally import; verified live against NOTLD; offline re-import of stored roots works, not-connected → clear 503 message, Dart surfaces the server's error text) and the V1 HISTORY-ROW DROP (8f7f0a1 — history.json address-keyed rows ignored on import, spec v2 member rows only; docs/PLAN-drop-v1-history-rows.md implemented). 29 cargo + 361 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi46-smoke.png — connected · 5 peers, NOTLD tile). Previously: v0.1.0-alpha.45 RELEASED (2026-08-04, APK + AppImage from 5f830be) — ships the SINGLE IMPORT ENTRY POINT (list editor's "Add .datamap files" FAB + Media page's "New list" FAB deleted; Add to library is the only import route and creates lists) and ADDRESS HYGIENE (bundle history.json spec v2 keyed by `.datamap` member names, missing-map entries' history kept out; ADDRESS section on DetailScreen + list-editor address subtitle deleted). 360 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi45-smoke2.png). Previously: v0.1.0-alpha.44 RELEASED (2026-08-03, APK + AppImage from 0e08d8f) — ships the COMBINED IMPORT (single multi-select "Add to library" picker, per-file routing by content/name), NETWORK-STORED BUNDLES (a bundle's own `.watch-list.datamap` fetched over GET /xor with progress + Cancel, 200MB cap), PLAIN-ENGLISH FLOW INFO (Media page import explainer + export-dialog ant share flow), and the LICENSING FIX (source MIT / binaries GPLv3, Settings → About → Open-source licenses). 356 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi44-smoke.png). Previously: v0.1.0-alpha.43 RELEASED (2026-08-01, APK + AppImage from 1522a83) — ships the AUTO-MODE REFINEMENTS (rev-3 plan implemented in 34f1fc2): "Media Lists" → "Media" rename (Settings tile, page title, drawer footer, home empty-state hint); Media page checkbox rows follow the arrangement mode (auto mode lists the virtual Movies/TV Shows rows, hideable via pref auto_hidden_lists_v1 → ArrangementStore.hiddenAutoIds, hiding applies wall + drawer); hidden virtual lists also filter Continue Watching + Recently Added at _posterWall build time (autoIdForEntry helper; Downloads row NEVER filtered); auto mode rendering zero children → "All media is hidden / Enable a list in Settings → Media" _EmptyState variant. 340 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi43-smoke.png). Previously: v0.1.0-alpha.42 RELEASED (2026-08-01, APK + AppImage from 34f755c) — ships AUTO-ARRANGE + LIST-BROWSE (Media Lists "My lists | Auto by type" SegmentedButton → virtual Movies/TV Shows lists from cached TMDB mediaType, home wall follows; left library drawer on Home + list pages; ListHomeScreen poster grid with multi-select AND-semantics genre FilterChips) and the IMPORT LIST-PICKER (checkbox dialog of existing lists replacing the typed name prompt in both loose-.datamap flows, multi-select + Create new list, silent merge). 330 Dart tests + analyze clean; clean-HOME AppImage smoke green (/tmp/wi42-smoke.png). Previously: XOR REMOVAL COMPLETE — v0.1.0-alpha.41 RELEASED (2026-08-01, Latest: APK + AppImage from 91d3910, releases 2+3 of the ROADMAP.md schedule shipped together — the deprecation window closed early because every testing device upgraded and every bundle migrated). Deleted in alpha.41: data_map_fetch (Engine::root_map network path), the one-time upgrade migration (library_migration.dart), v1-bundle import (hex lines skipped, all-v1 files error "re-export from 0.1.0-alpha.40+", rootmaps/ ignored), bundle-download-by-network-address (bundles are shared as files only — a bundle's public address re-leaks every datamap inside it); GET /resolve/{addr} survives as a LOCAL-store {size,chunks} lookup for the download manager (404 for never-imported). Tests: 309 Dart + analyze clean, 26 cargo; clean-HOME AppImage smoke green (/tmp/wi41-smoke.png — W@tch wordmark, 5 peers, NOTLD tile). Devices on ≤alpha.39 must go through alpha.40 for the migration pass. Previously: DATAMAP-FIRST PRIVACY MODEL released as v0.1.0-alpha.40 (733e8a3, release 1 of 3 — see Architecture Notes bullet 1; 317 Dart + 26 cargo, live POST /datamap round-trip of the real NOTLD map derived its exact public address, smoke /tmp/wi40-smoke.png); Phase 0 done, Phase 1 feature-complete, Phase 2 downloads shipped, bundles shipped (v0.1.0-alpha.39 from 69605d8 — W@tch rebrand: Anton wordmark app-bar/About, android:label, notifications, GTK titles, .desktop Name=, identifiers untouched, no functional changes; previously v0.1.0-alpha.38 from cc5443f — connectivity & background: auto-reconnect after network loss (Rust supervision + reconnect kicks), bundled NOTLD root map (instant demo resolve on fresh install), Android background downloads with progress notification, Settings → Network Wi-Fi-only/mobile-data policies, system-pause auto-resume; awaiting user on-device tests; previously v0.1.0-alpha.37 from 0de40f2 — blue theme + blue logo (accent copper #c9732b → blue #42a5f5 everywhere, blue-striped bucket icon incl. Android adaptive), no functional changes; previously v0.1.0-alpha.36 from 8a6ef40 — new striped popcorn-bucket logo everywhere (Android adaptive + legacy launcher icons, Linux taskbar icon, app-bar lockup), no functional changes; previously v0.1.0-alpha.35 from 3f92fdd — home-page library search (search icon + `/`/Ctrl+F, live grouped Shows/Movies/Episodes results) and the detail-header revert to the original layout; previously v0.1.0-alpha.34 from 640bcd8 — first KEYLESS release (⚠️ user must now ROTATE the old TMDB key), home-row customization, show-level Download All, 8px watch bar everywhere, bundle import opt-out dialog; previously v0.1.0-alpha.33 from a2a2015 — .watch-list bundles, TMDB attribution in About, Linux taskbar icon; previously v0.1.0-alpha.32 from 493b1b3 — season download-all button, home Downloads row, queue multi-select/delete-all, home top-bar download meter; previously v0.1.0-alpha.31 from 0a5370f — download badges on cards, offline gating with ConnectivityMonitor, Up-next offline rule, per-list export; previously "alpha A" v0.1.0-alpha.30, Latest: APK + AppImage from b41d96c — download manager with queue/pause/resume, prompt-on-play with remembered choice, Settings → Downloads, local playback of finished downloads (drift schema v6); previously v0.1.0-alpha.29 — resume points via watch_states table (drift schema v5), Continue Watching + Recently Added home rows, Resume/Start over + Watched badge + Next episode on DetailScreen, end-of-episode Up-next auto-play overlay; previously v0.1.0-alpha.28 — root-map SQLite persistence + /resolve, prefetch-on-import with hideable/resumable PrefetchManager, tile-open data-map warm, show-level home grouping, size-on-disk + factory reset; v0.1.0-alpha.27 — big-artwork show/season/detail pages with TMDB ratings, air dates, episode screenshots (6ddb5dd, cache schema v4); v0.1.0-alpha.26 — season grouping on the home wall + SeasonScreen, TMDB season artwork, seeded list renamed 'Movies'; and v0.1.0-alpha.25 — import lists from file (local or XOR download), multi-list files, merge-or-new clash handling, 10MB cap, Media Lists management page, TMDB key hidden from Settings UI; and v0.1.0-alpha.24 — bundles the TMDB key at build time so metadata works out of the box, and v0.1.0-alpha.23, APK + AppImage from f684964 — ships live TMDB metadata matching on top of the alpha.22 SQLite media-lists migration; AppImage carries all alpha.17–20 H/W-rendering fixes + alpha.21 prefetch/cache). Live TMDB verified with the user's real key (6 live tests pass; credentials in gitignored repo-root .env — TMDB_API_KEY + TMDB_READ_ACCESS_TOKEN, `set -a; source ../.env` before `flutter test test/tmdb_live_test.dart`). Default movie is now the H.264 8-bit 1080p mp4 at XOR 66cacd06… (5.3GB, faststart, Plex/Jellyfin name) — replaces the 3.8GB AV1 10-bit webm that choked phones/old desktops; ensureDefaults migrates both stale addresses (seeded key bumped to v3). File naming convention documented in docs/NAMING.md. Still open from the Mint-box report: hardware-decoding toggle + pre-first-frame error grace period (proposed for a later alpha; the H.264 default may already resolve it — awaiting user retest). Cold first byte ~20-25s even on fiber. CI live: GitHub Actions runs flutter analyze + test on push/PR (.github/workflows/ci.yml, Flutter 3.44.6 pinned, ~2min). Lists persist in SQLite via drift (watchit.sqlite in the app support dir; one-time import of the legacy shared_preferences blob), shipped in alpha.22. Release signing: keystore ~/keystores/watchit-release.jks + password in ~/keystores/watchit-key.properties (copy to app/android/key.properties, gitignored). gh CLI authenticated as aautonomicc; publish APKs via `gh release create`.

## Architecture Notes
- Library = user-held **lists**; each entry is `{derived address, file name}` — DATAMAP-FIRST since alpha.40 (docs/PLAN-datamap-privacy.md): content enters ONLY via `.datamap` files (private `ant file upload` output, bare msgpack root DataMap; legacy ant-gui JSON sniffed by `{`) or `.watch-list` bundles; the address is blake3(rmp(shrink(root))) derived OFFLINE by `POST /datamap` (verify.rs::derive_address), == the public XOR address for public uploads (validated live against the real NOTLD map: POST of its 108KB datamap derives 66cacd06…cb13 exactly). Root map REQUIRED in the local mapstore; `/xor/{addr}` serves stored maps only and fast-fails 404 "data map missing — re-import" (no network resolve on the play path, kills the 20-31s cold resolve). Public XOR addresses REMOVED as entry type: no add-entry dialog (and since 2026-08-03 the list editor has NO add button at all — the Media page's "Add to library" is the single import entry point, which also creates lists), no plain-.txt import/export, no DataMapPrefetcher/prefetch dialogs/tile-open warm (all deleted; wiMessengerKey moved to widgets/messenger.dart). `GET /resolve/{addr}` is a LOCAL-store {size,chunks} lookup since alpha.41 (404 for never-imported; kept only for the download manager) — data_map_fetch, the v1 border conversion, library_migration.dart, and bundle-download-by-address were all deleted in alpha.41 (releases 2+3). Bundle spec v2 (docs/BUNDLE-FORMAT.md): `datamaps/<original filename>.datamap` members (zip root accepted — hand-made `zip lib.watch-list *.datamap` is valid), optional filename-line list.txt (`ListName=` + lines ending .datamap; hex lines = v1 → SKIPPED since alpha.41, all-v1 files error "re-export from 0.1.0-alpha.40+", rootmaps/ members ignored), unclaimed members → list named from bundle filename (prompt when network-fetched); export = bundle only from `GET /datamap/{addr}` (ant-cli-compatible msgpack), history checkbox kept, includeRootMaps/pre-resolve pass gone. Widget tests fake the embedded server via HttpOverrides (test/fake_embedded_http.dart — real sockets deadlock in the fake-async zone; XFile.fromData ignores `name:`, use `path:`)
- Lists live in SQLite via drift: lib/db/app_database.dart (tables media_lists/media_entries, position columns keep order; generated code checked in) behind the unchanged LibraryStore load/save/ensureDefaults API. media_lists.enabled (schema v3, default 1) drives per-list home-screen visibility; lists are managed on the dedicated Settings → Media Lists page (screens/media_lists_screen.dart: create, checkbox show/hide, rename/delete behind a 3-dot menu; tap opens ListEditScreen). DB file `watchit.sqlite` pinned to getApplicationSupportDirectory (drift_flutter's documents-dir default degrades to $HOME on headless Linux). First open imports the legacy `media_lists_v1` shared_preferences JSON blob into an empty DB, then removes the key; `defaults_seeded_v3` flag stays in shared_preferences so upgraded installs don't re-seed. Tests inject `AppDatabase.forTesting(NativeDatabase.memory())` via LibraryStore.useForTesting
- Metadata matcher (alpha.23, lib/services/metadata_service.dart + tmdb_client.dart): file name → parseMediaName (title/year/imdb-id/SxxEyy + 1x02 markers) → TMDB (exact /find by IMDb id, else title/year search; TV gets show + episode details) → artwork/description/category, cached in SQLite (metadata_cache table, keyed by ParsedName.lookupKey so renames share rows; misses cached with 7-day TTL, transport errors never cached) with w342 posters as files in <support>/posters/. BYO TMDB key: Settings → Metadata accepts v3 api key (query param) or v4 read token (dots → Bearer header); `--dart-define=TMDB_API_KEY` bundles a default. Screens call the sync MetadataService.instance.metadataFor inside a ListenableBuilder; fallback chain memory → bundled NOTLD catalog → parsed name. Key change calls reset() (clears misses). schemaVersion 2 (onUpgrade creates the table)
- Playback via libmpv (media_kit) everywhere; streams `http://127.0.0.1:<port>/xor/<addr>` from the embedded client — seek works via HTTP Range
- Linux H/W video rendering (alpha.18 + alpha.20): vendored media_kit_video patch — upstream reads Flutter's EGL display with eglGetCurrentDisplay() on the platform thread, which returns EGL_NO_DISPLAY since Flutter 3.38, so every Linux box silently fell back to CPU mpv rendering + pixel-buffer copies (the Mint jerky-video/janky-spinner report). Fix: init stays EAGER on the platform thread (upstream flow) but derives Flutter's EGLDisplay exactly like the embedder's fl_opengl_manager.cc does — eglGetPlatformDisplayEXT(EGL_PLATFORM_WAYLAND/X11_EXT, gdk native display) returns the identical EGLDisplay handle; Flutter's *context* is never needed since the mpv render context lives in an isolated unshared context (only the display must match, for the display-scoped EGLImage frame sharing). Two hard-won invariants: (1) do NOT defer init to the first FlTextureGL populate — deadlock: Video widget mounts the Texture only after mpv reports dimensions, mpv reports dimensions only after the render context exists, the render context was only created in populate, and populate only runs when the Texture is composited (alpha.17's black-screen/stalled-playback bug); (2) after init on the platform thread, RELEASE the isolated context (eglMakeCurrent(dpy, NO_SURFACE, NO_SURFACE, EGL_NO_CONTEXT)) — restoring the saved EGL_NO_DISPLAY binding is an error that leaves our context current there and every raster-thread eglMakeCurrent then fails EGL_BAD_ACCESS; (3, alpha.20) on real-GPU X11 boxes GTK presents FlView through a GDK **GLX** context and leaves it current on the platform thread after painting — EGL cannot see a GLX binding (eglGetCurrentContext()==EGL_NO_CONTEXT) but Mesa refuses to bind an EGL context over it (EGL_BAD_ACCESS 0x3002 → S/W fallback, the Mint alpha.19 report), so video_output_release_foreign_gl_binding() calls gdk_gl_context_clear_current() before binding the isolated context (init + dispose); GDK re-binds on its next paint. xvfb NEVER exercises the GLX case (no DRI3 → no GDK-GL) — real-GPU testing needs the harness's REPRO_GDK_GL=1 runner hook that pins a GdkX11GLContext current before plugin init. Files: linux/video_output.cc (video_output_init_hw, video_output_get_flutter_egl_display, video_output_release_foreign_gl_binding), linux/texture_gl.cc (pure upstream populate). Verified via /tmp/mk_repro harness under xvfb (raw Texture + stock Video widget + upstream control + REPRO_GDK_GL both-ways test)
- Embedded client: `native/watchit_core` (Rust cdylib, ant-core 0.5.1 pinned rev 81a0a24) — tokio runtime + axum server on 127.0.0.1, `GET /xor/<addr>` (Range via self_encryption streaming_decrypt/get_range), `GET /health` for the Settings status tile; started once from main() over dart:ffi (lib/services/embedded_client.dart). APK is arm64-v8a only (abiFilters + jniLibs excludes); jniLibs/ and target/ are gitignored build artifacts
- Chunk cache + keep-ahead prefetch (post-alpha.20): all chunk traffic goes through a process-wide byte-bounded LRU (`cache.rs`, 256 MiB desktop / 128 MiB Android, single-flight claims so serving+prefetch never double-download; waiters poll 50ms — deliberate, avoids Notify lost-wakeup). Each `stream_range` spawns `prefetch_ahead` (engine.rs): serving loop reports last-served byte via a watch channel, prefetcher keeps PREFETCH_AHEAD_CHUNKS=16 (~64 MiB ≈ 60-70s at 7-8 Mbps) warm at PREFETCH_CONCURRENCY=4 (< FETCH_CONCURRENCY=16 so foreground batches win). Prefetch caps at the request's `end` — fine because mpv sends open-ended ranges. Prefetcher exits when serving drops the watch sender; chunk offsets rebuilt from DataMap::infos() cumulative src_size (same mapping get_range uses). /health gained cache_hit_chunks + cache_bytes
- Root-map persistence + prefetch (post-alpha.27): resolved root data maps persist in SQLite (`mapstore.rs`, rusqlite bundled, `root_maps.sqlite` in the app data dir, addr BLOB → bincode DataMap via DataMap::to_bytes) — content-addressed and immutable so cached forever; `Engine::root_map` reads memory → disk → network and writes back both, so any title's map resolves over the network at most once per device (cold resolve of the 5.3GB movie ≈ 22-31s, disk hit ≈ 7ms, across app restarts). `GET /resolve/{addr}` resolves+persists without streaming (returns `{size, chunks}`); `/health` gained `stored_maps`. Flutter: after a list import the Media Lists page offers "Prefetch data maps?" (DataMapPrefetcher walks the imported entries sequentially through /resolve; progress dialog = spinner + `File X of N` + file name + Cancel; declining costs nothing — maps persist on first play anyway). `Engine::new`/`start()` take the data dir; devserver honours WATCHIT_DATA_DIR
- Android $HOME shim (alpha.7): `watchit_core_start(peers_csv, data_dir)` — Dart resolves getApplicationSupportDirectory() in main() (awaited before runApp so no lazy start can race it) and the native side sets HOME/XDG_DATA_HOME/XDG_CONFIG_HOME to it before any ant-core code runs (temp-dir fallback when NULL). Needed because ant-core's config::data_dir unwraps the home-dir lookup → HomeDirNotFound panic on Android; peer cache persists in the app dir as a side benefit
- Connect hardening (alpha.6): each `Client::connect` attempt runs in its own task (panic→error) with 60s timeout; saorsa-core returns Ok with 0 peers when all bootstrap dials fail — engine treats that as failure; reconnect supervisor (2026-07-30): client lives in RwLock<Option<Arc<Client>>>, supervise() re-dials with 2s→60s backoff and evicts on 0 peers x2 polls (POST /reconnect kicks it); `/health` while connecting = `{state, attempts, message}`; panics routed to tracing (logcat tag `watchit_core`). Devserver takes `WATCHIT_PEERS=ip:port` to test the failure path
- Download manager (post-alpha.29, lib/services/download_manager.dart): pure Dart, no Rust changes — streams `GET /xor/<addr>` (deterministic decrypted bytes) to disk one task at a time, resuming partial files with `Range: bytes=<file length>-` (the file on disk, not the DB counter, is the resume offset; progress persists only after flush). Queue persists in the `downloads` drift table (schema v6, address PK); mid-flight rows re-queue on next launch, paused stay paused. Files land in `<support>/downloads/` (Android app-private, no permissions) or a user-chosen desktop folder (Settings → Downloads, file_selector, new downloads only); duplicate names get an addr-prefix. Total size pre-filled via `/resolve` at enqueue. UI: DetailScreen Download button cycles start/pause/resume/retry/remove with a progress bar; Settings → Downloads = queue + pause/resume-all + storage + behaviour. A finished download plays locally: DetailScreen resolves local-path-beats-streamUrl (also for Up-next chaining via PlayerScreen.sourceFor; isLocal skips the network buffering overlay + health poll). Streamed playback with active downloads applies PauseDownloadsOnPlay (ask/always/never, "remember my choice" writes the pref; chained episodes never re-prompt); pauseAllForPlayback tracks exactly what it paused so resumeAfterPlayback (player close, "Downloads resumed" snackbar) spares manual pauses. Race gotcha: resume() can land before an aborted transfer's error surfaces — the catch treats status queued like paused (keep bytes, let the pump retry), else it would mark the re-queued task error. Connection loss auto-pauses instead of erroring: a failed transfer probes `/health` and pauses (no error text) when the client is `connecting` or `ready` with 0 peers; a 2-minute no-data stall timeout on the body stream backstops streams that hang instead of erroring. System pauses (connection loss, waiting-for-Wi-Fi, 6h Android timeout) are flagged pausedBySystem (schema v7) and auto-resume on reconnect/Wi-Fi/app-resume/next-launch; user pauses stay manual
- Watch state/resume points local-only in SQLite; no accounts, no telemetry
- Branding follows the etchit.io family (fetch>it/etch/it): ink/bone tokens, system fonts, mono for XOR addresses — see docs/BRAND.md (mirrors etchit-io/fetchit docs/BRAND.md). DIVERGENCES: accent is blue #42a5f5 since 2026-07-30 (WiTokens accent/accentBright; dark/dim #42a5f5/#64b5f6, light #1976d2/#1565c0), not the family copper; icon centre stripe same blue (WiTokens.bucketBlue). Since 2026-07-31 the DISPLAY NAME is **W@tch** (not the family lowercase-mono compound): wordmark = Anton Regular bundled as app/assets/fonts/Anton-Regular.ttf + OFL (wordmark-ONLY — never UI text), drawn by BrandWordmark in widgets/brand_mark.dart (bone W/tch, accent-blue @). All identifiers stay watchit/Watch-It (applicationId, pubspec, crate, binaries, AppImage filename, repo)
- Repo: github.com/aautonomicc/Watch-It — LICENSING (2026-08-03): own source MIT (LICENSE), but binaries statically link self_encryption 0.36.0 (GPL-3.0) so every released APK/AppImage is a combined work offered under GPLv3 (COPYING.GPL-3.0 at repo root, README ## License). In-app notice: Settings → About → Open-source licenses (showLicensePage) + services/licenses.dart registerNativeLicenses() (called in main()) registering W@tch MIT+GPLv3 note, self_encryption GPL full text (asset assets/licenses/gpl-3.0.txt), watchit_core Rust crate list, libmpv/FFmpeg notice, Anton OFL

## Recent Changes
- [2026-09-02] v0.1.0-alpha.83 "ALBUM MERGING & UPLOAD POLISH" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit 05bce46, feature commits f46dfe5 album/year identity edits + 4859fde prepare step line + fe9e1c8 auto-delete/resume prompt; published ~18:50Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.83 — release mechanics: bump + tag pushed, local release_build.sh → dist/ APK (aapt2 versionCode=83, apksigner cert 7911…0a69 identical) + AppImage; Windows zip from tag CI run 33665805326 (53 files incl. data/app.so + watchit.exe, sha256 bf095d00…) → dist/; clean-HOME Xvfb :78 smoke of the dist AppImage (Terms renders, log "7 bootstrap peers" + peer connected, /tmp/wi83-smoke.png); gh release create --latest, confirmed Latest + 3 assets; main CI green on the bump. Notes at /tmp/wi83/notes.md.
- [2026-09-02] AUTO-DELETE FINISHED BATCHES + CRASH-RECOVERY RESUME PROMPT (RELEASED in alpha.83; implements the two discussion asks "delete a batch once every file uploaded successfully" + "prompt to restart the upload after a crash/shutdown"; user confirmed forgetting a batch's uploads is not a concern): (1) FINISHED BATCHES ARE SWEPT, NOT KEPT — `session.clear()` on a fully successful done batch now removes the WHOLE work dir (was: only encodes/+art/, keeping manifest/datamaps/bundle as "records"); nothing load-bearing lives there (playback addresses in the library, root maps in the core store, dedup hashes in the shared ledger). NEW `deleteFinishedBatches(root, {keepPath})` (batch_upload.dart) sweeps every `finished` batch dir (all entries uploaded/deduped/skipped, incl. attention/ready rows whose source file is gone) — `keepPath` spares the live session's dir at the done stage. The batch screen's `_loadAttention` calls it first, then shows only unfinished batches, so the Previous-uploads card lost its "N finished batches kept as upload records / Show" toggle entirely (Dismiss now flips needs-attention→skipped which makes the batch finished → swept on next reload). Done-page bundle note tells fully-successful users to "Save bundle to…" since working files are tidied on Done. (2) CRASH-RECOVERY PROMPT — NEW top-level `offerBatchResume(context, …)` fired once per launch from HomeScreen.initState (desktop-only, silent no-op otherwise): sweeps finished dirs, then `scanInterruptedBatches` (batches with still-ready/failed files whose source exists); if any, an AlertDialog "Finish an interrupted upload?" ("…was cut short — N files never made it up… Continuing is safe: anything already uploaded is recognised and never paid for twice") → Continue upload pushes BatchUploadScreen (new `resumeDir` ctor param → auto-`resumeBatch` at the review page when exactly one waiting; the management list when several). Once-per-launch guard `_batchResumeOffered` (+ `resetBatchResumeOffer`/`batchResumeRootOverride` test hooks); BatchUploadScreen gained `ffmpegOverride` test seam (real FfmpegService.available spawns a process that hangs the fake-async zone). Tests 831 app (+5 net: clear()-sweeps-done vs keeps-failed, deleteFinishedBatches+scanInterruptedBatches, 2 startup-prompt widget flows incl. once-per-launch + multi-batch management list, previous-uploads card test rewritten for sweep-on-sight) + analyze clean. LIVE Xvfb :91 clean-HOME verify (debug bundle + release .so, seeded `<support>/batch_uploads` with an interrupted "Home Movies" 1-ready+1-uploaded batch + a finished "Old Finished"): home wall → the resume dialog renders naming "Home Movies" cut short (/tmp/wiresume-1-prompt.png), the finished done-batch was swept on sight (only cut-batch left on disk), Continue → review page "1 ready · 1 uploaded" with Road Trip Clip (2024) waiting + Already Up (2023) green-checked + live mainnet estimate ≈0.199181 ANT + wallet gate (/tmp/wiresume-2-continue.png). Ships with the next release.
- [2026-09-02] BATCH-PREPARE STEP LINE + FINGERPRINT PROGRESS (RELEASED in alpha.83; implements the "~20s for movie data to come through" follow-up — the delay is the sha256 fingerprint read of the whole movie, and the preparing page looked hung on "Matching files · 0 of 1"): the preparing page now shows WHAT the scan is doing to the current file. SESSION (batch_upload.dart): new `PrepareStep {fingerprint, mediaInfo, match}` + `prepareStep`/`hashFraction` fields, set through the per-file loop (fingerprint with byte progress → ffprobe → MB/TMDB lookup; also set on the album legs), cleared on every terminal/reset path (scan end, error, ledger-hit continue, startPrepare/resumeBatch/clear). CLI: `sha256OfFile` gains optional `onBytes(bytesRead)` callback (cumulative, per 64KB chunk; CLI callers unchanged); session throttles notify to ≥1% steps. SCREEN: under the current file name — fingerprint step gets its OWN determinate LinearProgressIndicator + "Fingerprinting · NN% — reading the whole file once so anything you already uploaded is recognised and never paid for twice. Large files take a while."; mediaInfo = "Reading media info…"; match = "Looking it up on MusicBrainz…"/"…the movie database (TMDB)…" by file extension (answers the user's MB-first question in the UI). Tests 827 app (+2: service step/fraction sequence incl. cleared-at-end, widget fingerprint-bar/42%/wording + per-type match wording) / 20 cli (+1 onBytes cumulative + digest-unchanged) / analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, real 2.5GB file picked through GTK): "Fingerprinting · 11%" then "· 70%" with the sub-bar visibly growing under the file name (~25s hash — the user's exact symptom, now explained on screen), flow lands in the normal Review-matches carousel untouched (/tmp/wistep-shots/9-prep-3.png, -13.png, 10-now.png). Ships with the next release.
- [2026-09-02] ALBUM/YEAR EDITS RENAME THE WHOLE ALBUM (f46dfe5, RELEASED in alpha.83; implements the "I edited album name + year to combine loose tracks but they stayed separate cards" discussion — album grouping reads only file names, so identity edits must rename): the track editor's Album + Year fields are now IDENTITY like the track number. (1) NEW watchit_naming `realbumedMusicFileName(name, album:, year:)` (generate.dart): swaps ONLY the `Album (Year)` head segment on the raw name (artist/marker/title verbatim), DROPS any `{mbid-…}` tag (the edit overrides the database match — keeping it would pin the track to the old release cluster and defeat the merge), sanitizes the typed album, null unless the result parses back losslessly + fits 255 bytes; re-exported via app metadata.dart (with sanitizeNamePart). (2) NEW top-level `renameTrackAlbum` (edit_details_screen.dart, AlbumRenameResult typedef): renames EVERY track of the album fold (AlbumKeys, per list holding the edited entry; the collected (address,name) set renames in every list so an entry never has two names), migrates per-track override rows `<key>:tD-NN` → renamed keys, carries the old album row's artwork to the new album key when that key has none (fresh `saveUserPoster` copy, never a shared file), and REFUSES when a moved track lands on a (disc,track) an existing target-album track holds ("renumber that track first") — collision check compares NEW public `AlbumKeys.clusterFor` (pre-split cluster), NOT keyFor: the same-number-different-artist split guard would otherwise hide exactly this collision and quietly split the combined album apart (caught by the widget test on the first run). (3) TRIGGER RULE in _save: rename only when sanitized(album) != parsed.title || year != parsed.year AND (field text changed from its prefill OR the differing prefill row is userEdited — the pre-existing display-only override HEALS into a rename on a plain Save); a database match's canonical title merely differing from the file name never renames on an untouched save (else every description edit on a matched album would silently rename + drop the mbid). Save then writes the user row under the NEW lookupKey (staged posters too); combined number+album edits renumber first, then re-album. Editor helper text + docs/NAMING.md explain "same album + year (distinct numbers) = one card". Tests 41 naming (+7 realbumed golden incl. mbid drop, sanitize, digit/dashed albums) / 825 app (+6 in music_edit_test: year-only renames fold, sequential loose-single combine → ONE VA fold key, target-number collision refused, mbid-drop+row-migration+art-carry service test, canonical-mismatch-never-renames, override-heals; first widget test re-pinned to rename semantics), analyze clean. GOTCHA: `await MetadataService.instance.whenIdle()` inside testWidgets HANGS — metadataFor schedules via Future() (a zero timer) which fake-async never fires unpumped; warm the service with `tester.pump(const Duration(seconds: 1))` ×2 after pumping a SizedBox instead. LIVE Xvfb :77 verify (release bundle + release .so, /tmp/wi-albumid HOME seeded w/ 2 loose singles): the user's exact scenario — Singer A/Singer B singles shown as 2 separate cards → edit each to Album "My Album" Year 2006 through the real editor → sqlite names renamed, home wall shows ONE "My Album / Various Artists · 2 tracks" card, album page "Various Artists · 2006 · 2 tracks" w/ ordered 01/02 tracklist (/tmp/wialbumid-1-home.png … -10-albumpage.png). Ships with the next release.
- [2026-09-02] v0.1.0-alpha.82 "RESUME & FORGET" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit 0e3e6f6, feature commits d1b9144 collage + d890809 resume/forget; published ~16:05Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.82 — ships resumable interrupted batches + previous-uploads tidy + free re-add + forget-uploads opt-in + adaptive artist collage. Release mechanics: bump 0e3e6f6 + tag pushed, local release_build.sh (~3 min warm, watchit-build unit) → dist/ APK (aapt2 versionCode=82, apksigner cert 7911…0a69 identical) + AppImage; clean-HOME Xvfb :78 smoke of the dist AppImage (Terms renders, log "bootstrap peers" + connected, /tmp/wi82-smoke.png); Windows zip from tag CI run 33651198026 (53 files incl. data/app.so + watchit.exe + ffmpeg.exe, sha256 1a150ee2…) → dist/; gh release create --latest, confirmed Latest + 3 assets; main CI green on the bump commit. WATCH-OUT: grep for build failures in /tmp/watchit-build.log must not use plain "error" — libgpg-error matches it; key on "ALL BUILDS DONE"/unit state instead.
- [2026-09-02] FORGET-UPLOADS OPT-IN (RELEASED in alpha.82; implements the "fully remove it from the W@tch app" discussion — deleting media from the library deliberately does NOT touch upload records, so forgetting is an explicit action instead): the Previous-uploads **Delete batch** dialog gains a default-OFF checkbox "Also forget this upload / these N uploads" (shown only when the batch has content hashes recorded — manifests predating sha256 rows never offer it): ticking it calls new `forgetUploads(batch, configDir:)` (batch_upload.dart) → `Ledger.removeAll(shas)` (cli ledger.dart — the ONE deliberate exception to the ledger's append-only rule; rewrites ledger.jsonl without those hashes, compacting corrupt/duplicate lines, returns rows dropped) over `PreviousBatch.uploadedShas` (NEW field: shas of `uploaded` + `already-uploaded` manifest entries — deduped rows count as the batch's uploads too, skipped don't). Copy warns: same files through a new batch upload — and PAY — again; bytes on the network stay either way, "this only makes this app forget". Tests: 19 cli (+1 removeAll forget/reload/re-append), 819 app (scan test asserts uploadedShas incl. already-uploaded & not-skipped + forgetUploads drops exactly the batch's rows; card widget test drives the checkbox — ledger row gone, unrelated row kept, no-checkbox for sha-less batches), 34 naming, analyze clean. LIVE Xvfb :77 clean-HOME verify (/tmp/wifg, seeded interrupted "Home Movies" batch w/ sha + 2-row ledger): dialog renders unticked checkbox + copy, tick → Delete → batch dir gone AND ledger.jsonl holds only the unrelated row (/tmp/wifg-6-dialog.png, -7-ticked.png, -8-after.png). GOTCHA (recurring): pkill -f "Xvfb :77" in a compound Bash call self-matches → exit 144; use pgrep -x Xvfb | xargs kill.
- [2026-09-02] RESUMABLE INTERRUPTED BATCHES + TIDIER PREVIOUS-UPLOADS + FREE LIBRARY RE-ADD (RELEASED in alpha.82; implements the discussion "continue a batch cut short mid-upload; hide finished batches; deleting a batch keeps the media library"; user Q answered — YES, Delete only removes the batch's OWN records): (1) RESUME — new `BatchUploadSession.resumeBatch(api, workDir, …)` reopens an interrupted batch straight at its review page: loads the manifest, flips `failed`→`ready` (clears error), drops `ready` rows whose source file is gone to `failed`, COLLAPSES per-tier sibling rows back to one `ready` per source (tiers re-picked + re-expand at upload), then `_finishPrepare()` (probe videos + live estimate). `_videoItems` now filters out tier outputs already `uploaded`/`already-uploaded` for that source (new `_uploadedNamesFor`), and `_buildTasks` skips a non-tier ready row whose name is already on the network (→`skipped`, never re-paid). (2) PREVIOUS-UPLOADS CARD — `PreviousBatch` gains `resumableSources` (ready/failed entries whose file still exists) + `interrupted`/`finished` getters; the setup card splits: unfinished batches (attention OR interrupted) always shown with a NEW **Continue** button (interrupted) beside Review/Dismiss (attention) + Delete; fully-finished batches (all uploaded/deduped/skipped) hide behind a "N finished batch(es) kept as upload records" line with a **Show** toggle (Delete stays reachable there). Delete-dialog copy clarified: removes ONLY the batch's manifest/bundle/working files — "Anything already added to your library stays there, files already uploaded stay on the network". `_countsLine` now also shows uploaded/failed. (3) FREE LIBRARY RE-ADD — the review page's Upload button becomes "Add to library" when readyCount==0 but dedupCount>0 (calls startUpload, which pays nothing, rebuilds the bundle, and auto-adds the deduped files to the chosen list) — the recovery path for media deleted from the library by mistake; explains "finishing is free and adds them to '<list>'". Tests 819 app (+3: resumeBatch continue/kept/failed/tier-collapse service tests, previous-uploads card Continue+Show+Dismiss+Delete widget flow, PreviousBatch getters) + analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, seeded `<support>/batch_uploads` with an interrupted "Home Movies" 1-ready+1-uploaded batch whose ready source exists + a finished "Old Stuff"): Upload doorway → batch screen shows "Home Movies · 1 uploaded · 1 ready" + Continue, finished batch collapsed behind "1 finished batch kept as upload records"/Show → Show reveals Old Stuff (Delete-only) → Continue reopened the batch at the review page "1 ready · 1 uploaded" with Cut Short (2002).mp4 waiting + Already Up (2001).mp4 green-check kept + live storage quote (/tmp/wiv-6-batch.png, -7-show.png, -8-continue.png). NOTE (3rd discussion Q — dedup vs library membership): the content-hash ledger dedup does NOT check whether a file is still in the media library; a file uploaded then deleted from the library is recognized by the ledger and skipped (not re-added) on a normal re-prepare — the free "Add to library" re-add path above is how to get it back without re-paying. Ships with the next release.
- [2026-09-02] ARTIST-CARD COLLAGE ADAPTS TO THE ALBUM COUNT (d1b9144 pushed, RELEASED in alpha.82; implements the discussed fix for "2 albums show as a quad with each cover twice"): ArtistCard's collage no longer cycles covers (`albums[i %% albums.length]`) — each album appears exactly ONCE: 2 albums = two side-by-side full-height halves, 3 = one full-height half (first album, year-sorted) + two stacked right-hand quarters, 4+ = the 2×2 grid of the first four. Tests 817 (+3: per-count widget tests pinning cell counts + geometry via fallback-icon centres) + analyze clean; LIVE Xvfb :77 verify on the reused /tmp/wi-art-home seeded HOME: Stones card renders blue|red halves (was quad w/ dupes), then a seeded third album (Aftermath 1966, green cover) → green half + blue/red quarters, "3 albums · 5 tracks" (/tmp/wiadcol-1-home-2albums.png, -2-home-3albums.png). Ships with the next release.
- [2026-09-02] v0.1.0-alpha.81 "EDITABLE TRACK NUMBERS & TIDY BATCHES" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit dae75ca, feature commits 260d1e0 + 1f08dc0, published ~14:40Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.81 — ships the track-number/disc editor, per-batch upload isolation + Previous-uploads management, and the artist-agnostic album fold. Release mechanics: bump dae75ca + tag pushed, local release_build.sh (~4 min warm) → dist/ APK (aapt2 versionCode=81, apksigner cert 7911…0a69 identical to prior releases) + AppImage; clean-HOME Xvfb :78 smoke of the dist AppImage (Terms renders, engine log "7 bootstrap peers" + DHT peer connected, /tmp/wi81-smoke.png; known harmless NetworkManager DBus exception headless); Windows zip from tag CI run 33640403294 (53 files incl. data/app.so + watchit.exe, sha256 14e57c5b…) → dist/; gh release create --latest, confirmed Latest + 3 assets; main CI green on the bump commit.
- [2026-09-02] TRACK-NUMBER EDIT + BATCH-BLEED FIX IMPLEMENTED (1f08dc0 pushed, UNRELEASED — the 2026-09-02 plan is now code, both user reports answered): (1) TRACK EDITOR gains Track number + Disc (optional) fields — the number is identity (file name), so Save RENAMES the entry: new watchit_naming `renumberedMusicFileName(name, track:, disc:)` (generate.dart, re-exported via app metadata.dart) swaps ONLY the `NN`/`D-NN` marker on the raw name and returns null unless the renamed name re-parses to identical artist/album/year/title/mbid with the new numbers (lossless by construction); top-level `renumberTrackEntry` in edit_details_screen.dart renames the entry in EVERY list holding it (matched by name+address), refuses when the album (AlbumKeys fold = what the wall calls one album) already holds the target (disc,track) on another entry, and migrates the per-track override row `<key>:tD-NN` → renamed key with the marker updated in its `NN · Title` label; _saveTrackTitle then writes under the NEW parsed name. disc null = plain `NN`, value = `D-NN`. Address untouched (playback/downloads/ledger unaffected); detail/album pages built pre-rename show the old number until re-entered (entry objects are copies). (2) BATCH ISOLATION: `batchDirName(list, now)` = slug + start timestamp (`Music-20260902-153000`) — every batch gets a FRESH work dir, so a new batch can never inherit the old manifest's counts or (real-money risk) its abandoned `ready` files into the estimate + startUpload; cross-batch dedup stays the sha256 ledger's job. The earlier-batches card is now a PREVIOUS UPLOADS management list (`scanPreviousBatches` → PreviousBatch{workDir,listName,created,counts,attentionSources}, newest first; scanAttentionBatches reimplemented as its filter): per-batch name/date/status-counts rows + Review (unchanged, attention only) + Dismiss (`dismissBatch`: needs-attention → skipped in the manifest; ledger/bundle kept) + Delete batch (confirm dialog → `deleteBatch` rm -r workDir). Hygiene: session.clear() from a fully-successful done page (no failed/attention) deletes the batch's encodes/ + art/ dirs — manifest/datamaps/bundle stay. No migration: the old merged `batch_uploads/Music/` dir keeps surfacing until Dismissed/Deleted. Tests 34 naming (+5 renumber golden incl. digit-leading title) / 814 app (+7: renumber rename+collision widget tests, row migration, disc move, batchDirName, scan+dismiss+delete service, card widget flow) / 18 cli, analyze clean ×3. LIVE Xvfb :77 verify on the wi-albfold seeded HOME (reused as /tmp/wi-tracknum + seeded batch dirs): editor shows the new fields, 01→07 renames the sqlite row + album page re-sorts 02/03/07 (one card, same address), Previous-uploads card shows both batches w/ counts, Dismiss flips header to "Previous uploads" + counts to skipped, Delete dialog removes row + dir (/tmp/witn/10-editor.png, 14-album-fresh.png, 18-batchsetup.png, 19-dismissed.png, 21-afterdelete.png). GOTCHA: default window is 1280 wide — Xvfb screens narrower than that clip the right edge (track-row ⓘ buttons invisible); use 1300x900.
- [2026-09-02] ALBUM FOLDING IGNORES THE PER-TRACK ARTIST (260d1e0 pushed, UNRELEASED — post-alpha.80; fixes the user report "renamed loose tracks into the same album but the home screen shows separate cards that each say they're in that album"): a track's fold key was its parsed lookupKey, which embeds the artist (`music:<artist>:<album>:<year>`) — so an album whose tracks credit different artists (the natural hand-rename of loose tracks into a compilation) split into one wall card per artist, and the documented mixed-artists→Various-Artists detection was unreachable without a shared mbid. NEW `AlbumKeys` in season_grouping.dart (the VersionKeys idiom for music), used by groupSeasons for the album fold: tracks cluster by `{mbid-...}` tag, else album title + year with the artist LEFT OUT; a year-less track adopts the only year its album name carries elsewhere in the list, an untagged track adopts the only mbid claiming its album title(/year). Safety guard: a cluster whose (disc,track) number is credited to ≥2 different artists is really two same-named albums (two "Greatest Hits") and splits back out per artist — mbid clusters never split (tag = one release by declaration). Display: AlbumCard + album-page header now let `group.isCompilation ? group.artist` ("Various Artists") beat the first track's `meta.artist` (ArtistCard untouched — artists never hold compilations). All groupShows consumers (wall, list grid, editor tree, search, downloads/favourites) get the fold for free. docs/NAMING.md music section documents it. Tests 807 (+7: VA fold no-mbid, year adoption, mbid adoption, Greatest-Hits split, VA AlbumCard credit line) + analyze clean; LIVE Xvfb :77 clean-HOME verify (sqlite-seeded Music list, 3 per-artist-named tracks "Singer A/B/C - Road Mix (2002) - NN X.mp3"): ONE "Road Mix / Various Artists · 3 tracks" card on wall + Music row (was 3 cards), album page "Various Artists · 2002 · 3 tracks" with ordered tracklist (/tmp/wialbfold-2-home.png, -3-album.png). Main CI green. NOTE: audio files with NO track number still deliberately stay single entries (docs/NAMING.md) — grouping needs `NN Title`.
- [2026-09-02] v0.1.0-alpha.80 "ARTIST CARDS & CLEARER UPLOAD DOORWAY" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit 0b7d7ac, feature commits 7fcfeb1 + c3cc5e2; published ~12:48Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.80 — ships the artist fold (2×2 collage cards + artist page), state-aware upload doorway (running-batch card returns straight to the upload), visible auto-accept confirm cards with inline art, batch-free single-file copy, live needs-attention refresh. Finished across two sessions: the first built APK+AppImage into dist/ (versionCode 80 / cert 7911…0a69) and pushed the tag; this one verified the assets (Windows zip from tag CI 33628868851, sha256 4232e890…, 53 files incl. app.so), clean-HOME Xvfb :78 smoke of the dist AppImage (Terms renders, log "connecting to Autonomi network … 7 bootstrap peers", /tmp/wi80-smoke.png) and published (--latest confirmed, all 3 assets). NOTE: CI run 33628855487 on feature commit 7fcfeb1 failed ONE test (channels_screen_test "failed publish offers Try again" — widget-tap flake); the two later CI runs on identical code (bump + docs commits) passed it.
- [2026-09-02] DOCS REFRESH THROUGH ALPHA.79 (docs-only, no code/tests; done while the alpha.80 release was building): README/ROADMAP/UI-DESIGN/VISION caught up from the 2026-08-27 alpha.62 refresh. README: intro gains the music pitch (album wall + inline player, keyless MB/CAA) + batch-upload rewrite of How-it-works step 1 (auto-matching, dedup ledger, CLI pointer), step 3 music naming, Status extended alpha.63–.79, docs list gains UPLOAD-CLI.md. ROADMAP: status header rewritten (2026-09-02/alpha.79), Channels section heading unreleased→shipped alpha.65 + 3 new ticks (paid publish verified, delta updates .68, profile .70), Upload section +2 ticks (resilient batches .72, batch upload pointer) + stale post-alpha.62 notes fixed to .63, TWO NEW SECTIONS "Music (alpha.76/.78/.79)" (album wall/inline player/music edit/artist grouping-next-release; open: artist bio pages, Video/Music split) and "Batch upload with auto-matching (alpha.77/.78/.79)" (CLI, in-app screen, album-at-a-time, one-flow; open: mobile), parking-lot music struck as shipped. UI-DESIGN: Built-so-far → alpha.79 with a .63–.79 paragraph. VISION: goal 5 + MB/CAA, goal 7 + batch matching, "no music-first" non-goal amended (music shipped, app stays video-first). (UNRELEASED — post-alpha.79; implements the two discussion sessions): (1) ARTIST FOLD — groupShows now folds an artist's albums under ONE wall card once they have 2+ albums in the list (the music mirror of the show→season fold): new sealed HomeArtist {artist, albums sorted year→title, trackCount}; HomeAlbum gains year + isCompilation (per-track parsed-artist mismatch → artist becomes "Various Artists", or an explicit Various Artists credit — compilations NEVER fold, they stand alone; single-album artists stay plain album cards). New widgets/ArtistCard (120px square 2×2 collage of album covers, cycled when <4, download badge, "N albums · M tracks") + screens/artist_screen.dart (name header + counts + ALBUMS wrap of AlbumCard tiles → AlbumScreen). Wired through every HomeItem switch: home wall + list-page grid (both open ArtistScreen), list editor artist→album→track ExpansionTile tree (_albumTile gains nested indent; artist menu curates all tracks), SearchIndex (albums under a HomeArtist index per-track exactly as before via extracted indexAlbum; compilation tracks also index their OWN parsed artist so "singer b" finds the duet track). (2) BATCH ATTENTION REFRESH — batch_upload_screen's earlier-batches needs-attention card was loaded ONCE in initState and stayed stale after reviewing/dismissing batches until the screen was reopened; _onSession now re-runs _loadAttention() + _loadLists() on every session stage change (manifests rewrite during prepare/decide/upload; also picks up lists a finished batch created). Tests 800 (+9: fold-by-year/VA-never-fold/mixed-artist-compilation units, ArtistCard + ArtistScreen widget incl. tap-through to AlbumScreen, editor artist-tree, 2 search tests, batch widget test proving the card clears after a Review resolves without leaving the screen) + analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, sqlite-seeded Music list: 2 Stones albums + 1 other-artist album, local cover art): wall + list grid show ONE "The Rolling Stones · 2 albums · 4 tracks" collage card beside the standalone "Elsewhere" album card, artist page tiles sorted 1968→1969, tap → album page (/tmp/wiartist-1-home.png … -5-listpage.png). Ships with the next release.
- [2026-09-02] v0.1.0-alpha.79 "ONE UPLOAD FLOW" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit 0b09176, feature commit 879f3a0; published ~09:35Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.79 — ships the six-pack + downloads-default + music-edit entry below. Local watchit-build unit built APK+AppImage straight into dist/ in 3min (Dart-only, warm caches; APK versionCode 79 / cert 7911…0a69, sha256 e58c8f30…; AppImage e14f2511…), Windows zip from tag CI 33611736064 (sha256 ce8f31e3…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit.exe, watchit_core.dll 48.7MB, app.so contains "Review matches"), main CI green; clean-HOME Xvfb :78 smoke of the exact dist AppImage (Terms → seeded home connected · 5 peers → Settings → Upload shows the merged one-flow doorway, /tmp/wi79live/smoke-*.png); published with /tmp/wi79-release-notes.md, no --prerelease → Latest confirmed via releases/latest, all 3 assets.
- [2026-09-02] BATCH-UPLOAD SIX-PACK + DOWNLOADS DEFAULT + MUSIC EDIT IMPLEMENTED (releasing as alpha.79; the two 2026-09-02 planning sessions are now code): (1) TIER MERGE — publish_screen's tier flow DELETED (publish_session.dart gone; screen is now just wallet row + "Upload files or folders" + needs-attention pointer; pickTargetLists stays there, addEntriesToLists MOVED to library_store.dart); QUALITY lives on the batch review page: session gains ffmpeg param + videoProbes (app-side publish_plan probe — the CLI probe lacks codec fields) + tiers set; startUpload expands each ready hasVideo entry via buildQueue into per-tier _UploadTasks (encode to workDir/encodes, sha256 of the ENCODED file on its ledger row, sibling ManifestEntry per extra tier so manifest/bundle/library carry every version, videoInfoByName feeds '720p H.264' labels, tierOutputName folds into the version picker), estimate counts predicted encode sizes, rights checkbox moved to batch review gating Upload. (2) CONFIRM CAROUSEL — prepare no longer blocks per-Completer: confirm-needing files/albums queue into session.confirmables during the scan, reviewed afterwards as "Review matches · N of M" with prev/next arrows; decisions record via _recordOutcome (upsert on manifest.bySource) + auto-advance to next undecided → review+estimate when none left; decided cards reopen (confirmGoTo / reopenConfirmForSource from tappable review rows, "Already answered — a new choice replaces the earlier one", Back to summary button); BatchConfirm/AlbumConfirm carry sha/size/decided, completers gone. (3) NEEDS-ATTENTION FROM EARLIER BATCHES — scanAttentionBatches(<support>/batch_uploads) reads each watchit-manifest.yaml for needs-attention entries whose source still exists; Upload screen shows an amber pointer line, batch setup a card with per-batch Review button → startPrepare over just those files in the batch's own workDir under its original list (existing-entry upsert path re-raises the cards). (4) ART/DETAILS BUG FIXED — after _writeBundle the session parseBundle+seedBundle's its OWN bundle (importHistory:false, try/catch) so case-B manual details/art land in the LOCAL metadata cache. (5) LIST BUTTONS + AUTO-ADD — setup dropdown replaced by done-page-style buttons (FilledButton.tonal `Add to "Music"` + OutlinedButton "Choose another list…" → radio SimpleDialog incl. create-new); on finish the session AUTO-ADDS uploads to the chosen list (addEntriesToLists w/ videoInfo) and the done page shows "Added N titles to X" instead of re-asking (manual button kept as fallback). (6) Done → session.clear() + popUntil(isFirst) → home wall. (7) DOWNLOADS DEFAULT — download_manager._downloadsDir: desktop default is now getDownloadsDirectory() (system Downloads folder) before the app-private fallback; Settings→Downloads subtitle says "System Downloads folder (default)". (8) MUSIC EDIT ALL FIELDS — drift schema v12: metadata_cache gains `artist` (migration from>=4&&from<12; the v10 fixture test needed the table added); MediaMetadata.artist threaded through _fromCache/_fetchMusic/fallback (parsed artist fallback) + saveUserDetails artist Value param (preserved in applyRemoteUserDetails) + bundle metadata.json rows; NEW trackLookupKey ('<albumKey>:tD-NN') — a track's own title lives in a per-track userEdited row whose episodeLabel overlays trackLabel via _overlayFor (now carries episodeLabel); EditDetailsScreen track scope ("Edit track details"): Artist/Album/Year/Track title/Description/Artwork — album fields hit the shared row, track title its own row (typing the original back clears it), Remove-edits clears both; album_screen header artist + track rows and AlbumCard artist read meta (edits show everywhere). Tests 791 app (+6 skipped; net −2 — the old tier-flow tests went with the flow; new: carousel re-decision incl. reopen-flips-status, auto-add+seed w/ metadataRowFor pin, tier expansion e2e incl. per-output ledger rows + bundle members + library videoInfo, attention scan+resume-upsert, carousel-header widget, publish_flow rewritten slim ×4, music_edit ×4) + 18 cli + 29 naming + analyze clean; GOTCHA: in-memory drift opened in one test zone deadlocks queried from the other — keep a widget test entirely fake-zone OR entirely runAsync. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, real MB/CAA, mainnet estimate): tagged sine album + 720p HEVC clip → "Review matches · 1 of 2" carousel w/ arrows, real Let It Bleed card (3/3 placed, real CAA art) accepted, manual video entry, review page shows canonical names + QUALITY (Medium/Low ticked, Original unticked) + "5 uploads ≈2.142602 ANT" LIVE quote + rights gate (wallet-gated, unfunded as ever — paid leg pinned by fake-server tests incl. auto-add/seed); tap-row reopen + Back-to-summary; attention fixture: Upload pointer line + setup card + Review re-raised the card; sqlite-seeded Music list → Edit track details (all fields prefilled from filename) → artist/track-title edits render on album header + track rows + DetailScreen; Settings→Downloads "System Downloads folder (default)" (/tmp/wi79live/*.png, esp. 14-batchsetup, 18/19-carousel, 22-review, 24-rights, 27-attention-review, 6-trackeditor, 9-albumafter).
- [2026-09-01] v0.1.0-alpha.78 "ALBUM-AT-A-TIME REVIEW" RELEASED (Latest: AppImage + Windows zip + APK; tag on bump commit 7a4cab7, feature commit 087f002; published ~23:15Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.78 — implements the user ask "auto-match a full album at a time for review, not track by track" AND fixes the correctness hole where per-track matching could split one album across releases. SESSION (batch_upload.dart): audio files WITHOUT a per-file sidecar defer into album groups during prepare (new cli albumGroupKey: embedded MB release id → normalized artist/album tags → `Artist - Album - NN Title` filename guess → parent dir; per-file path extracted into _matchAndRecord/_recordOutcome); per group the release is DISCOVERED ONCE (tracks carrying probe.releaseMbid tried first) then EVERY track resolves against that mbid via the matcher's sidecar path (one release by construction); id-backed high-confidence albums with all tracks placed AUTO-ACCEPT, everything else raises ONE pendingAlbumConfirm (new AlbumConfirm: ordered tracks, per-track outcomes, representative art/note, albumLine strips the ", track N" note suffix) with session actions albumAccept/Skip/Reject/Search/PickMb/PasteId (MB release ids only)/albumManual (case-B: artist/album/year/art entered once, per-track titles+numbers from tags or file names, completes when all tracks resolve); groups of ONE keep the old per-file card (incl. music/video toggle); clear/reset fail the album completer. UI (batch_upload_screen.dart): _albumConfirmCard — CONFIRM ALBUM MATCH / NO ALBUM MATCH header, 72px cover, "N of N tracks placed", per-track ✓/? rows with canonical mono names, Use-for-all-N button + Search/Paste ID/Enter album details dialogs; setup copy says "Music is reviewed one whole album at a time". CLI: _fromSidecar release rematch gains titleHint fallback (untagged unnumbered tracks place by title similarity — benefits the CLI too) + caaFrontCover in-memory cache per release (definitive answers only) + albumGroupKey exported. Tests 793 app (+5: tagged-album-auto-accepts-no-cards, search-match-raises-ONE-card + ordered names, reject-lands-whole-album, no-match→albumPickMb-re-resolves-all, widget NO-ALBUM-MATCH card + Enter-album-details covers both tracks) / 18 cli (+1 albumGroupKey matrix) + analyze clean both. LIVE Xvfb :77 clean-HOME verify vs REAL MusicBrainz+CAA (3 tagged sine mp3s t1-t3, artist/album/title/track tags, no mbid): ONE "CONFIRM ALBUM MATCH" card with real Let It Bleed cover art, "The Rolling Stones — Let It Bleed (1986)", 3 of 3 tracks placed sharing mbid 499485cb…, one click → 3 ready + live mainnet estimate ≈0.770461 ANT + wallet gate (/tmp/wialbum78/8-albumcard.png, 9-review, 10-estimate); dropdown auto-defaulted to Music (7-picked). RELEASE: local watchit-build unit APK+AppImage straight into dist/ (APK versionCode 78 / cert 7911…0a69, sha256 3d6d0d22…; AppImage 2a148de4…), Windows zip from tag CI 33567560610 (sha256 7f3af47d…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit_core.dll 48.7MB), main CI green; clean-HOME Xvfb :78 smoke of the exact dist AppImage (Terms → home → batch screen shows the new album copy, /tmp/wialbum78/smoke-*.png); published with /tmp/wi78-release-notes.md, no --prerelease → Latest confirmed via releases/latest, all 3 assets.
- [2026-09-01] ALBUM PLAYER + UPLOAD MERGE + BATCH LIST DROPDOWN + NO-MATCH MANUAL ENTRY (UNRELEASED — post-alpha.77; implements the whole next-release discussion in one piece, user picked "subtle glow pulse"): (1) ALBUM PAGE PLAYS INLINE — album_screen.dart rewritten stateful: tapping a track plays it ON the album page through a lazily created media_kit Player (new injectable `AlbumAudioPlayer` seam + `sourceOverride` for widget tests), with seek bar + time labels, transport row (shuffle · previous · play/pause · next · favourite-heart for the current track), auto-advance to the next track (shuffle = random no-repeat pass, ends at album end), prev = restart-after-3s / history under shuffle, current row swaps its mono number for a graphic_eq mark, per-row ⓘ button keeps DetailScreen (download/file info) reachable, "Play album" button when idle; the cover art gets a SUBTLE ACCENT GLOW PULSE while playing (AnimationController 2.2s repeat/reverse driving a BoxShadow, fades out on pause). Streaming tracks get the same gates as DetailScreen: cellular block/ask + pause-downloads-on-play — those two dialogs extracted from _DetailScreenState to top-level `maybePauseDownloadsForStreaming`/`confirmCellularStreaming` in detail_screen.dart (shared, downloads resumed on page close/album end). (2) UPLOAD SCREEN MERGED TO ONE OPTION — publish_screen.dart setup: single primary FilledButton "Upload files or folders" → BatchUploadScreen; the old tier-encoding flow demoted under an ADVANCED header as "Encode quality versions…" ("files keep their own names — no database matching"); both old competing buttons gone. (3) BATCH LIST DROPDOWN — batch setup's free-text "List name" replaced by an "Add to list" DropdownButtonFormField over existing non-channel lists + Music + My uploads + "Create a new list…" (promptForText); DEFAULT FLIPS TO "Music" when the picked files are mostly audio (new pure `defaultBatchList`, kAudioExtensions; ValueKey(_list) so the FormField follows the flip); done page's primary button is now "Add to \"<list>\"" (straight into the chosen list) with "Add to other lists…" keeping the multi-list picker. (4) NO-MATCH FILES GET THE CONFIRM CARD (closes the user-reported alpha.77 gap): batch_upload.dart auto-accept condition inverted so unmatched outcomes now raise pendingConfirm (card header says NO MATCH FOUND, no "Use this match") — Search/Paste ID/Enter details/Treat as music·video/Skip all available before needs-attention; the Enter-details dialog gains a Video|Music SegmentedButton (fields follow) and "Artwork from file…" image picker riding Sidecar.art into the case-B bundle row (CLI machinery already supported it). Tests 783 (+7: album_player_test.dart 5 fake-player widget tests incl. shuffle-visits-once + heart, no-match-confirm-card widget test with the type toggle driven via tester.runAsync — prepare does real IO — and defaultBatchList unit; updated pins: confirmSeen now includes the no-match file, manual-entry test uses a truly-unmatched file, Upload-screen link test pins the merged layout, publish_flow 'Choose files to upload' → 'Encode quality versions…') + analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, seeded Music list of 3 real sine-wave mp3s with REAL Let It Bleed mbid 499485cb… + downloads rows status done → local playback; pulseaudio null sink so mpv has an AO — `pulseaudio --daemonize=no --load=module-null-sink` with XDG_RUNTIME_DIR, kill stale pid first): real CAA cover on the wall card, album page Play-album/ticks/ⓘ, tap track → REAL AUDIO PLAYING with moving seek bar + glow visibly pulsing between shots, auto-advance track2→track3 observed, heart persisted to favourites_v1, shuffle lit, album-end stops cleanly with glow faded; Upload screen shows the merged layout; GTK-picked track1.mp3 → dropdown AUTO-DEFAULTED TO "Music", opened showing Music/Movies/My uploads/Create-a-new-list (/tmp/wialbumplay/*.png, esp. 3-playing, 6-autoadvance, 9-upload, 12-picked, 13-dropdown). GOTCHA: DropdownButtonFormField caches initialValue — key it by the selection or programmatic default flips never show. USER: ships with next release.
- [2026-09-01] v0.1.0-alpha.77 "BATCH UPLOAD" RELEASED (Latest: AppImage + Windows zip + APK; tag on 99f024e's follow-up bump commit; published ~20:40Z): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.77 — ships the in-app batch uploader (entry below). Local watchit-build unit built APK+AppImage in 3min (warm caches, Dart-only; the fixed scripts named both straight into dist/ — first release with NO rename step); Windows zip from tag CI run 33553812728 (sha256 0826856b…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit.exe, watchit_core.dll 48.7MB); main CI green. Verified APK versionCode 77/cert 79:11:66:99; clean-HOME Xvfb :78 smoke of the exact dist AppImage: Terms gate → I agree → seeded NOTLD home → Settings → Upload shows "Batch upload with auto-matching" live in the shipped binary (/tmp/wi77-smoke-1-terms.png, -2-home.png, -3-upload.png). Published with /tmp/wi77-release-notes.md (no --prerelease → Latest confirmed via releases/latest, all 3 assets).
- [2026-09-01] BATCH UPLOAD IN-APP (99f024e feature commit, releasing as alpha.77; implements the "build the CLI into the main app" decision series — desktop-only, wallet reused, no Android/iOS): the upload CLI's prepare/upload pipeline is now a desktop screen — Upload screen gains "Batch upload with auto-matching" → NEW screens/batch_upload_screen.dart + services/batch_upload.dart (BatchUploadSession singleton, PublishSession's survive-navigation pattern with a Completer-based pending-confirm). REUSED FROM cli/ UNCHANGED via NEW package export cli/lib/watchit_upload.dart (app depends on ../cli by path): Matcher (MB/TMDB id recovery + canonical name regeneration), Manifest lifecycle (saved per state change into <support>/batch_uploads/<slug>/), content-hash Ledger at ~/.watchit-upload/ledger.jsonl (SHARED with the CLI — either tool's uploads dedup the other's), bundle_out writeBundle, Sidecar (files beside sources still honored; the in-app manual form builds a synthetic manual-entry Sidecar = case-B userEdited bundle rows). REPLACED: ant CLI + SECRET_KEY → embedded core's authed named /upload (paid from the keychain wallet; NO staging symlink — /upload takes a name param) + GET /datamap/{addr} for bundle bytes (PublishApi gains a public `base` getter); terminal confirm loop → on-screen confirm card (accept / manual MB/TMDB search pick-list / paste-id / music↔video toggle / manual-entry form / reject→needs-attention / skip — every action re-enters matchFile with a synthetic Sidecar, CLI semantics exactly, incl. the id-backed-high auto-accept rule). Cost = one live /upload/estimate scaled by Ant.chunksFor + wallet balances; upload loop = per-entry poll, ledger append at the uploaded flip, one retry pass, .watch-list bundle emit, add-to-library + save-bundle-copy on the done page. cli/ changes (17 tests still green): probeFile gains ffprobeBin param (app passes bundled-binary path), collectMediaFiles/sha256OfFile/cueSiblingProblem publicized. TMDB key = app's Settings → Metadata key, falling back to CLI config; MB/CAA keyless. Tests 776 app (+5: full pipeline vs fake server incl. bundle parseBundle round-trip + userEdited row, ledger dedup with matcher-must-not-run, confirm skip/reject, manual-entry case B, Upload-screen link) + analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so, seeded terms+TMDB key prefs): real MB search matched an untagged "The Rolling Stones - Let It Bleed - 01 Gimme Shelter.mp3" → confirm card with REAL CAA cover art + canonical {mbid-499485cb…} name, NOTLD-named mp4 AUTO-ACCEPTED via TMDB find-by-imdb with real [240p] probe tag, review page showed live mainnet estimate ≈0.265585 ANT + wallet gating (/tmp/wibatch-14…17-*.png). NOT live-driven: the paid upload leg (no funded wallet — same standing gap as every release; the /upload machinery is the user-verified alpha.55+ path). GOTCHA: GTK folder chooser (getDirectoryPath) fights xdotool location-bar autocomplete hard — the file picker (openFiles + ctrl+L + short path + End + Return) is the reliable drive.
- [2026-09-01] RELEASE SCRIPTS NAME ARTIFACTS FOR UPLOAD (scripts-only, closes the alpha.76 release warning — the local AppImage built as Watch-It-0.1.0-x86_64.AppImage and needed a manual rename in dist/, and the APK sat unversioned at app/build/app/outputs/flutter-apk/): build_appimage.sh now derives the FULL release version from pubspec (`0.1.0+76` → `0.1.0-alpha.76`, no-`+` versions stay plain) so the AppImage lands as dist/Watch-It-0.1.0-alpha.N-x86_64.AppImage; release_build.sh additionally copies app-release.apk to dist/Watch-It-0.1.0-alpha.N.apk after the AppImage build and `ls`es both artifacts before ALL BUILDS DONE. Both names match the GitHub release-asset convention exactly (verified against the alpha.76 assets) — release publishing is now `gh release upload` straight from dist/, no rename step. bash -n both + derivation exercised against the real pubspec. NOT changed: the Windows zip still comes from CI and is renamed at publish time.
- [2026-09-01] UPLOAD CLI v1 IMPLEMENTED (UNRELEASED — post-alpha.75; the ten-entry planning series is now code; docs/UPLOAD-CLI.md is the user doc): NEW pure-Dart packages `packages/watchit_naming` + `cli/` (watchit_upload). NAMING EXTRACTION: ParsedName/parseMediaName moved verbatim out of app/lib/services/metadata.dart into watchit_naming (app re-exports them — zero call-site churn, 759 app tests green; audio extensions added to the extension-strip regex as the music/video discriminator) + NEW sanitizeNamePart (NFC via unorm_dart, locked substitution table, ≤255-byte fitFileName preserving the id-tag suffix) and musicFileName/videoFileName generators (D-NN multi-disc prefix only when discs>1, no-id-tag case-B names; 22 golden-corpus tests pin generate→parse round-trips — CLI and app share ONE parser by construction). CLI: `prepare` (scan → sha256 → content-hash LEDGER dedup at ~/.watchit-upload/ledger.jsonl → ffprobe classify/tags → match: Picard MBID tags → AcoustID (fpcalc+key, optional, graceful skip) → MB Lucene search / TMDB find-by-imdb + scored search → name regenerated from DB canonical data → CAA/TMDB art fetch → beets-style confirm (id-backed auto-accept, search matches ALWAYS confirm; e=manual search pick-list, i=paste id, m=music/video toggle) → .watchit.yaml sidecar skeletons for failures → manifest.yaml + one-quote-scaled cost estimate (4MiB chunks, 3-chunk floor) + wallet check), `upload` (zero-prompt: staging symlink so ant names the datamap with the FINAL W@tch name, `ant file upload --json --overwrite` private, manifest saved per state change = crash-resumable, ledger appended at the uploaded flip, retry pass at end, optional get_range retrievability proof via devserver POST /datamap + ranged GET /xor, .watch-list bundle emit, Telegram ping via ~/telegram-bot/.env), `ledger list/export` (offline bundle rebuild = disaster recovery). Case-B custom items ride as userEdited metadata rows + posters (the shipped bundle format; VERIFIED: app parseBundle accepts a CLI bundle incl. the row+poster in a live cross-test). ant facts learned: `ant file cost --json` → {file_size,chunk_count,storage_cost_atto,…}; wallet = SECRET_KEY env var; upload writes `<file>.datamap` in CWD. GOTCHA fixed: StreamSubscription.asFuture() attached AFTER process streams close never completes and the VM exits 0 silently mid-batch — use .forEach futures created before awaiting exitCode (bit the upload loop live). Tests: 22 naming + 17 cli + 759 app, all analyze clean. LIVE smoke (real MB/TMDB/CAA, fake-ant shim for the paid leg): tagged flac → canonical Abbey Road name {mbid-…} + CAA art; NOTLD imdb-tag → TMDB find + poster + [360p] tag; untagged mp3 → MB search + confirm flow; no-match → skeleton; sidecar manual entry → case-B; ledger dedup on re-prepare (HAVE × 4, renamed files still hit); simulated failure → retry pass → bundle with all 4. MB client retries 3× w/ backoff (MB 503s freely — first smoke run failed on it). NOT exercised: real paid upload (funded SECRET_KEY) + the plan's dogfood task (regenerate the shipped PD/NOTLD bundles via the CLI). App-side music support (type:music UI) still future.
- [2026-08-30] v0.1.0-alpha.75 "QUIET NETWORK" RELEASED (Latest: AppImage + Windows zip + APK; tag on 9297049, the version-bump commit; published ~20:05Z): ships 79b7f94 (x0x 0.40.4 bump + presence beacons stopped — entry below). All 3 assets verified before publish: APK versionCode 75 / cert 79:11:66:99, libwatchit_core.so + Windows watchit_core.dll both contain x0x-0.40.4 and the "presence beacons stopped" string; AppImage smoke-tested on a clean HOME (terms → library). releases/latest points at it.
- [2026-08-30] x0x 0.40.4 BUMP + PRESENCE BEACONS STOPPED (79b7f94 pushed, UNRELEASED — post-alpha.74; implements the idle-bandwidth cut agreed after the saorsa-labs review): x0x vendored dep bumped 0.39.10 → **0.40.4** (pulls the whole upstream issue-#380 campaign: leaf-mode-by-default clients, V3 slim announces ~7.5KB, replay-storm per-topic validators via saorsa-gossip 0.5.75 — order-of-magnitude idle-traffic cut) + NEW native src/x0x_tune.rs `quiet_agent()`: both agents (mywatch.rs + channels.rs) stop the 30s presence-beacon broadcaster right after `join_network()` via the public presence API (`presence_system().manager().stop_beacons()` — AgentBuilder has no enable_beacons knob, but join starts the broadcaster synchronously so the stop is race-free; no vendored patch needed). Nothing in Watch-It rode beacons: the My W@tch device-row online dot now derives from the 60s CRDT store heartbeat (`updated_at_ms` fresh within 3 min ⇒ online — beacons under-reported anyway; `presence()` still consulted as a bonus signal and for art-transfer candidate ORDERING, which never gated). Both status JSONs gain `"gossip_mode"` ("leaf" on every healthy client; "full" would mean this agent relays overlay traffic — field-visible drift detector), and quiet_agent logs the participation mode. `pub fn start()` now calls init_tracing() (was FFI-entry-only), so the devserver emits logs. VERIFIED LIVE (release devserver, clean data dir, real mainnet join): log shows x0x "Presence beacons started (interval=30s)" then 7ms later "mywatch: presence beacons stopped" + "gossip participation leaf"; /mywatch status reaches state ready with gossip_mode "leaf" (channels agent uses the identical call — only starts with a subscription, not live-driven). 64 cargo + 759 Dart tests + analyze clean; Android NDK cross-build of the 0.40.4 tree clean (build-android.sh). Main CI watched green. STILL OPEN from the plan: before/after idle-bandwidth measurement (nethogs baseline was recommendation 1). GOTCHA: /usr/bin/cargo is 1.75 and fails on the v4 Cargo.lock — PATH must lead with ~/.cargo/bin (1.95). USER: ships with next release
- [2026-08-30] v0.1.0-alpha.74 "MOBILE DATA" RELEASED (Latest: AppImage + Windows zip + APK; tag on dcea88c, the version-bump commit; published 17:49Z): ships 4e157e2 (mobile-data consolidation + x0x cellular gates — entry below). All 3 assets confirmed on the release; releases/latest points at it.
- [2026-08-30] MOBILE DATA CONSOLIDATED + x0x CELLULAR GATES (4e157e2, RELEASED in alpha.74; implements the planned design, defaults keep mobile data ON for the x0x agents so existing users see no change): Settings → NETWORK's two policy tiles (Downloads, Streaming on mobile data) replaced by ONE **Mobile data** tile → new screens/mobile_data_screen.dart listing all four consumers: Streaming (Ask first/Allowed/Wi-Fi only), Downloads (Wi-Fi only/Wi-Fi + mobile data — still pokes DownloadManager.onNetworkPolicyChanged), and NEW per-feature Channels / My W@tch switches (prefs channels_cellular_v1 / mywatch_cellular_v1, default TRUE). NEW services/x0x_cellular.dart X0xCellularGate (started from main() after ChannelService): on cellular with a feature set Wi-Fi-only it pauses that x0x agent via the existing POST /…/enabled switch and resumes it the moment Wi-Fi returns; remembers exactly what IT paused (pref x0x_cellular_paused_v1, survives restart) so a user's manual off is never overridden — manual flips call noteManualChange and always win, agent-state reads skip unsupported/user-off agents, failed resumes keep the flag for retry, initial apply delayed 2s (transport unknown at launch would flap). Honest wording everywhere the pause shows: x0x screen state lines, drawer rows ("paused on mobile data" vs "switched off"), My W@tch switched-off card, Channels connection bar. 759 Dart tests (+15: gate matrix incl. pause/resume/manual-override/user-off-respected/unsupported/failed-resume-retry, mobile-data screen quartet, drawer + x0x paused wording, settings nav + NETWORK repin) + analyze clean. LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so): NETWORK shows Buffer → Autonomi → x0x → Mobile data, screen renders all four with defaults, streaming dialog, Channels toggle off → subtitle "Wi-Fi only — pauses on mobile data, resumes on Wi-Fi" + pref false on disk (/tmp/wimobile-4-network.png, -5-mobiledata.png, -6-streamdialog.png, -7-channelsoff.png). Cellular legs desktop-untestable (no cellular transport) — pinned by the gate unit tests. RELEASED in alpha.74
- [2026-08-30] v0.1.0-alpha.73 "HONEST SYNC" RELEASED (Latest: AppImage + Windows zip + APK; tag on e55951f, the version-bump commit): ships d8e829e (map-import backoff reset + pending maps) + 2ba56aa (channels Sync now button + honest manual checks). Finished across two sessions: the first bumped/tagged/built APK (versionCode 73, cert 79:11:66:99) + AppImage, smoke-tested on clean-HOME Xvfb (/tmp/wi73-smoke-*.png) and wrote /tmp/wi73-release-notes.md, but its Windows-CI watcher died with the session; the follow-up Status task found tag CI run 33317333861 green, downloaded the zip (sha256 fa5a159d…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit.exe, watchit_core.dll 48.4MB; app.so verified to contain the new code — NOTE the pending-maps string needs `strings -el` (UTF-16) because the em-dash makes Dart store it as a TwoByteString), copied to dist/ and published (no --prerelease → Latest confirmed via releases/latest, all 3 assets).
- [2026-08-30] CHANNELS SYNC NOW BUTTON MATCHES MY W@TCH + HONEST MANUAL CHECKS (2ba56aa pushed, RELEASED in alpha.73; two user asks after the My W@tch backoff-reset task): (1) DESIGN CONTINUITY — the Channels app-bar refresh icon is gone; the Subscribed segment gets the same OutlinedButton.icon(Icons.sync, 'Sync now') the My W@tch screen uses, below the channel cards above Add channel, hidden while nothing is subscribed. (2) The analysis answer: channels has NO backoff to reset (every 5-min cycle unconditionally retries failed manifest imports — importedSeq only advances on success), BUT the old `syncNow()` silently returned while a background cycle was mid-flight, so a manual press during one no-opped yet still snacked "Checked all subscribed channels". NEW `ChannelService.checkNow()` waits out any in-flight cycle then runs a FRESH full pass (a press right after connectivity returns always re-attempts pending imports after the press); plain `syncNow()` now returns/joins the in-flight cycle's future (`_run` coalescing replaces the `_syncing` bool) instead of no-opping. 743 Dart tests (+2: gated-API service test proving join + fresh-pass-after-wait, widget test pinning body button/no app-bar refresh/press-checks-and-reports) + analyze clean. LIVE Xvfb :77 (clean HOME, debug bundle + release .so): subscribed to a throwaway Ed25519 code via the Add channel dialog → card + body Sync now render in My W@tch style, press → "Checked all subscribed channels" snack (/tmp/wichsync-4-channels.png empty-state no-button, -6-subscribed.png button, -7-syncnow.png snack). USER: ships with next release
- [2026-08-30] MY W@TCH MAP-IMPORT BACKOFF RESET + PENDING MAPS SURFACED (d8e829e pushed, RELEASED in alpha.73; closes the Android PLAY-404 saga's fix leg — synced entry whose shrunk-map import never completed left the phone's mapstore without the datamap → /xor 404 on play, invisible because the card said "Everything is in sync."): (1) the flat 10-min map-import retry backoff (`_mapRetryAt`) now CLEARS when connectivity returns — each cycle probes ClientHealth (injectable `health` ctor param, default EmbeddedClient.health) and a not-ok→ready-with-peers transition drops the backoff so offline-failed imports retry the very next cycle; (2) Sync now clears the backoff FIRST (manual sync = explicit "try again now" — previously it ran the identical cycle, skipped backed-off maps and reported in-sync); (3) `_importMissingMaps` now checks `_mapStored` BEFORE the backoff gate and counts still-missing maps into new `MyWatchSyncStatus.pendingMaps`; the sync activity card shows an amber "N data map(s) pending — those titles can't play on this device yet. Retrying automatically; Sync now retries immediately." line + amber warning icon (green check only when problems empty AND pending 0); a map arriving another way (bundle re-import) stops counting at once. New `cycleForTesting()` (plain cycle without the manual reset) + `clientBase` ctor param (Dart 3 private named param `this._clientBase`) for the mapstore/import base; fake_embedded_http gains GET /resolve/<addr> (resolvedAddrs set, POST /datamap success auto-adds) + `datamapUnavailable` 503 flag. 741 Dart tests (+4: connectivity-return reset, Sync-now reset, stored-map-never-pending, card pending line) + analyze clean. NO live GUI shot — the state needs a linked device with a failing map import (same precedent as the channel update-fetch calm message: pinned by widget test). USER: ships with next release; on the phone, alpha.73+ will self-heal the 404 title once connectivity returns or Sync now is pressed
- [2026-08-30] v0.1.0-alpha.72 "RESILIENT UPLOADS" RELEASED (Latest: AppImage + Windows zip + APK; tag on 2101287, the version-bump commit): ships 26057fe (drawer status on top + Settings CONTENT reorder) + 97ceaba (upload session + retry + deferred channel announce) + 792693b (build hygiene). Local watchit-build unit built APK+AppImage fresh; Windows zip from tag CI run 33304939788 (sha256 8cf16bba…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit.exe, watchit_core.dll 48.4MB, app.so contains publish_session.dart); APK versionCode 72/cert 79:11:66:99; clean-HOME Xvfb smoke of the dist AppImage (/tmp/wi72-smoke-1…3-*.png); published with /tmp/wi72-release-notes.md (no --prerelease → Latest confirmed via releases/latest, all 3 assets). The release was finished across two sessions: the first built+verified local artifacts and armed a CI watcher that died with the session; the follow-up Status task downloaded the zip and published.
- [2026-08-30] UPLOAD SESSION + RETRY + DEFERRED CHANNEL ANNOUNCE + x0x SWITCH ORDER (97ceaba pushed, RELEASED in alpha.72; four user asks): (1) Built-in x0x client screen lists Channels above My W@tch (CONTENT section's order); Settings tile subtitle now "Channels on/off · My W@tch on/off". (2) NEW app/lib/services/publish_session.dart — the running Upload batch is a singleton OUTSIDE the Upload screen, so leaving mid-upload and returning shows the batch where it stands (running / pending error / done) instead of a fresh setup form dead-ended by the core's single upload slot; screen offers setup only when session idle. (3) Upload errors pause the batch on Try again / Skip this file / Stop (error state also survives navigation). (4) Channel publish with the Channels x0x switch OFF no longer fails "the channel link is not running": head uploads and is saved as a deferred announce (channels.rs pending_head.json, announced:false) gossiped automatically on re-enable; live publish supersedes an older deferred head, unlink clears it; channels screen shows pendingAnnounce. 64 cargo + 737 Dart tests + analyze clean; live Xvfb :77 verified switch order, mid-batch resume, payment-error Retry page (0-ANT wallet), Stop → done page (/tmp/wi4fix-*.png).
- [2026-08-30] DRAWER STATUS ON TOP + SETTINGS "CONTENT" SECTION + REORDER (26057fe pushed, RELEASED in alpha.72; five user asks in one task): (1) the drawer's three connection status rows (Autonomi peers / My W@tch / Channels) moved to the TOP of the drawer, above the Library list section (order now: status rows · divider · Library lists · divider · Settings). (2) Settings LIBRARY section RENAMED **CONTENT** (answering "a better name to reflect its contents"): Channels · My W@tch · My Media · Upload (desktop) · **Downloads** — the Downloads queue/storage tile moved in at the section's end (below My Media on mobile, below Upload on desktop) and the standalone DOWNLOADS section is gone. (3) **Buffer size** (+ its explainer) leads the NETWORK section above Built-in Autonomi client; the single-tile STREAMING section is gone. (4) APPEARANCE moved below METADATA (section order now CONTENT · NETWORK · METADATA · APPEARANCE · WALLET · ABOUT). Files: settings_screen.dart, library_drawer.dart, drawer_status.dart (doc comment), docs/UI-DESIGN.md. Tests: drawer placement pin inverted (rows above Library header + Settings), library_test settings-flow re-pinned (CONTENT order incl. Downloads via its subtitle — title collides with the network Downloads policy tile — buffer-above-Autonomi, APPEARANCE-below-METADATA, STREAMING/LIBRARY absent), theme_mode tests gained scrollUntilVisible (Colour scheme now further down). 732 Dart tests + analyze clean; LIVE Xvfb :77 clean-HOME verify (debug bundle + release .so): drawer shows status rows above Library, Settings shows CONTENT with all five tiles in order, NETWORK leads with Buffer size, METADATA → APPEARANCE → WALLET (/tmp/wirelayout-3-drawer.png, -4-settings-top.png, -5-settings-mid.png). RELEASED in alpha.72
- [2026-08-29] v0.1.0-alpha.71 "STATUS IN THE DRAWER" RELEASED (Latest: AppImage + Windows zip + APK; tag on 60c09c1, the version-bump commit): ships e1738f6 (connection status moved into the drawer) + f642f71 (Settings reorder + Built-in x0x client switches). Local watchit-build unit built APK+AppImage (ALL BUILDS DONE 21:09), Windows zip from tag CI run 33274963607 (sha256 4f7a0a79…, 53 files incl. ffmpeg.exe+ffprobe.exe+watchit.exe, watchit_core.dll 48.3MB); verified APK versionCode 71/cert 79:11:66:99; clean-HOME Xvfb :96 smoke of the exact dist AppImage (wi71-* units): Terms gate → I agree → bar-free home with seeded NOTLD "2 versions", drawer shows the three new status rows (green Connected · 5 peers / grey My W@tch: not linked / grey Channels: not connected) (/tmp/wi71-smoke-1-launch.png … -3-drawer.png); published with /tmp/wi71-release-notes.md (no --prerelease → Latest, confirmed via releases/latest, all 3 assets)
- [2026-08-29] SETTINGS REORDER + BUILT-IN X0X CLIENT SWITCHES (f642f71 pushed, RELEASED in alpha.71; three user asks, follows the x0x data-usage analysis): (1) Settings LIBRARY order now Channels · **My W@tch** (moved up from Network — sharing surfaces together, public above private) · My Media · Upload; (2) NETWORK section leads with **Built-in Autonomi client** (moved up from Streaming, above the Downloads policy tile; Streaming keeps Buffer size); (3) NEW **Built-in x0x client** tile below it (Icons.hub, live subtitle "My W@tch on · Channels off") opening screens/x0x_client_screen.dart: two independent SwitchListTiles that stop the My W@tch or Channels x0x agent — and ALL its background gossip/relay traffic — without touching the link, channel key or subscriptions. RUST: persisted marker files `<data>/mywatch.off`/`channels.off` (BESIDE the agent dirs so unlink's dir wipe can't flip the switch); `set_enabled(false)` shuts a Ready agent down (cancel + store cancel_sync + agent.shutdown, mem::replace for channels' multi-store), `true` re-runs autostart; autostart AND both bring-up retry loops gated on the flag (incl. the switched-off-while-connecting race); create_link/join_link/channel create/restore/subscribe flip the switch back ON (explicit intent); status JSON gains `enabled` and reports state "off" (not "starting") when disabled; new PROTECTED routes POST /mywatch/enabled + /channel/enabled (token-guard test extended); stubs return unsupported. DART: MyWatchApi/ChannelsApi setEnabled + enabled on both status models (absent key → true, old cores fine) + MyWatchSyncStatus.enabled threaded through all 5 constructions; drawer rows say grey "My W@tch/Channels: switched off" (enabled-check beats agentState so no eternal "connecting…"), Channels bar "Switched off — turn Channels on in Settings → Built-in x0x client", My W@tch linked view swaps the connecting spinner for a switched-off card. 62 cargo (+2: marker persists across restart + gates status, both agents) + 732 Dart (+5: x0x screen quartet incl. POST body + unsupported-disabled, settings nav; drawer switched-off wording; LIBRARY/NETWORK Y-coord pins extended) + analyze clean. LIVE Xvfb :77 (clean HOME, debug bundle + fresh release .so, WATCHIT_AUTH_TOKEN curl on the app's own port): settings order verified, both switches toggled off (markers on disk, /channels+/mywatch report enabled:false state off), Channels bar shows the switched-off wording, curl subscribe AUTO-RE-ENABLED (channels.off deleted) → agent ready in ~70s → POST enabled:false on the LIVE agent → x0x "Network node shut down" in the log, state off, sub kept → app RESTART: autostart gated (0 "channels agent up", sub still listed) → enabled:true → ready again (/tmp/wix0x-1…13-*.png). USER: ships with the next release
- [2026-08-29] CONNECTION STATUS MOVED INTO THE DRAWER (e1738f6, RELEASED in alpha.71; user ask, second half of the "align mobile" task — the first half needed NO code: the Settings LIBRARY reshuffle (Channels on top, My Media rename) is shared Dart already in alpha.70, the user's mobile device just runs an older APK since Android has no update check): the home screen's NetworkStatusBar is GONE (class deleted from main.dart); the library drawer now carries, below the Settings tile after a divider, three dot-style status rows via new widgets/drawer_status.dart (WiDrawerStatus, same 8px-dot + 11.5px wording as the old bar): (1) Autonomi peers — "Connected · N peers"/"Connecting…"/"Connection error", same health poll cadence (3s connecting/15s ready, stops on 'unavailable'), not tappable; (2) My W@tch — fed by MyWatchSync.status (no polling), same wording as the old bar segment PLUS a grey "My W@tch: not linked" state when supported-but-unlinked (the old bar hid it), tap opens MyWatchScreen; (3) NEW Channels row — polls ChannelsApi().status() (5s while starting/30s otherwise, stops on error or unsupported): green "Channels: connected"/amber spinner "Channels: connecting…"/grey "Channels: not connected", tap opens ChannelsScreen. WiLibraryDrawer gains healthProvider/channelsStatusProvider test overrides forwarded to the rows. test/status_bar_test.dart → test/drawer_status_test.dart (10 tests, +5: row states incl. not-linked + channels trio + unsupported-hidden, tap-navigation both rows, drawer placement pinned by Y-coords below Settings, home-has-no-bar, Settings→Network group kept). 727 Dart tests + analyze clean. LIVE Xvfb :77 verify (clean HOME, debug bundle + release .so): home renders with NO status bar, drawer shows Movies → Settings → green "Connected · 5 peers" / grey "My W@tch: not linked" / grey "Channels: not connected" in order, tapping the Channels row opens the Channels screen with its own connection bar agreeing "Not connected"; Settings LIBRARY re-confirmed Channels/My Media/Upload (/tmp/widrawer-2-home.png, -3-drawer.png, -4-channels.png, -5-settings.png). USER: ships with the next release; mobile alignment = install alpha.70+ APK (LIBRARY reshuffle is already in it).
- [2026-08-29] v0.1.0-alpha.70 "CHANNEL PROFILES" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.70 (Latest, AppImage + Windows zip + APK; tag on bump commit 9044554). Ships af01b00 (channel profile) + connection bar/softer fetch message/no category tags/LIBRARY reshuffle. Windows zip from tag CI run 33260984689 (sha256 875470e2…, 53 files incl. ffmpeg/ffprobe/watchit_core.dll); APK versionCode 70, cert 79:11:66:99; clean-HOME Xvfb :96 smoke: Terms gate → I agree → Connected · 5 peers + seeded NOTLD (/tmp/wi70-smoke-*.png). Desktop users ≥alpha.56 get the update notification.
- [2026-08-29] CHANNEL PROFILE SHIPPED (released in alpha.70; implements docs/PLAN-channel-profile.md rev 3 in full, plan → IMPLEMENTED): a channel has a FACE — circular avatar (still image, forced 1:1 crop ≤512px output, 2 MB hard cap `kMaxChannelAvatarBytes`) + optional author name-or-handle ("by <author>", ≤80, unset = key omitted) + existing name/description. RUST: OwnConfig gains `author` (empty = unset, description's convention); create/set_meta/`/channel/create`/`/channel/meta` pass it, status reports it; restore recovers it from the manifest via set_meta (60 cargo tests, +2). DART DATA: drift **schema v11** (media_lists.channel_author/channel_avatar, refreshed from every imported manifest + sub records carry author/avatar); channel.json gains optional `author`+`avatar`; avatar bytes ride as a content-hash-named `posters/channel_avatar_<sha8>.img` member (buildBundle's new extraPosterMembers, written LAST with the posters → the existing delta fetch skips an unchanged avatar with ZERO new delta code — pinned by a dedicated delta test); owner's crop lives in `<support>/channel/` under its member name with a posters-dir copy under the SAME name (own list renders pre-publish; post-publish import maps to the identical file); avatar name gate `isChannelAvatarMemberName` (strict pattern = path-traversal-proof); `channelCodeFromPubkeyHex` = Dart twin of the Rust base32 code (pinned vs a vector AND live vs the core). UI: create form = profile form (96px ChannelAvatarPicker, amber ring, camera badge; author field "Optional… Public and permanent"), FirstPublishGate gains the profile bullet, avatar staged after create; My Channel header = avatar + "by author" + **Edit channel details** (EditChannelScreen: avatar/name/author/description, staged locally, "goes public with the next publish" note); **ChannelInfoCard** on channel list pages (72px avatar, name, "by author · N entries", 2-line expandable description, copyable wchn1 code in amber mono as anti-impersonation UI, edit pencil on the own channel; app bar drops the entry count there); mini avatars w/ podcasts fallback: channels-screen cards 40px + "by author" line, drawer rows 20px, My Media rows 20px, home wall 18px beside the badge (widgets/channel_avatar.dart — resolves member name via posters dir, THE circular-artwork idiom). PLAN DEVIATION (documented): no pre-subscribe add-confirm preview — the manifest only arrives after joining the topic; the subscribed card shows the profile when the first import lands. GOTCHAS: myAvatarFile/setMyAvatar are SYNC IO + fail-soft on no support dir (async dart:io + real path_provider hang the fake-async test zone — channels_screen_test/channel_info_card_test override postersDirProvider+profileDirProvider). 722 Dart tests (+15: profile parse/build/import/own-list/restore/setMyAvatar/2MB-gate/delta-avatar-skip units, v10→v11 migration, create-form/gate/edit-screen/own-header/card-author widget tests, info-card trio, code vector) + 60 cargo + analyze clean. LIVE Xvfb :77 verify (clean HOME, debug bundle + fresh release .so, channel created via authed curl WITH author): Edit channel details → real GTK pick → **forced-square crop dialog** (no Use-whole-frame) → saved → header/home-wall/drawer/info-card ALL render the green-face avatar + "by Ella", info card code == the core's code EXACTLY; staged NOTLD via describe+attestation → Publish → the REAL built manifest inspected: channel.json carries author+avatar, `posters/channel_avatar_31113d3b.img` is the LAST member, bytes IDENTICAL to the profile crop; live mainnet estimate ≈0.143006 ANT/3 chunks for the 63 KB manifest, 0-balance wallet flagged, cancelled before paying (/tmp/wiprof-1…20-*.png). NOT live-driven: the paid publish + subscriber-side refresh + true delta on a second head (funded-wallet gap as ever — the user's next real publish exercises all three; unit/delta tests pin the logic). Docs: UI-DESIGN §9 profile block, ARCHITECTURE manifest bullet, BUNDLE-FORMAT posters row, plan → IMPLEMENTED w/ deviations.
- [2026-08-29] CHANNEL PROFILE PLANNED, REV 3 — docs/PLAN-channel-profile.md (STILL PLAN ONLY; rev 3 resolves the last 2 open questions per user "Author name or handle is optional and yes let's have a mini avatar on the home wall": AUTHOR IS OPTIONAL — create-form field + helper text reworded, empty = key omitted from channel.json / OwnConfig empty string = unset like description, every "by <author>" line renders only when set (same path as old manifests lacking the field), FirstPublishGate wording now "and the author name and avatar if you set them"; HOME-WALL 18px MINI AVATAR CONFIRMED IN — was "cut first if busy". NO open questions left — plan ready to implement on the user's word): avatar (STILL IMAGE ONLY — animated GIF support dropped in rev 2, user call "would look cheap and tacky"; every avatar goes through the forced 1:1 crop, a picked gif flattens to its first frame; circular everywhere — first circular artwork in the app; circles = channel identity, rectangles = media) + author name-or-handle (one free-text field, "by <author>") + existing name/description become the channel profile. Design: channel.json stays version:1, gains optional `author` + `avatar` (member name); avatar bytes ride as a content-hash-named `posters/channel_avatar_<sha8>.img` member so the existing delta-fetch/import machinery handles caching+invalidation for free; Rust OwnConfig gains author (avatar stays Dart-side); drift v11 adds media_lists.channelAuthor/channelAvatar. NEW §2a answers the user's update question: the profile is mutable by construction — avatar/author/name/description ride EVERY publish (manifest rebuild + new signed head; subscribers' 5-min head-follow refreshes all four, changed avatar = new hash = fetched, unchanged = delta-skipped, republishing held chunks free via dedup; profile-only publishes legitimate; old manifests permanent — old avatar stops being referenced, never erased). UI: create form becomes profile form (96px circular picker, author optional, 2MB cap on crop output), FirstPublishGate warns avatar/author are public+permanent, full-width CHANNEL INFO CARD above the poster grid on channel list pages (72px avatar, name, by author · N entries — just "N entries" when authorless, description, copyable wchn1 code as anti-impersonation UI — no handle registry/uniqueness, the code stays the only real identity), mini avatars w/ podcasts-icon fallback on channels-screen cards/add-confirm/drawer/home wall. 4-stage build order (stage 4 live-verifies the §2a edit→republish→subscriber-refresh leg). USER: say implement to start the build
- [2026-08-29] CHANNELS CONNECTION BAR + SETTINGS LIBRARY RESHUFFLE + CHANNELS DROP CATEGORY TAGS (released in alpha.70; three user asks in one task): (1) CONNECTION STATUS OBVIOUS — the Channels screen gains an always-visible connection bar under the segment switch (both segments), home-bar styling: green dot "Connected to the channel network" (state ready) / amber spinner "Connecting to the channel network…" (starting) / grey "Not connected — connects when you create or add a channel" (off); the old easy-to-miss starting-only footnote in the Subscribed list deleted. (2) SETTINGS → LIBRARY RESHUFFLE — order now Channels (amber, top) · My Media (renamed from "Media"; MediaListsScreen title too) · Upload (moved OUT of the home drawer, desktop-only, "Private · only your devices", opens PublishScreen); the drawer is now list navigation + Settings only; every "Settings → Media" hint string → "Settings → My Media" (main.dart empty states, drawer hint, list_edit empty-list hint). (3) NO CATEGORY TAGS ON CHANNELS — DescribeItemScreen's CATEGORY chip section deleted (save omits category → a TMDB match's genres stay in the LOCAL row only; Check TMDB no longer feeds chips); BundleExportOptions gains omitCategories (buildBundle writes category:null) and buildMyManifest sets it, so manifests publish no categories; ListHomeScreen never builds genre chips for isChannel lists (also hides chips a subscriber's own TMDB matches would add, covers already-published manifests). 702 Dart tests (+4 net: 3 old describe-category tests replaced by no-chips + category-preserved-locally, new connection-bar trio off/ready/starting, bundle omitCategories, channel-list-no-chips, settings LIBRARY order pinned by Y-coords, drawer Upload-absence) + analyze clean. LIVE Xvfb :77 verify (clean HOME, debug bundle + release .so): Terms → home Connected · 5 peers → drawer shows ONLY Movies+Settings → Settings LIBRARY = Channels/My Media/Upload in order → Channels bar grey "Not connected" → subscribed to a random valid wchn1 code → bar flipped LIVE to amber spinner "Connecting…" then green "Connected to the channel network" (~60s x0x join) → My Media page title (/tmp/wi3chg2-1…9-*.png). NOT live-driven: describe page w/o chips + manifest null category (channel ceremony needed; pinned by tests). Docs: UI-DESIGN Settings/Upload/Channels sections + README step 1. USER: on main, ships with the next release
- [2026-08-29] CHANNEL-UPDATE FETCH PROBLEM READS CALM (released in alpha.70; user "Yes do that to soften the message", follow-up to the DataMap-chunk analysis below after the error self-healed as predicted — transient connectivity): the channel card's status line for a `ChannelService.lastProblem` entry no longer prints the raw red error ("Update failed: manifest lookup failed: not found: DataMap chunk not found at 6fd4265…") — it now reads "Update found — not fully reachable yet, retrying automatically" in channelAmber (pending) instead of rust (error), because the 5-min head-follow loop retries until the fetch succeeds and the common cause is propagation lag/bad connection. The raw error stays available for field reports via a Tooltip on the line (hover on desktop, long-press on touch) — that raw text is exactly what made the original diagnosis possible, so it must remain reachable. channels_screen.dart only; +1 widget test (soft wording shown, "Update failed"/"DataMap chunk" absent from the card, tooltip carries the raw error, line color == channelAmber). 698 Dart tests + analyze clean. No GUI screenshot — the state needs a real subscribed channel with a failing manifest fetch (same precedent as the 3f26a5c naming fix: surfaced state pinned by widget test instead).
- [2026-08-28] CHANNEL "Update failed: manifest lookup failed: not found: DataMap chunk not found at 6fd4265…" ANALYSED (user report, analysis only — no code changes): the error is the channels_screen card status fed by ChannelService.lastProblem — syncNow saw a NEW signature-verified head over gossip but `GET /channel/manifest/{addr}` → `data_map_fetch` couldn't retrieve the manifest's datamap chunk from the network. The gossip/signature layer WORKS; the manifest data isn't fetchable. Key fact: the publishing device stores the manifest root map locally at publish (upload.rs store_root_map before the "announcing" phase), so this error can only appear on a device that did NOT perform that publish — a subscriber, or the owner's restored/second device. Causes ranked: (1) fresh publish + propagation lag — chunk not yet retrievable from this client's position; the 5-min head-follow loop retries forever (importedSeq only advances on success), usually self-heals; (2) the manifest upload reported success but the datamap chunk didn't persist on the network (node churn / silent store failure — head is only announced after upload success, so a persistent error across devices+hours means this); FIX = owner publishes an update again (dedup makes re-storing held chunks free, a fresh manifest+head supersedes the broken one); (3) fetching device poorly connected (low peers / VPN — ExpressVPN blocks Autonomi). NOT the delta-import code: failure is in the map lookup before any ranged read, and the full-fetch fallback hits the same lookup. "0 items" = no manifest ever imported for that channel on this device (consistent with a first import failing).
- [2026-08-28] v0.1.0-alpha.69 "ANDROID STABILITY" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.69 (Latest, AppImage + Windows zip + APK; tag on fix commit 43600f5). Ships 43600f5 (SAF Downloads-provider SecurityException on file pick caught in MainActivity → dialog with pick-the-folder-directly workaround instead of a crash — root cause from the test2 stack) + 248e682 (Java crash stacks captured, KEPT permanently per user for future field debugging) + 30ba5ef (Impeller off + exit-reason page). NOTE: Phrax reported (on test2, pre-fix) that moving the file to other locations did NOT dodge the bug on his tablet, so the dialog's workaround advice may need revising if his retest on THIS build still fails via every pick path. Local build unit watchit-build ALL BUILDS DONE 18:17; main CI 33198474383 + tag Windows CI 33198492428 both green. Verified APK versionCode 69/cert 79:11:66:99 (same signer as .68); zip sha256 5d379c61… 53 files incl. ffmpeg/ffprobe/watchit_core.dll; clean-HOME Xvfb :96 smoke of the dist AppImage (wi69-xvfb/wi69-app units): Terms v2 → I agree → home Connected · 5 peers + seeded NOTLD "2 versions" (/tmp/wi69-smoke-1-launch.png, -2-home.png). Published with /tmp/wi69-release-notes.md, no --prerelease → Latest confirmed via releases/latest. NOT verified: the SAF fix on a real tablet — awaiting Phrax's retest on this build.
- [2026-08-28] v0.1.0-alpha.69-test2: JAVA CRASH STACKS CAPTURED IN-APP (APK-only --prerelease https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.69-test2; closes the GAP in the entry below — REASON_CRASH records carry no trace): NEW WatchItApplication (registered as android:name, so it runs before any activity/plugin code) installs Thread.setDefaultUncaughtExceptionHandler via JavaCrashRecorder — on any unhandled Java exception it writes app version/SDK/device + thread + full stack to filesDir/crash_logs/java_<ms>.txt (keep 5, 64KB cap, OOM-safe fallback to a one-line summary) then chains to the previous handler so the process still dies and the OS still records REASON_CRASH. MainActivity.collectExitReasons attaches the saved stack to the matching "crash (java)" record (nearest timestamp within 60s, each file used once) through the existing empty "trace" field — the "Why did the app close?" page + Copy button show it with ZERO Dart changes. Gotcha: longVersionCode is API 28 but minSdk 24 → SDK-gated (lintVitalRelease would fail the release build). 697 Dart tests + analyze clean (Dart untouched); Kotlin verified by the release build itself (unit wi69t2-apk, ~1min warm); APK verified versionCode 69/cert 79:11:66:99, WatchItApplication in the APK manifest, crash_logs string in dex. TEST PLAN: install over test1 (same versionCode 69 — Android allows same-version reinstall), re-import Public Domain.watch-list; after the crash reopen → Settings → About → Why did the app close? → the java crash record now includes the exact exception class + stack line → Copy → send. NOT testable locally (no Android device on Ella).
- [2026-08-28] TABLET CRASH IS A JAVA CRASH (user report from the alpha.69-test1 "Why did the app close?" page, analysis only — no code changes): the Lenovo Tab P11 Pro record reads "crash (java), code 4, status 0" at 2026-08-28 13:34 — ApplicationExitInfo.REASON_CRASH, an unhandled JAVA exception. This EXONERATES the two prior suspects: Impeller/Mali would be code 5 "crash (native)" (so the EnableImpeller=false change in test1 was aimed at the wrong layer), a background kill would be user-stopped/other, and a Rust-core death would also be native. New prime suspect: Java plugin code on the import path — file_selector_android's content-resolver copy of the 1.8MB bundle (OutOfMemoryError or activity-result exception on the tablet's Android 14). GAP: Android attaches no trace to REASON_CRASH (readTrace correctly only reads CRASH_NATIVE/ANR), so the exit page can't name the exception. PROPOSED NEXT (awaiting user): alpha.69-test2 with Thread.setDefaultUncaughtExceptionHandler in MainActivity persisting the Java stack to a file, surfaced on the same exit-info page — next repro names the exact class/line, no adb needed. (Unknown whether the 13:34 record is from alpha.68 or a test1 repro; the reason code stands either way.)
- [2026-08-28] v0.1.0-alpha.69-test1 DIAGNOSTIC PRERELEASE PUBLISHED (APK only, --prerelease so desktop update checks and releases/latest still point at alpha.68): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.69-test1 — tag on 30ba5ef "Android crash diagnostics: Impeller off + in-app exit-reason report" (697 tests + analyze clean, pushed to main): AndroidManifest meta-data EnableImpeller=false (Skia rendering, the Mali-G77 suspect from the tablet crash analysis below) + NEW Settings → About → "Why did the app close?" page (Android ActivityManager.getHistoricalProcessExitReasons via MethodChannel, list of prior process-death reasons with Copy button — names native crash vs ANR vs background kill with no adb needed). Local APK build via transient unit wi69-apk (log /tmp/wi69-apk-build.log); verified versionCode 69 / cert 79:11:66:99 (SHA-256), copied to dist/Watch-It-0.1.0-alpha.69-test1.apk, published with /tmp/wi69-test1-release-notes.md. TEST PLAN (tablet is remote, no debug access): install over alpha.68, re-import Public Domain.watch-list; no crash → Impeller/Mali confirmed, ship the fix properly next release; still crashes → the exit-reason page shows the real culprit
- [2026-08-28] ANDROID BUNDLE-IMPORT CRASH REPORT ANALYSED (user report: alpha.68 crashes on a Lenovo Tab P11 Pro 2nd Gen, Android 14, when loading "Public Domain.watch-list"; analysis only, no code changes): the shared Dart import path is EXONERATED — the user's exact 1.8MB bundle (~/projects/Public Domain.watch-list, the pre-pull Aug-10 export: 49 datamaps/48 metadata rows/53 posters) was driven through current-HEAD parseBundle + importBundleEntries against a live devserver via a throwaway flutter test (deleted): parse 22ms, all 49 maps imported 0-invalid in 535ms, lists Movies:11 + TV Shows:38. Every import failure path shows an error dialog (never crashes), and a Rust panic can't kill the process (no panic=abort; hook logs to logcat tag watchit_core). So a hard crash must be native/OS-layer. LEADING SUSPECTS: (1) Impeller (Vulkan) driver crash on the tablet's Mali-G77 (Kompanio 1300T) when the post-import home wall first rasterises ~40 poster cards at once — fits "worked on the other Android phone"; confirm by a test APK with AndroidManifest meta-data io.flutter.embedding.android.EnableImpeller=false; (2) vendor OS killing the backgrounded app while the system file picker is foreground (returns to a fresh home screen — looks like a crash, logcat shows am_kill not a tombstone). NEXT: need one `adb logcat` capture from the tablet while reproducing (or `adb logcat -b crash`) to pick between them — the backtrace names the culprit (Mali/libvulkan → Impeller; am_kill → background reap; libwatchit_core → Rust).
- [2026-08-28] v0.1.0-alpha.68 "TIDIER DRAWER" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.68 (Latest, AppImage + Windows zip + APK; tag on bump commit 118174e). Ships 56dd444 (drawer slimmed: Media + Channels tiles removed from the home drawer, Channels tile added to Settings → LIBRARY beside Media — amber podcasts icon, "Public · anyone with the code" subtitle; drawer empty-state hint points at Settings → Media; +1 drawer-absence widget test, Settings-flow test pins both LIBRARY tiles) plus the three earlier unreleased commits (5207460 library UX quartet, ebfe8e0 delta-aware channel import). 687 Dart tests + analyze clean; main CI 33171722794 + tag Windows CI 33171722739 both green. Verified APK versionCode 68/cert 79:11:66:99; zip sha256 2954ca5c… 53 files incl. ffmpeg/ffprobe, watchit_core.dll 48.2MB; clean-HOME Xvfb :96 smoke of the dist AppImage (wi68-xvfb/wi68-app units): Terms v2 gate → I agree → home Connected · 5 peers + seeded NOTLD → drawer shows ONLY Movies/Upload/Settings (no Media, no Channels) → Settings LIBRARY shows Media + amber Channels → Channels tile opens the Channels screen (/tmp/wi68-smoke-1-launch.png … -5-channels.png). Published with /tmp/wi68-release-notes.md, no --prerelease → Latest confirmed via releases/latest.
- [2026-08-28] MEDIA + CHANNELS MOVED TO SETTINGS → LIBRARY (56dd444, released in alpha.68; user ask "stop displaying media and channels menu buttons in home screen drawer, only show them in settings in the library section"): WiLibraryDrawer's Media and Channels tiles deleted (imports pruned) — the drawer is now list navigation + Upload (desktop) + Settings; SettingsScreen's LIBRARY section gains a Channels ListTile after Media (Icons.podcasts in WiTokens.channelAmber, subtitle "Public · anyone with the code", pushes ChannelsScreen) — the amber public/private colour wall follows the tile. Drawer empty-state hint updated to "(Settings → Media)". Tests: new drawer-absence test in list_browse_flow_test.dart, Settings-flow test in library_test.dart pins Media + Channels under LIBRARY (687 Dart tests, +1).
- [2026-08-28] LIBRARY UX QUARTET (UNRELEASED — post-alpha.67; four user asks): (1) VERSION NESTING — in the list editor, multiple uploads of one title (same parsed lookupKey, e.g. a 480p and a 1080p copy) fold under a single expandable "N versions" tile mirroring the show → season tree; the group's menu curates all versions at once. (2) COPY TO ANOTHER LIST — every item/season/show/version-group menu gains "Copy to another list…" beside Move (same target picker incl. Create-new-list, merge-on-name-match, duplicates dropped + reported; copy leaves the source untouched — for building custom playlists; `_moveEntries` generalised to `_transferEntries(copy:)`). (3) ONE MEDIA MENU — Settings' separate "Home screen" page deleted (home_layout_screen.dart gone); the Media page now owns the whole home library: every home row (user lists + built-in Continue Watching/Favourites/Downloads/Recently Added) in home order via reconcileHomeSections, drag-handle reorder (ReorderableListView with the new onReorderItem callback — newIndex pre-adjusted) and show/hide checkboxes; built-in rows shaded lighter (tileColor ink2, "Built-in row — fills itself" subtitle) to mark they are not actual lists. (4) COLOUR SCHEME — Settings → new APPEARANCE section: Dark (default, the original look) / Light / System default; `wiThemeMode` ValueNotifier (theme/tokens.dart) + AppSettings.themeMode (theme_mode_v1 pref, garbage → dark), MaterialApp theme/darkTheme pair from WiTokens.light/dark, loaded pre-first-frame in main(), flips live from the picker. TESTS: 686 Dart (+9: theme_mode_test.dart round-trip/default/garbage/MaterialApp wiring; version-nest collapse/expand + group-remove; copy-keeps-source + copy-to-existing-merge; Media-page shade check + reorder/visibility reworked from home_layout_test) + analyze clean. LIVE Xvfb :77 verify (clean HOME, seeded 2-version NOTLD lists, debug build): editor shows "Night of the Living Dead · 2 versions" collapsed → expands to both files; Copy → Create new list → "Favourite Films" created with source intact (drawer shows Movies 2 + Favourite Films 1); Media page lists all 6 rows with built-ins visibly lighter, live handle-drag moved Movies to the top and the home wall followed; Colour scheme dialog → Light applied instantly (settings + home light), back to Dark (/tmp/wi4chg-1…21-*.png)
- [2026-08-28] CHANNEL UPDATES IMPORT DELTA-AWARE (UNRELEASED — post-alpha.67; closes the design note from the channels data-consumption answer: full-replace import re-downloaded every poster on every head update): a channel update now fetches only what changed. RUST: `GET /channel/manifest/{addr}` stores the fetched root map (mirrors the POST /datamap import — child maps expand over the wrapper chunks + verify_root_map, derived address must equal the requested one; manifests are content-addressed so the stored map is valid forever) and serves through the new `serve_stored_map` helper extracted from `/xor` — full `Range`/`HEAD` support + chunk cache; repeat fetches of the same manifest skip the network map lookup entirely. DART: new `zip_ranges.dart` (EOCD + central-directory parser, member spans, span coalescing, ranged member extraction with CRC-32 verify; zip64/encryption/multi-disk → null) + `channel_manifest_delta.dart` (`fetchManifestMembersDelta`: 64KB tail read → parse directory → skip `posters/` members whose file already exists in the posters dir — their bytes would lose seedBundle's existing-file-wins gap-fill anyway — fetch coalesced ranges of the rest, stitch tail-straddling members, assemble `BundleZipMember`s); since buildBundle writes posters LAST, unchanged posters occupy contiguous trailing chunks the ranged reads never touch. `parseBundle`/`parseChannelManifest` refactored onto member lists (`parseBundleMembers`/`parseChannelManifestMembers`, `BundleZipMember` with the declared-size bomb guard — also fixes the old double zip-decode in parseChannelManifest); ChannelService `_fetchManifest` runs delta-first for both subscription imports and own-channel restore, falls back to the whole-manifest fetch on ANY hiccup (older core answers 200 → its full body is used as-is; first import with no posters held → plain fetch). `ChannelsApi.fetchManifestRange` parses Content-Range. TESTS: 59 cargo (+1: manifest route serves stored maps with ranges via HEAD — body streaming needs network) + 677 Dart (+14: crc vector, directory parse/extract equivalence vs ZipDecoder, corrupted-member refusal, coalescing, skip-plan never requests held-poster bytes + parses identical to the full manifest minus held posters, 200-fallback, tiny-manifest tail-covers-all, garbage fallback, over-cap refusal, tail-straddle stitch, real-socket Content-Range parse) + analyze clean. LIVE VERIFY vs mainnet (fresh-dir release devserver): `GET /channel/manifest/00ac7cbe…` (BegBlag.mp3, 15MB public file >12MiB → real child-map expand) with `Range: bytes=-1024` → 206 `bytes 15765358-15766381/15766382` in 30s cold; map stored (/resolve 4 chunks), full download through the route md5-identical to the known-good file, mid-range + tail slices byte-identical, tokenless 401. NOT live-driven: a true delta import of a real channel update (needs a published channel with a second head; the user's next channel publish exercises it — the fallback keeps correctness independent of it).
- [2026-08-28] v0.1.0-alpha.67 "MEDIA CURATION" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.67 (Latest, AppImage + Windows zip + APK; tag on bump commit 18e6d41). Ships 58ff09e (media/list curation trio) + 50d52fa (drawer mirrors home order, fresh channels on top) — see entries below. Local unit ALL BUILDS DONE 10:32; tag Windows CI 33163549713 + main CI 33163543121 both green. Verified APK versionCode 67/cert 79:11:66:99; zip sha256 152e102f… 53 files incl. ffmpeg/ffprobe, watchit_core.dll 48.2MB; clean-HOME Xvfb :96 smoke of the dist AppImage (wi67-xvfb2/wi67-app2 units): Terms v2 gate → I agree → home Connected · 5 peers + seeded NOTLD wall (/tmp/wi67-smoke-1-launch.png, -2-home.png). Published with /tmp/wi67-release-notes.md, no --prerelease → Latest confirmed via releases/latest.
- [2026-08-28] DRAWER MIRRORS HOME ORDER + FRESH CHANNELS ON TOP (UNRELEASED — post-alpha.66; two user asks): (1) the library drawer's list order/visibility now MATCHES the home screen's rows — browsableLists (library_arrangement.dart) takes an optional stored home-sections param and derives its lists from reconcileHomeSections (same order, same visibility as the wall; no arg = library order, the old behaviour, so old call sites/tests stand), and WiLibraryDrawer loads AppSettings.homeSections alongside the library; previously the drawer used raw library position order, which diverged from a customized Settings → Home screen arrangement. (2) reconcileHomeSections (home_sections.dart) now PREPENDS lists it hasn't seen in the stored order when they are channels (isChannel) instead of appending — a freshly subscribed/created channel surfaces at the VERY TOP of the home wall and drawer (above Continue Watching) until the user reorders (which persists the arrangement and pins it); fresh plain lists still append at the end. 663 Dart tests (+5: channel-prepend + channel-stays-where-stored reconcile pins, browsableLists follows-stored-order + channels-first units, drawer-follows-home-order widget flow) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so, seeded DB + custom home_sections_v1 omitting a channel list): home wall renders Nature Films CHANNEL row at the very top, then Zeta → Recently Added → Movies → Bees exactly per the stored order with Downloads hidden; drawer lists Nature Films (amber) / Zeta / Movies / Bees in the identical order — pre-change it would have shown Movies/Zeta/Bees/Nature Films (/tmp/widro-2-home.png, -3-home-scrolled.png, -4-drawer.png). Docs: UI-DESIGN channels section notes the top-surface + drawer-mirror behaviour.
- [2026-08-28] MEDIA/LIST CURATION TRIO (UNRELEASED — post-alpha.66; three user asks in one task): (1) "AUTO BY TYPE" REMOVED ENTIRELY — the alpha.42-era arrangement hid custom lists whose entries have no TMDB match, so browsing is always the user's own lists now: segmented control gone from the Media page, LibraryArrangement/ArrangementStore/auto helpers deleted from library_arrangement.dart (file now holds only kUncategorised/genreNames + a single-arg browsableLists(enabled lists)), auto branches stripped from main.dart's wall (incl. _EmptyVariant.autoHidden), drawer, list_home_screen; prefs library_arrangement_v1/auto_hidden_lists_v1 dropped (stale values ignored). (2) LIST EDITOR IS A TREE with move/remove: ListEditScreen (tapping a list on the Media page) now groups entries show → season → episode via groupShows (movies/singles stay top-level rows), every item/season/show row has a 3-dot menu "Move to another list…" (target picker over other non-channel lists + Create new list; typed name matching an existing list merges; duplicates in the target dropped and reported "(N already there)") and "Remove from list" (instant for one item, confirm dialog for season/show); new test/list_edit_tree_test.dart (5 tests: grouping, season remove w/ confirm, move-to-new-list, move-to-existing dedupe incl. 0x-normalization, channel lists never offered as targets). (3) CATEGORY AT PUBLISH TIME + SMARTER CHIP: DescribeItemScreen (required before anything joins a channel) gains a CATEGORY (optional) FilterChip wrap over kDescribeCategories (the 18 TMDB genre names, amber selection; a TMDB match's genres auto-select and out-of-set genres stay selectable), saved via saveUserDetails' new `category` Value param (absent = preserve) — the category rides the userEdited row into the channel manifest (bundle metadata.json already carried it), so subscribers' list pages get real filter chips instead of Uncategorised; and ListHomeScreen only shows the Uncategorised chip when the list ALSO has categorised items (all-uncategorised list → no chip row at all). 658 Dart tests (+10: 5 tree, 3 describe-category, no-chips-when-uncategorised, browsableLists repin; the old arrangement/browse-flow auto tests deleted) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so): Media page shows plain list rows (no segmented control), My Shows editor renders One Step Beyond → Season 1/2 → episode rows with menus, moved an episode into a freshly created "Extras" list live (snackbar "Moved 1 entry to \"Extras\"", drawer gained Extras · 1, season recounted), and the all-uncategorised My Shows list page shows NO chip row (/tmp/wi3chg-1…14-*.png, esp. -5-media, -8-season, -10-movedlg, -12-moved, -14-listpage). NOT live-driven: the Describe category chips (needs a channel ceremony; pinned by 3 widget tests). Docs: README/UI-DESIGN/ROADMAP gained removal/behaviour notes.
- [2026-08-28] PAID CHANNEL PUBLISH VERIFIED LINUX → ANDROID (user report, no code changes; closes the funded-wallet gap standing since the alpha.55-era note and flagged on every channel release): on alpha.66 the user did a real funded channel publish from the Linux AppImage — paid public upload to mainnet — and the item was received on an Android device (signed head over gossip → manifest fetched → item visible in the subscribed amber channel). The full Channels pipeline is now live-verified end-to-end across platforms; no open channel verification gaps.
- [2026-08-28] v0.1.0-alpha.66 "CHANNELS, ROUNDED OUT" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.66 (Latest, AppImage + Windows zip + APK; tag on bump commit d520ebf). Ships 86997ba (channel Publish-an-item is file-first), 988b879 (Check TMDB on Describe), 04cff7a (own channel on home wall + channel subscriptions sync over My W@tch) — see entries below. Local unit ALL BUILDS DONE 22:45 (2026-08-27); tag Windows CI 33123572501 green. Verified APK versionCode 66/minSdk 24/cert 79:11:66:99; zip sha256 ef7f0918… 53 files incl. ffmpeg/ffprobe, watchit_core.dll 48.2MB; clean-HOME Xvfb :96 smoke of the dist AppImage (wi66-xvfb/wi66-app units): Terms v2 gate → I agree → home Connected · 5 peers + seeded NOTLD → drawer shows Upload (Private · blue) and Channels (Public · amber) doors (/tmp/wi66-smoke-1-launch.png, -2-home.png, -3-drawer.png). Published with /tmp/wi66-release-notes.md, no --prerelease → Latest confirmed via releases/latest. (Paid public publish leg verified by user on this release — see entry above.)
- [2026-08-27] OWN CHANNEL ON THE HOME WALL + CHANNEL SUBSCRIPTIONS SYNC OVER MY W@TCH (UNRELEASED — post-alpha.65; user asks: a created channel should appear with its badge on the creator's machine, and channels should cross My W@tch sync AS channels, not as personal lists): Dart-only, no Rust changes. (1) OWN CHANNEL AS AMBER LIST: ChannelService.syncNow gains _syncOwnChannel — the creator's channel appears as the same read-only amber channelPubkey list subscribers get: EMPTY the moment the channel is created (list `channel-<pk16>`, title from config, renamed-before-first-publish follows setMeta), each published head imports the manifest via the exact subscriber fetch+verify path (_importManifest; importedSeq in prefs channel_own_import_v1; failed fetch leaves the list standing + lastProblem), channel removal deletes the list; create/publish-update/remove flows fire an immediate syncNow. Media-page "Unsubscribe" on the OWN channel list is guarded (snackbar → manage from Channels page). (2) CHANNELS SYNC AS SUBSCRIPTIONS: channel lists are EXCLUDED from the personal sync doc everywhere (membershipOf/libraryLookupKeys/buildDoc; mergeRemoteDocs never adds entries to nor tombstone-strips an isChannel list — and on upgrade the vanished snapshot entries auto-tombstone the badge-less copies old builds synced, deliberate cleanup); NEW doc section `channels` {subs: {pubkey: {code, added_ms}}, removed: {pubkey: ms}} — subs from ChannelService records + status codes, OWN channel announced with added_ms=createdAtMs (restore re-stamps → beats old stones), unsubscribe writes tombstones (prefs channel_sub_stones_v1, never regresses; ChannelService.subscribe/unsubscribe grew addedMs/removedMs params). Receiver: pure MyWatchSync.channelActions LWW per pubkey (newest add vs newest stone, tie→stone, own skipped, wchn1-/64-hex validated) → _applyChannelActions subscribes by code (channel arrives amber + auto-updating via normal gossip import), unsubscribes carrying the STONE's time (not now — a racing re-subscribe elsewhere must win), adopts newer stones; new cycle stage 6b + SyncCycleResult.channelsChanged in the summary; ChannelService.syncView() feeds both sides (null while unsupported → stage no-ops, incl. in tests). 669 Dart tests (+21: own-channel matrix incl. fetch-fail/removal/rename, stones/adopt/syncView, buildDoc channels section + exclusions, merge guards, channelActions matrix) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so, WATCHIT_AUTH_TOKEN curl): channel created via authed route → after the Channels-screen refresh the drawer shows "Ella Nature Films · 0" amber + Media page "read-only, updates automatically" badge + own-guard snackbar; then app↔devserver linked over a real My W@tch invite, devserver created its own channel and published a sync doc announcing it → app AUTO-SUBSCRIBED within one 30s cycle (Subscribed card "Waiting for the channel's current head"), and the app's republished doc (read back via the devserver) carried channels.subs with BOTH pubkeys+codes while `lists` held only Movies (/tmp/wichs-1…9-*.png). NOT live-tested: own-manifest import after a real paid publish (funded-wallet gap as ever; unit-tested) and the subscribed list materializing (needs a published manifest — pre-existing alpha.65 path). Docs: ARCHITECTURE My W@tch channels bullet + Subscribing own-channel note, ROADMAP 2 ticks
- [2026-08-27] CHANNEL PUBLISH-AN-ITEM IS NOW FILE-FIRST (UNRELEASED — post-alpha.65; user ask: replace the pick-from-library dropdown with the Upload flow — local file → encoded versions → describe/TMDB → channel, with optional add-to-library): NEW screens/channel_publish_screen.dart, an amber single-file Upload-shaped flow launched by My Channel → "Publish an item": Choose a file (one per pass — the no-bulk rail stands) → probe verdict + QUALITY tier checkboxes (reuses publish_plan/ffmpeg; per-tier ≈size/≈ANT off one live estimate; note says the manifest publish is a separate later step) → required DESCRIBE section pushing the shared DescribeItemScreen → ChannelAttestationDialog on the publish button → encode+upload queue (private-visibility uploads — datamaps only go public inside the manifest; retry/skip/stop per version) → each finished tier AUTO-STAGED via ChannelService.addMyItem (name = tierOutputName, so tiers fold into one item via the version picker; describe row keyed by parsed source name is shared by all tiers) → done page "Staged for the channel… still private" + optional "Also add to my library" list picker + "Back to the channel" (pops true → snack + reload). The old picker survives as a smaller "Add an item already in the library" TextButton (same describe+attestation rails) for content whose source file is gone. REFACTORS: DescribeItemScreen generalized — entry now optional, new fileName/localPath params (metadata row keyed by the file name exactly like an entry's; frame grabs sample the LOCAL file, so From-video-frame works pre-upload); publish_screen.dart's list picker + merge extracted as top-level pickTargetLists/addEntriesToLists (both now skip read-only channel lists) shared by both done pages; ChannelsScreen gained an ffmpeg test-override forwarded to the pushed screen. 648 Dart tests (+5: new test/channel_publish_flow_test.dart — describe-gating w/ "Still needed" hint, full file→describe→attest→upload→staged+library flow asserting staged names/addresses + userEdited row + videoInfo, attestation-declined stages nothing, no-wallet nudge; channels_screen_test split into new-screen + library-picker tests) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so, channel + throwaway hardhat wallet created via WATCHIT_AUTH_TOKEN curl): My Channel shows both doors → Publish an item → picked a real 1080p HEVC 10-bit lavfi mkv through the GTK dialog (GOTCHA: location-bar autocomplete races even slow typing — use a short unique path, /tmp/qq symlink) → verdict + amber tiers High/Medium/Low ticked, Original unticked → LIVE mainnet estimate ≈0.257 ANT/tier, "3 uploads ≈ 0.771521 ANT" → Describe prefilled from file name, description typed, From video frame sampled the LOCAL file (12 frames) → crop → Save → "Described for subscribers ✓" → attestation dialog → REAL ffmpeg encodes of all 3 tiers ran, upload correctly failed at payment on the 0-balance wallet ("Insufficient tokens… need 258967… atto" ≈ the estimate) with Try again/Skip/Stop UI (/tmp/chpub-4-own.png, -5-publish-empty, -7-tiers, -8-estimate, -9-describe, -10-frames, -11-crop, -12-described, -13-back, -14-attest, -15-running, -16-payfail.png). NOT live-tested: the paid legs (item upload + manifest publish) — same funded-wallet gap as alpha.65. Docs: ARCHITECTURE Publishing bullet rewritten, ROADMAP channels item, UI-DESIGN My Channel line, README step 6, PLAN Part 4 tree CHANGED note
- [2026-08-27] CHECK TMDB ON DESCRIBE-THIS-ITEM (UNRELEASED — post-alpha.65; user ask: a public-domain movies channel should be able to use the TMDB database while publishing): the channel Describe page gains a **Check TMDB** button (between Year and Description) that looks the item up on TMDB, previews the match (poster/title/rating/genres/synopsis + attribution note) in a TmdbMatchDialog, and on **Use these details** stores the FULL row through the normal metadata pipeline — new `MetadataService.lookupTmdb` (uncached explicit lookup) / `tmdbPosterBytes` (preview) / `adoptTmdbMatch` (persists via the extracted `_persistMatch`, poster under the shared TMDB naming, `userEdited: false`, memory + notify) — then prefills title/year/description and shows the adopted poster in the artwork slot (Save enabled). Search name via new top-level `tmdbSearchName`: typed title/year when the publisher changed them (correction beats a mangled file name, drops the IMDb tag), else the parsed file name (keeps the exact `/find` lookup); season/episode markers always from the file name. Keyless → dialog pointing at Settings → Metadata; miss → snackbar; TmdbException → snackbar. Because the adopted row carries rating/genres/tmdbId and the describe-save (`saveUserDetails`) preserves those extras, keyless subscribers get the complete metadata + poster from the channel manifest. GOTCHAS hit: (1) the button's spinner must stop BEFORE the preview dialog opens (an indeterminate spinner behind the dialog never settles pumpAndSettle); (2) `_persistMatch` image saves are now sync IO (writeAsBytesSync — async dart:io hangs the fake-async zone, same rule as user_metadata.dart); (3) the auto-matcher runs on describe-page open, so tests asserting "cancel adopts nothing" must use an unmatchable file name and filter for found rows. 643 Dart tests pass (+11 in new test/describe_tmdb_test.dart: search-name matrix, lookup-doesn't-cache, adopt persists row+poster+memory, keyless/miss/accept/cancel widget flows) + analyze clean; LIVE VERIFY Xvfb :77 clean HOME via throwaway entrypoint (deleted) against the REAL TMDB API: NOTLD describe page → Check TMDB → real match dialog (real poster, 7.6/10 · Horror · Thriller · Science Fiction, real synopsis) → Use these details → description 143 chars + poster in artwork slot + Save amber/enabled → Save wrote the row (tmdb_id 10331, rating 7.606, genres, movie_10331.jpg on disk, user_edited=1) (/tmp/witmdbchk-1-describe.png, -2-dialog.png, -3-filled.png). Docs: ARCHITECTURE Publishing bullet, ROADMAP channels item, UI-DESIGN My Channel line, README step 6, PLAN-personal-vs-channels deviations note
- [2026-08-27] v0.1.0-alpha.65 "CHANNELS" RELEASED — https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.65 (Latest, AppImage + Windows zip + APK; tag on bump commit 907c86c). Ships 823001b (Channels: public signed media lists + safety wall, plan Parts 2+3 — see entry below). Local unit ALL BUILDS DONE 18:45; tag Windows CI 33104404670 + main CI 33104404055 both green. Verified APK versionCode 65/minSdk 24/cert 79:11:66:99; zip sha256 5795d27a… 53 files incl. ffmpeg/ffprobe, watchit_core.dll 48.2MB (grew with channel code); clean-HOME Xvfb :96 smoke: Terms v2 gate (incl. new "5. Public channels" section) → I agree → home Connected · 5 peers + seeded NOTLD → drawer shows Upload (Private · blue) and Channels (Public · amber) doors (/tmp/wi65-smoke-1-launch.png, -2-home.png, -3-drawer.png). Published with /tmp/wi65-release-notes.md, no --prerelease → Latest confirmed via releases/latest. NOT yet live-tested: the paid public publish leg (needs a funded wallet) — user is testing on real devices now.
- [2026-08-27] CHANNELS: PUBLIC SIGNED MEDIA LISTS + SAFETY RAILS (released in alpha.65; implements Parts 2+3 of docs/PLAN-personal-vs-channels.md in one piece, per the plan's "core never ships without rails"; user: "Yes let's move on with the plan parts 2 and 3"): a channel is an Ed25519 identity — code `wchn1-<base32(pubkey)>`, secret derived from the channel's OWN 12-word BIP-39 phrase (SLIP-0010 ed25519 master key, spec vector pinned; deliberately separate from the hot-wallet phrase) via the wallet-style show-words→retype-3 ceremony, stored in the OS keychain (`channel-key` entry beside the wallet's, WalletStore generalized w/ `named()`) with 0600 channel.key fallback. RUST (new channel.rs + channels.rs; engine grows `channels`; ~+900 lines): manifest = bundle-spec-v2 zip + channel.json uploaded PUBLICLY via ant-core `file_upload_public_with_progress` (datamap chunk fetchable by anyone, same payment batch; job machinery shared w/ Upload — upload.rs refactored, Outcome grows `seq`, new `announcing` phase); mutability via signed head records `{seq, manifest, sig}` gossiped in a self-keyed CRDT KV store on an x0x topic derived from the pubkey — topic is public by construction, strangers can write, so heads are trusted PURELY by signature (`verify_head` binds pubkey+seq+manifest; readers take max valid seq); the channels agent is its OWN x0x agent under <data>/channels/ (independent lifecycle from My W@tch), one store per channel w/ per-channel snapshot dirs so unsubscribe deletes cleanly; next seq = max(local, visible-head)+1 so a restored channel continues its history. Routes (all protected): GET /channels, POST /channel/{generate,create,restore,meta,publish,subscribe}, DELETE /channel[/subscribe/{pubkey}], GET /channel/manifest/{addr} (data_map_fetch + data_download — the ONLY network datamap fetch in the app, manifests are public by intent). DART: schema v10 (media_lists.channel_pubkey marks read-only channel lists), channels_api.dart + channel_service.dart (5-min head-follow loop; fetch→verify-pubkey→import manifest as a FULL-REPLACE amber-badged list via the bundle machinery — metadata/posters gap-fill so keyless subscribers render offline; my-channel item staging; manifest build reuses buildBundle + new extraTextMembers param; restore pulls the newest manifest back to repopulate items), channels_screen.dart (Subscribed|My Channel segments per plan Part 4: cards w/ update state, Add channel dialog w/ paste/QR-scan + subscriber note, create= form→ChannelSeedBackupScreen→FirstPublishGateScreen full-screen public/permanent/attributable warning confirmed by TYPING the channel name, code+QR share dialog, backup-status row, danger zone), describe_item_screen.dart (REQUIRED title/description/artwork before the attestation — Save disabled until complete, artwork via image file / frame-picker+crop reuse), ChannelAttestationDialog (exact plan wording, checkbox-gated), cost preview dialog (live /upload/estimate + balance check + first-publish warning). SAFETY WALL: Terms v2 (kTermsVersion 1→2, new section 5 "Public channels", re-prompts every user), amber everywhere public (WiTokens.channelAmber + ChannelBadge widget: drawer tile "Channels · Public · anyone with the code" beside blue Upload, home-wall row badges, media-page rows "read-only, updates automatically" w/ Unsubscribe-only menu, channel lists route to browse not edit). DEVIATIONS from plan text (documented in the plan): owner signature covers the HEAD (content addressing makes zip-internal channel.sig redundant); no category dropdown on Describe (derives from file name). TESTS: 58 cargo (+12: SLIP-0010 vector, phrase→identical-channel, code round-trip incl. case, head sig binds every field, store persistence, route matrix incl. 401s) + 655 Dart (+23: manifest parse/build round-trip, subscribe/unsubscribe, syncNow import/replace/skip/pubkey-mismatch-refused, item staging, segments/dialogs/ceremony/gate widget flows, attestation + describe gating, terms v2 pin) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so): Terms v2 gate → drawer w/ both doors → full create ceremony (real words, retype 3, typed-name gate) → channel EXISTS (code wchn1-gnidy…resa, key in file fallback w/ red callout) → QR dialog → pick NOTLD → Describe prefilled from seeded metadata → attestation → staged → cost preview w/ LIVE mainnet estimate (≈0.143 ANT / 3 chunks for the real 55KB manifest); devserver RESTORED the ceremony phrase to the IDENTICAL code (determinism live); app subscribed to a devserver's channel over real gossip, and with a TEMPORARY announce-only route (removed before commit) the signed head crossed the network devserver→devserver AND devserver→app in seconds, signature-verified, with the app correctly failing the placeholder manifest fetch ("DataMap chunk not found") — every leg live except the paid public upload itself (needs a funded wallet, exactly like alpha.55 pre-user-test); channel list badges verified on home wall + drawer + media page (/tmp/wich-*.png, 30+ shots). NEXT: ship as alpha.65 after a real funded publish test
- [2026-08-27] v0.1.0-alpha.64 "UPLOAD" RELEASED — see Current Status; tag v0.1.0-alpha.64 on bump commit 0605e2e, all three artifacts (AppImage + Windows zip + APK), Latest confirmed via releases/latest. Ships the Publish → Upload rename below (naming/copy only). Windows zip from the v-tag CI run 33094207338 (completed 17:05 after ~55 min — the long x0x Windows build); local watchit-build unit had built APK+AppImage earlier (16:58/16:40). Desktop users ≥alpha.56 get the update notification. NEXT: plan Parts 2+3 (Channels core + safety rails)
- [2026-08-27] PUBLISH → UPLOAD RENAME (6a172ad pushed, RELEASED as alpha.64; Part 1 of docs/PLAN-personal-vs-channels.md, user approved the whole plan incl. all open-question recommendations): the private upload flow stops using the word "publish" — reserved exclusively for the future public Channels feature. Drawer tile Publish → Upload w/ subtitle "Private · only your devices"; Upload screen title/copy reframed private ("Uploads are private: only you and your linked devices can watch. Nothing is published."), buttons Upload / "Upload N files" / "Choose files to upload" / "Upload more", done header "Uploaded", "N of M uploads finished", rights checkbox reworded ("my own work, verifiably public domain, or a personal copy for private use"), done-page notes My W@tch sync carries uploads to linked devices; Settings PUBLISHING section → WALLET (comment: one wallet funds both spaces), wallet tile/screen copy publish → upload; My W@tch sync activity strings "Publishing…" → "Sending this device's library to your devices…". Internal identifiers (PublishScreen/PublishApi/PublishTier/file names) deliberately unchanged; terms.dart deliberately untouched (versioned legal text — reworded at kTermsVersion 2 when Channels land). Docs renamed with before-2026-08-27 notes (README, ARCHITECTURE, ROADMAP incl. parking-lot channels idea → points at the plan, UI-DESIGN incl. stale My W@tch drawer line fixed, NAMING); plan doc status → Part 1 implemented. 609 Dart tests + analyze clean (publish_flow_test string pins updated); GUI verified Xvfb :77 clean HOME (/tmp/wiup-2-drawer.png tile+subtitle, -3-upload.png screen copy, -4-settings-wallet.png WALLET section). NEXT: Part 2+3 — Channels core (Ed25519 identity + 12-word ceremony, signed manifest, public upload, x0x head topic, subscribe) + safety rails, one release
- [2026-08-27] CHANNELS PLAN EXTENDED: MENU LAYOUT + KEY BACKUP + NON-TMDB METADATA (docs/PLAN-personal-vs-channels.md, still PLAN ONLY — no code; user follow-ups): (1) NEW Part 4 proposed menu tree — Upload (renamed Publish, blue) and Channels (NEW, amber) as sibling drawer tiles ("separate doors" wall), Channels screen = Subscribed segment (channel cards + Add channel paste/scan) | My Channel segment (create/restore ceremony, code+QR, items list, + Publish an item, backup status); subscribed channels render as badged read-only lists on the normal home wall/drawer; Settings → PUBLISHING renamed Wallet (one wallet funds both spaces), My W@tch stays Settings → Network; mobile gets subscribe-only. (2) Channel master key backup is REQUIRED and part of creation: channel Ed25519 key derived from its OWN 12-word BIP-39 phrase (SLIP-0010) via the shipped wallet ceremony (show → retype 3 → persist), so backup exists before the channel does; lost key = channel frozen forever (code IS the pubkey, no recovery/re-issue), leaked key = attacker publishes as you irrevocably; deliberately a SEPARATE phrase from the disposable hot wallet; My Channel shows backup-status row; "Restore channel" re-derives same code on a new machine. (3) Fresh content is NOT in TMDB → manifest must be self-describing: reuses bundle-spec-v2 metadata.json+posters and shipped userEdited import semantics (receiver's matcher never overwrites, keyless renders offline); NEW required "Describe this item" step in add-to-channel BEFORE the attestation — title/description/artwork mandatory (Add disabled until present), artwork via shipped image-file/frame-picker+crop, rows also saved locally as normal Edit-details so library and channel stay consistent. Open questions +2 (separate phrase agreed? menu layout agreed?)
- [2026-08-27] PERSONAL vs PUBLIC CONTENT SPACES PLANNED (docs/PLAN-personal-vs-channels.md, PLAN ONLY — no code; user ask: personal upload/consumption + a YouTube-style public channel, and how to name/separate them so nobody publishes copyright material by accident): KEY FINDINGS — today's Publish is already the private half (upload.rs uses file_upload_with_progress → Visibility::Private, datamap never on the network; My W@tch sync makes it consumable across devices), and current ant-core (81a0a24) is Chunk-only (no Pointer/Scratchpad/Register), so a channel needs an app-layer mutable head. PLAN: (1) rename Publish → "Upload" and make My W@tch the umbrella brand for the private space ("publish" reserved exclusively for the public act — vocabulary is the first safety wall); (2) Channels = Ed25519 channel keypair (code `wchn1-<pubkey>`, key in OS keychain), signed manifest = bundle-spec-v2 zip + channel.json/sig uploaded PUBLICLY, head `{seq, manifest_address, sig}` distributed via an x0x gossip topic derived from the channel pubkey (subscribers replicate the CRDT store so late joiners don't need the owner online; unsigned records filtered); subscribing imports a read-only auto-updating channel list; publishing an existing private upload is cheap (deterministic chunks dedup — only datamap chunk + manifest are new); v1 has NO directory (code-only sharing — biggest liability reducer); (3) safety rails: separate doors (never a toggle on Upload), per-item rights attestation, full-screen first-publish ceremony w/ typed channel name, kTermsVersion bump w/ channel section, amber PUBLIC badging vs blue private. Build order: rename release first, then channels core + rails in one release. Open questions for user in the doc (naming, no-directory v1, mobile subscribe, cost preview): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.63 — Latest, AppImage + Windows zip + APK (tag on bump commit 023919b). Ships 26ab96f (TMDB metadata + posters sync to keyless devices) and 3a06199 (edit-sync root-cause fixes, sync activity card, simplified home status bar w/ My W@tch segment, My W@tch moved to Settings → Network). Verified: APK versionCode 63 / cert 79:11:66:99 (== prior releases); Windows zip from v-tag CI run 33066559114 (sha256 f1836407…, 53 files incl. ffmpeg/ffprobe, watchit_core.dll); main CI 33066556476 green; clean-HOME Xvfb :96 smoke of the dist AppImage (wi63-* units): Terms gate → I agree → home shows NEW simplified bar "Connected · 30 peers" + seeded catalog w/ poster (/tmp/wi63-smoke-1-launch.png, -2-home.png). Published with /tmp/wi63-release-notes.md, no --prerelease → Latest confirmed via releases/latest. Desktop users ≥alpha.56 get the update notification
- [2026-08-27] MY W@TCH SYNCS TMDB METADATA + POSTERS TO KEYLESS DEVICES (UNRELEASED — post-alpha.62; user ask "go further propagate TMDB posters as well for full keyless sync"): full TMDB matches now travel between linked devices — a device WITHOUT a TMDB key gets posters, descriptions, ratings, categories, episode stills, and show/season texts from a linked device that has (or had) a key. Dart-only (the Rust art transfer is already generic sha256→path). PROTOCOL: every sync doc now publishes a compact `have` list (sorted 8-hex FNV-1a hashes of the library lookup keys whose metadata + referenced artwork this device holds in full; ALWAYS present, even empty — its presence marks a tmdb-sync-capable build, so alpha.62 docs are skipped cleanly both ways). A device publishes a `tmdb` doc section — full cache rows (userEdited=false, `updated_ms`=fetchedAt) + per-show/per-season shared texts DEDUPED into `shows`/`seasons` maps keyed by lookup-key prefix + a `files` manifest (fileName → sha256/size) — ONLY for keys some remote `have` list lacks, so the section DRAINS TO NOTHING at steady state and re-publishes automatically for late joiners (receivers re-serve rows AND files). Receivers fill only missing rows and cached misses (`local != null && local.found` skips — a user edit, own TMDB match, or bundled seed always wins), then pull missing art files by manifest from up to 2 owners (online-ordered, never gated) and save them under the ORIGINAL TMDB names (movie_<id>.jpg / tv_<id>_s<n>.jpg / …_still.jpg) so shared season/show files light up every row; `safeArtFileName` guards the posters dir against path-y names from a peer. Budget drop order now watch states → tmdb rows (quarter per round, shows/seasons/files pruned via `shrunkenTmdbSection`, self-healing — log only) → user meta rows; art index posted to the embedded client now covers ALL referenced files (user_ + TMDB) of found rows. TWO BONUS FIXES: (1) MyWatchApi now `utf8.decode(res.bodyBytes)` — package:http decodes application/json without charset as latin1, so any non-ASCII synced text ("Amélie", "S01E02 · Pilot") mojibake'd (caught by a new test); (2) artwork retry backoff is now PROGRESSIVE (`artRetryDelayMs`: 1 min doubling to the 10-min cap, counter clears on success) — the first fetch right after link-up routinely fails with x0x "recipient key material unavailable: agent not found" (key material still exchanging) and the old flat 10-min wait is why alpha.62 art "eventually" synced; seen live in this task's verify. 609 Dart tests (+19, new test/my_watch_tmdb_sync_test.dart: hash/name-guard/budget-priority/shrink-prune units, winners + files parsing, full keyless-adopt + art-pull cycle, miss-upgrade vs never-overwrite, need-based publish + drain, no-have-no-section, art-pending-keeps-key-off-have, episode extras + 3-file pull) + analyze clean. LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so, keyless): app joined a devserver link via UI (Settings → Network → My W@tch), devserver published a Charade entry + tmdb row + 157KB poster manifest + art index → app adopted title/year/genres/rating/description AND pulled the poster byte-identical (sha 3701410a…) saved as movie_4808.jpg; wall card + detail page render it, bar shows "My W@tch: synced"; app's republished doc carried both have hashes + BOTH file manifests, devserver then fetched the app's seeded NOTLD TMDB poster BACK byte-identical (sha 8c2e0fed…), and after the peer reported full `have` the app's tmdb section drained away (/tmp/witmdb-*.png, esp. -10-home-synced, -11-detail). Docs: ARCHITECTURE My W@tch gains the keyless-metadata bullet, ROADMAP ticks it, README step 5 updated (+ fixed the stale "drawer's My W@tch page" → Settings → Network)
- [2026-08-27] MY W@TCH: EDIT-SYNC FIXES + SYNC VISIBILITY + SETTINGS MOVE (UNRELEASED — post-alpha.62; user report: fresh publishes sync but custom artwork/descriptions still don't, plus three UI asks): THREE ROOT-CAUSE FIXES for "entries sync, edits don't": (1) DOC BUDGET REPRIORITISED — the old budget loop dropped ONLY meta rows when the sync doc topped 56KB, while up to 300 watch states (~45KB alone with a long viewing history) and all list entries were kept, so a well-used library permanently crowded every detail edit out of the doc; new `buildDocWithinBudget` drops watch states first (stalest quarter per round, newest kept — they re-sync when there's room) and meta rows only when no watch state is left, with a problems entry when edits still don't fit. (2) ARTWORK PULL NO LONGER GATED ON PRESENCE — `_fetchMissingArt` returned 0 when x0x `presence()` listed no online device, and presence under-reports on real networks (the user's phone showed the desktop offline while both were up); presence now only ORDERS candidates (online-looking first), up to 2 owners are tried per file, failures land in the sync problems + 10-min backoff. (3) BUNDLE IMPORT LWW POISON — metadata.json rows now export `fetchedAt` and import preserves it (was: stamp `now` + keep userEdited=true, so a phone that imported a bundle held rows "newer" than the desktop's real edits and won the LWW merge forever). Cycle also HARDENED: stages 3–6 (watch states, maps, detail edits, artwork) each catch their own errors into the problems list instead of silently killing the rest of the cycle after the list merge already saved. VISIBILITY: new `MyWatchSync.status` ValueNotifier (MyWatchSyncStatus: supported/linked/agentState/lastSyncMs/syncing/activity/lastCycleAt/lastSummary/problems) updated at every stage; My W@tch screen gains a SYNC ACTIVITY CARD under Last sync (spinner + stage while running, outcome + "Checked X ago" + red problem lines while idle); HOME STATUS BAR simplified ("Connected · 5 peers" / "Connecting…" / "Connection error" — dropped the "Autonomi network:" prefix and attempt detail) and gains a same-line right-aligned My W@tch segment (green/amber dot + "synced X ago"/"syncing…"/"connecting…"/"sync issue", hidden while unlinked, tap opens the My W@tch page; fed by the notifier, no extra polling; NetworkStatusBar gained a healthProvider test hook). MY W@TCH MOVED into Settings → NETWORK (bottom of section, after the traffic footnote) and REMOVED from the drawer. 590 Dart tests (+11: budget-priority matrix, presence-ignored art pull, refused-owner problems on the notifier, bundle fetchedAt round-trip, status-bar wording/segment/tap-nav, settings-tile nav + drawer-gone, activity card) + analyze clean; two tests gained ensureVisible (new UI shifted tap targets). LIVE VERIFY Xvfb :77 clean HOME (debug bundle + release .so): linked via UI, devserver joined the invite, published a meta row (title/description/art manifest for seeded NOTLD imdb:tt0063350) + art index → within one 30s cycle the app adopted title+description AND pulled the 1.4KB poster byte-identical (sha256 146cc175… both sides), home wall renders the synced poster + title, bar shows "● My W@tch: synced just now", activity card cycled "Everything is in sync. / Checked just now" (/tmp/wi-sync-1…11-*.png, esp. -4-settings-top, -9-mywatch-linked, -11-home-synced). NOTE: the pre-existing published sync-state file (`mywatch_sync.json` lastPublished fingerprint) makes both devices republish once after upgrade — harmless
- [2026-08-27] DOCS REFRESH THROUGH ALPHA.62 (docs-only, no code/tests): user-facing docs caught up from the alpha.57 refresh — they had no mention of My W@tch or anything after alpha.57. README: intro gains the My W@tch sync pitch, NEW How-it-works step 5 (link devices, QR/invite, what syncs, invite-is-the-key warning), Status extended alpha.58–.62. ARCHITECTURE: NEW "My W@tch — device linking & sync" section (x0x agent, blake3-derived topic, CRDT store, LWW merges + tombstones, shrunk-map travel, sha256 artwork manifest + pull transfer, auth'd routes, desktop+Android/iOS-stubbed, online-together constraint), Publish section gains the alpha.59 finalize-fix note + videoInfo stamping, broken pasted `/health` fragment at the end of Publishing removed, diagram + Data-&-state ("optional future sync" → shipped) + repo-layout comments updated. ROADMAP: status header rewritten (2026-08-27, alpha.62, .58–.62 summary), Publish section gains 4 checked items (TV edit scopes .58, >12MiB fix + crop .59, Terms + quality explainer .60, version-picker labels incl. the post-.62 videoInfo stamp), NEW shipped section "My W@tch — device sync" (4 checked + open: iOS, sync-while-apart), parking-lot sync idea struck through as shipped. UI-DESIGN: About line gains Terms page, NEW "### 8. My W@tch" screen, Built-so-far → alpha.62. NAMING: Publishing section documents the entry videoInfo stamp ("1080p H.264 · 1.1 GB" picker lines, pre-existing entries backfill on first play). VISION: goal 9 gains My W@tch peer-to-peer sync. BUNDLE-FORMAT/SEED-CATALOG/BRAND/PLAN-* untouched (accurate as-is)
- [2026-08-27] PUBLISHED ENTRIES CARRY `videoInfo` (resolution + codec in the version picker): the Publish → Add-to-library step now stamps each entry with a `480p H.264`-style label via new `tierVideoInfo()` in publish_plan.dart (encode tiers = real output height + always H.264; Original = probed height + codec via new `videoCodecName()`, null for unprobed/audio sources), so the detail-page version dropdown and cards show `1080p H.264 · 1.1 GB` instead of size-only — matching seed-catalog/imported entries. Only fixes NEW publishes; pre-existing published entries stay size-only until first playback backfills resolution (no codec) via the player's `resolutionLabel` path. Label syncs to linked devices automatically (meta rows already carry `video`). 579 Dart tests (+4 in publish_plan_test, videoInfo assertion added to the flow test's add-to-library check) + analyze clean
- [2026-08-27] ANDROID MY W@TCH VERIFIED ON A REAL PHONE (user report, no code changes): My W@tch sync completed between the desktop AppImage and an Android phone on the night of 2026-08-26→27 — closes the last open verification gap from alpha.61's Android build (e10b249, "NOT yet tested on a real device"): the x0x agent, device linking, and list/viewpoint sync all work on real Android hardware
- [2026-08-27] v0.1.0-alpha.62 "EDIT SYNC" RELEASED — see Current Status; tag v0.1.0-alpha.62 on bump commit e7968a5, all three artifacts (AppImage + Windows zip + APK), Latest confirmed. Smoke drove the shipped AppImage clean-HOME through Terms gate → home connected · 5 peers → drawer → My W@tch screen; Windows zip came from the v-tag CI run 33047195805
- [2026-08-26] MY W@TCH SYNCS USER EDITS + FULL-QUALITY ARTWORK (7fcb27d, RELEASED as alpha.62; implements the manifest + direct-transfer design the user greenlit — nothing is downscaled): the sync doc gains META ROWS — every `userEdited` metadata_cache row travels with title/year/description/episode label INLINE (descriptions capped 2000 chars, doc budget 56KB, oldest rows dropped first) and artwork as a MANIFEST ONLY (sha256+size), because the store's 60KB value cap would force downscaling. Rows merge LWW by edit time across devices (`applyRemoteUserDetails` stamps the REMOTE time so both sides converge instead of ping-ponging; a winning text-only row deletes the losing local `user_` poster; TMDB artwork is never touched and TMDB extras — rating/genres/stills — survive the overlay). Artwork bytes travel OUTSIDE the store over x0x direct messages: each sync cycle the app publishes its art index (sha256 → path of its `user_` posters, POST /mywatch/art/index); a device that adopted a row whose manifest it can't satisfy pulls the file from any ONLINE owner (POST /mywatch/art/fetch; any device whose row names the hash serves it, so once synced the original editor need not stay online), verifies the hash, saves under the normal `user_` naming — byte-identical, full quality. TRANSPORT GOTCHA found live: the first cut pushed the file as a back-to-back chunk-DM stream + art_end, and x0x 0.39.10 silently loses the leading DMs of a ≥5-message burst (sends succeed, only the tail is delivered; ≤4-message bursts and single DMs up to ~40KB payload arrive fine — probed devserver↔devserver with graded sizes) → REWRITTEN PULL-BASED: receiver requests each chunk (`art_req` v2 with seq), server answers exactly ONE chunk (seq/total/size/data), 10s per-chunk timeout × 3 attempts, 120s whole-file deadline, 30KB raw chunks, 10MB cap both ends, every request carries the blake3 link-secret tag. New deps: crypto (Dart, manifests) + sha2 (Rust, already in the x0x tree). 46 cargo + 575 Dart (+12, new my_watch_meta_sync_test.dart; fake_embedded_http grew the art routes) + analyze clean. LIVE VERIFY devservers (/tmp/artsync/verify.sh): link+join, 157KB 6-chunk fetch in 13ms sha256-identical, 1MB and full-10MB-cap files clean, unknown sha refused, tokenless 401. GUI Xvfb :77 (debug bundle + release .so, clean HOME): app joined the link through the UI, devserver published a meta row for seeded NOTLD (imdb:tt0063350) + art index → within one cycle the app adopted the "— Synced Cut" title + description AND pulled the 157KB poster (user_imdb_tt0063350_*.jpg byte-identical, home wall + detail page render it, TMDB 7.6/genres intact), then RE-SERVED it: the app re-published the manifest and the devserver fetched the poster back FROM the app byte-identical (/tmp/wiartx-1-launch.png … -8-detail.png). BUILD GOTCHA: `cargo build --release --bin devserver` builds the fresh cdylib in deps/ but does NOT uplift it to target/release/libwatchit_core.so — a stale .so 404'd the new routes mid-verify; run plain `cargo build --release` before copying the .so anywhere
- [2026-08-26] v0.1.0-alpha.61 "MY W@TCH SYNC" RELEASED — see Current Status; tag v0.1.0-alpha.61 on bump commit 684f718, all three artifacts (AppImage + Windows zip + APK), Latest confirmed. Smoke drove the shipped AppImage to the My W@tch screen showing the new sync copy (clean HOME: Terms gate → home connected · 5 peers → drawer → My W@tch)
- [2026-08-26] MY W@TCH SYNC + ANDROID + QR SCAN (e2afcea + e10b249 + ffb946d, RELEASED as alpha.61): (1) SYNC — linked devices keep watch lists AND viewpoints (resume points) in sync via the link's CRDT KV store. Each device publishes under its bound keys: `<agent>/sync` (JSON doc: lists matched across devices BY TITLE like the import merge, entries w/ added_ms, removal tombstones, watch states) and `<agent>/maps/N` (base64 SHRUNK data maps via new verify::shrunk_map_bytes — a few hundred bytes regardless of file size, so synced entries are PLAYABLE remotely; receiver imports via POST /datamap, expanding wrapper chunks over the network like a shared .datamap). Store quota maths: 64KiB/value (publish cap 60KB), 64 keys + 256KiB per agent → ≤3 map part keys (stale parts removed), doc caps 400 entries/300 watch states, overflow dropped+logged. Merge = LWW element set per (title, address): newest add vs newest remove wins; tombstones from a persisted membership snapshot diff (support-dir mywatch_sync.json, 90-day GC); a list emptied purely by remote tombstones is deleted; viewpoints via existing WatchStateStore.mergeAll (newest updatedAt, never regresses). Rust: MyWatchStore::publish_sync/sync_docs + POST/GET /mywatch/sync (protected; the route attaches maps from the mapstore); watcher/status now ignore keys under a device's own prefix (else own publishes stamp last_sync / show as bogus devices). Flutter: services/my_watch_sync.dart (30s cycle gated on linked+ready, pure static merge fns unit-tested, map import w/ 10-min retry backoff, MyWatchSync.revision ValueNotifier → HomeScreen reloads), Sync now button + updated copy, started in main(). (2) ANDROID — the x0x tree cross-compiles under the NDK; only casualty was link-time getifaddrs (Android libc API 24+) → build-android.sh passes --platform 24 (app minSdk already 24, Flutter default); x0x+tokio-util target block includes android (keyring stays desktop), mywatch stub is iOS-only, drawer tile on all platforms; libwatchit_core.so 27→37MB. VERIFIED on a real Android phone 2026-08-27 (user report — sync completed AppImage↔phone; see the 2026-08-27 entry above). (3) QR SCAN — join dialog gains "Scan QR code" on Android/iOS via NEW mobile_scanner dep (screens/qr_scan_screen.dart, QR-only, accepts only wtch1- payloads; CAMERA permission arrives via the plugin manifest). 563 Dart (+14) + 44 cargo + analyze clean; LIVE VERIFY devservers: A→B doc+map in ~5s, B expanded the NOTLD 319-byte shrunk map over mainnet in 17s to the full 1357-chunk root (/resolve 200), reverse direction, self-exclusion, tokenless 401; GUI Xvfb :78 (debug bundle + release .so, fresh HOME): app joined a devserver link, the background cycle merged with NO UI action — Continue Watching NOTLD "1 h 25 min left" from the desktop's published viewpoint, Charade card + "From my desktop · 1" drawer list, map auto-imported, publish-back seen by the devserver, Sync now → "Everything is in sync." (/tmp/wisync-1-home.png, -2-drawer, -3-mywatch, -4-syncnow). GOTCHA: debug Linux bundles lack the .so (cp the release build into bundle/lib); the app's first listening port is the Dart VM service — probe /health per port.
- [2026-08-26] MY W@TCH DEVICE LINKING — TEST IMPLEMENTATION (UNRELEASED — post-alpha.60; user asked for a test implementation of "My W@tch" linking-only with a drawer menu + last-sync info, after the x0x design discussions): link a user's own devices through an IN-PROCESS x0x AGENT (crates.io x0x 0.39, saorsa-gossip over ant-quic, post-quantum, mDNS LAN discovery + public bootstrap) inside watchit_core. NEW native src/mywatch.rs: the link is a 32-byte secret shared as invite `wtch1-<hex64>` (QR/paste); devices meet in a SELF-KEYED CRDT KV STORE (join_self_keyed_kv_store_persistent) on a topic blake3-DERIVED from the secret (raw secret never on the wire); each device owns one record (its agent-id key): name/platform/lists/entries/updated_at; merge is the CRDT's = bidirectional for free. Heartbeat 60s + 5s watcher; last_sync_ms = wall-clock when a REMOTE record last changed (persisted state.json; restored-from-snapshot first scan doesn't count — fresh-join arrivals DO). Config link.json 0600; agent identity pinned INSIDE <data>/mywatch (machine.key/agent.key/peers — two instances on one host must not share ~/.x0x); autostart on app launch resumes the link; unlink wipes the dir. Server routes (protected, x-watchit-auth): GET/DELETE /mywatch, POST /mywatch/link|join|announce, GET /mywatch/invite. DESKTOP-ONLY like Publish (x0x unproven under the Android NDK; Android builds get a stub reporting supported:false, tile hidden). DEP-GRAPH GOTCHA: ant-quic pins unicode-width =0.2.0 while ant-core→saorsa-transport pins =0.2.2 (two exact pins, unresolvable) — VENDORED native/third_party/ant-quic (published 0.27.47 tarball, ONLY that pin bumped, dangling example/test targets stripped) via [patch.crates-io]; x0x needs rustc 1.95 (in ~/.cargo/bin). Flutter: services/my_watch_api.dart + screens/my_watch_screen.dart (create link → name dialog → QR invite dialog via NEW qr_flutter dep; join = paste code; linked view: Last sync, Linked since, per-device rows w/ online dot + last-heard + lists/items, Show invite, Unlink w/ confirm; screen announces real library counts on open, 5s status poll that only rebuilds when the JSON changed — pumpAndSettle + spinner GOTCHA otherwise) + drawer "My W@tch" tile between Publish and Settings. 44 cargo + 549 Dart tests (+9, fake_embedded_http grew /mywatch routes) + analyze clean. LIVE VERIFY: two devservers linked over the invite (records both ways, presence online, announce 3/57 propagated in ~5s, last_sync stamped, restart resumes same agent-id + counts from snapshots, unlink wipes + double-link guarded, tokenless 401) AND clean-HOME Xvfb :77 GUI drove the whole flow — drawer tile, create link, QR dialog, then a devserver joined via that invite and the app showed both devices w/ "Last sync just now" + its real 1 list/2 items (/tmp/mywatch-3-drawer.png, -4-screen.png, -5-namedialog.png, -6-qr.png, -7-linked.png). SCOPE: linking + presence/counts only — NO watch-list/viewpoint sync yet (that rides the same store or an MLS group later); no QR *scanning* (phone camera) yet. RISK: Windows CI build of the x0x tree untested (windows.yml only fires on v* tags — check before the next release)
- [2026-08-26] v0.1.0-alpha.60 "TERMS OF USE" RELEASED — see Current Status; tag v0.1.0-alpha.60 on bump commit bb4fb65, all three artifacts (AppImage + Windows zip + APK), Latest confirmed. Smoke verified the new first-launch Terms gate live in the shipped AppImage (accepted → connected · 5 peers)
- [2026-08-26] TERMS OF USE & DISCLAIMER: FIRST-LAUNCH ACCEPT GATE + SETTINGS PAGE (abc1789 pushed, UNRELEASED — post-alpha.59; user asked for a devs-protecting T&C, click-to-accept on first install, and a Settings entry): NEW lib/services/terms.dart (kTermsVersion=1 + kTermsIntro + 10 TermsSection consts: client-only/no hosting/no control, user responsibility for imported/published content, prohibited use, Autonomi permanence, hot-wallet risk/non-refundable fees, TMDB/GitHub third-party note, no warranty, liability cap, indemnity, changes-reprompt) + lib/screens/terms_screen.dart (TermsScreen: gate mode when onAccept != null — PopScope canPop:false, no back arrow, bottom bar Exit (desktop exit(0), mobile SystemNavigator.pop) / "I agree" FilledButton; read-only mode is a plain pushed page). main.dart WatchItApp home is now TermsGate(child: HomeScreen()) — async-reads AppSettings.termsAcceptedVersion (new pref terms_accepted_version_v1, 0=never) and holds the UI on the terms until accepted at the current kTermsVersion, so BUMPING kTermsVersion re-prompts every user after a material change; network client keeps warming behind the gate. Settings → About gains a "Terms of Use & Disclaimer" tile (policy icon, between licenses and Size on disk) opening the read-only page. 540 Dart tests (+6: gate/accept/persist, straight-to-home when accepted, re-gate on version bump, read-only mode, settings tile nav, load-bearing-phrase unit pin) + analyze clean; every test file pumping WatchItApp gained 'terms_accepted_version_v1': kTermsVersion in its mock prefs (and terms_test/widget_test also set defaults_seeded_v4 — GOTCHA: without it the empty-state text appears for one frame then vanishes when ensureDefaults seeds NOTLD). Live Xvfb :79 verify on clean HOME (debug bundle + release .so): fresh launch gates on the terms (no back button), I agree → seeded home + connected · 5 peers, restart goes straight to home, Settings→About tile opens read-only page with back arrow and no accept bar (/tmp/terms-1-gate.png, -2-accepted-home.png, -3-restart.png, -5-settings-about.png, -6-settings-terms.png). NOTE: terms text is a dev-written disclaimer, not lawyer-reviewed
- [2026-08-26] PUBLISH QUALITY SECTION EXPLAINS ITSELF (352014b pushed, UNRELEASED — post-alpha.59; user asked why multiple versions are encoded + where best to impart it — answer: at the point of choice): the QUALITY helper text now LEADS with the why ("Devices and connections vary — smaller versions play smoothly where the full-quality file can't…" + the existing mechanics sentence), and the QUALITY header gains an info_outline IconButton (tooltip "Why multiple versions?") opening a concise 3-paragraph AlertDialog: device/bandwidth fit (full-quality stutters/fails on phones/TVs/old machines, needs fast links), Autonomi permanence (can't replace with a re-encoded copy later, so qualities must be published now), and no library clutter (same-title uploads share one card + version picker). All in _tierSection/_showWhyVersionsDialog in publish_screen.dart. 534 Dart tests (+1 widget: icon opens dialog w/ permanence + picker text, Close dismisses; GOTCHA: page intro also says "permanent" — match a dialog-specific phrase) + analyze clean; live Xvfb :78 verify on clean HOME with a real 1080p HEVC 10-bit lavfi mkv picked through the GTK dialog (GOTCHA: xdotool type races GTK location-bar autocomplete and mangles paths — type a short prefix, End to accept the completion, then Return): helper + icon render, dialog shows all 3 paragraphs, Close works (/tmp/whyq-4-tiers.png, /tmp/whyq-5-dialog.png)
- [2026-08-26] UNIVERSAL ORIGINALS GET THE RESOLUTION TAG (UNRELEASED — post-alpha.59; user request "so it matches the naming scheme of the encoded files"): a file published as-is (Original tier) that already plays everywhere (isUniversal: H.264 8-bit MP4 ≤1080p — exactly when Original stands in for the top encode tier) now gains its real resolution tag: 1080p `Movie.mp4` → `Movie [1080p].mp4` (existing tags normalized, own extension kept). Non-universal originals stay untagged ON PURPOSE — tagging them could collide with the same-resolution encode tier's `.mp4` output name. tierOutputName in publish_plan.dart is the only code change; propagates to network upload name, add-to-library entry, .datamap export, done page for free. NAMING.md Publishing section documents it. 533 Dart tests (+5 naming units incl. tag-normalize/extension-kept/non-universal-untagged; publish_flow expectations updated to the tagged names) + analyze clean; no GUI screenshot — same rationale as the 3f26a5c tag fix, the name only surfaces on the paid queue/done pages, pinned by unit+widget tests
- [2026-08-26] v0.1.0-alpha.59 "PUBLISH FIXED, POSTER CROP" RELEASED (tag on bump commit f8db301): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.59 — Latest, AppImage + Windows zip + APK. Ships a223b3a (>12MiB publish shrunk-child-map fix) + 4dea923 (poster crop/zoom step after frame pick) — see the 2026-08-25 entries below. Local watchit-build unit built APK+AppImage, Windows zip from CI run 32937149786 on the bump commit (6m5s; zip sha256 40d5d194…, ffmpeg.exe+ffprobe.exe present); CI green on bump (run 32937145102); verified APK versionCode 59; clean-HOME Xvfb :97 smoke of the exact dist AppImage (wi59-xvfb/wi59-app units): connected · 34 peers, health state=ready stored_maps=2, NOTLD seed wall (/tmp/wi59-smoke.png). Published with /tmp/wi59-release-notes.md (no --prerelease → Latest, confirmed via releases/latest; notes tell shrunk-child-map victims their file IS on the network and re-publishing on .59 finishes it free). NOTE: tag push fires windows.yml's v* trigger again (duplicate zip artifact, harmless)
- [2026-08-25] PUBLISH >12MiB FIX + POSTER CROP/ZOOM STEP (UNRELEASED — post-alpha.58; implements the shrunk-child-map fix analysed below plus the user's crop request): (1) upload.rs drive_upload no longer feeds ant-core's result map straight into verify::derive_address — new uploaded_root_map mirrors the POST /datamap import path: non-child map → derive_address as before; child map (>3 chunks ≈ >12MiB, EVERY real movie) → shrunk_map_address is the address, stored_root_map shortcut for re-publishes of known files, else expand_child_map (wrapper chunks just uploaded) + verify_root_map. Outcome.size now from the expanded ROOT (the child map's original_file_size is the serialized-root size — wrong). Any finalize error is wrapped with "the upload itself succeeded (all chunks stored and paid for)… publish the same file again to finish for free" since it fires post-payment. 44 cargo tests (+2: small-map-is-own-root, child-map-resolves-via-stored-root — Engine::new offline harness like server.rs tests). NOT live-verified end-to-end (>12MiB paid upload = real ANT); the primitives are the alpha.46 import path, live-proven. (2) NEW app/lib/widgets/poster_crop_dialog.dart PosterCropDialog, chained after FramePickerDialog in edit_details_screen._pickVideoFrame: fixed 2:3 poster-aspect selector box (kPosterAspect) over the frame with dimmed surround + thirds guides, drag to pan / pinch / scroll-wheel / slider to zoom (1–5x, pan clamped so the box stays covered), "Use selection" pops a PNG of the crop at natural resolution (never upscales; PNG bytes land in the .jpg-named user poster file — Flutter decodes by content, harmless), "Use whole frame" keeps the old uncropped behaviour, Cancel aborts. Pure crop math exposed as top-level coverBaseScale/clampPan/cropSourceRect. 529 Dart tests (+8: math units incl. clamp-never-escapes-image, dialog widget tests — image codec + Picture.toImage need tester.runAsync delays) + analyze clean; live Xvfb :89 GUI drive via a throwaway -t entrypoint (deleted): dialog renders, drag+slider-zoom move/scale the image under the box, Use selection closes clean (/tmp/crop-1-default.png, /tmp/crop-2-zoomed-panned.png). GOTCHA: ~/.cargo/bin must be PREPENDED to PATH — /usr/bin/cargo is 1.75 and fails on the v4 Cargo.lock
- [2026-08-25] PUBLISH "shrunk child map" ERROR ANALYSED (user report, analysis only — FIXED same day, entry above): publishing any mp4 over ~12 MiB fails with "map is a shrunk child map, not a root map". NOT caused by the file having been uploaded before (dedup doesn't change the map shape) — it's file SIZE. Root cause: ant-core's file_upload_with_progress builds its data_map via self_encryption stream_encrypt(...).into_datamap(), which for >3-chunk files (>3×~4MiB) returns the SHRUNK child map, but upload.rs:193 feeds it to verify::derive_address which hard-rejects child maps (verify.rs:22). Every pre-release Publish test used small files (≤3 chunks → map is its own root), so it never surfaced; ALL real-movie publishes fail. IMPORTANT: the error fires AFTER payment+storage complete — the file IS fully on the network (shrink wrapper chunks included; stream_encrypt yields them into the upload stream); only the local finalize (derive address + store root map) fails, and a retry re-uploads for free (already-stored chunks cost nothing). PROPOSED FIX (awaiting greenlight): in upload.rs drive_upload, mirror the POST /datamap import path (server.rs:200-233) — if result.data_map.is_child(): addr = verify::shrunk_map_address(&map), root = engine.expand_child_map(map).await (wrapper chunks just uploaded so fetchable), verify_root_map, store; else current derive_address path. Outcome fields (size/chunks) should then come from the expanded root
- [2026-08-25] EPISODE ARTWORK NO LONGER LEAKS ONTO SEASON SURFACES (7e4b527 pushed, UNRELEASED — post-alpha.57; closes the alpha.57-scopes KNOWN RESIDUAL): for TV rows posterFile is the SEASON-art slot (season tiles/headers read it via the first episode), but an episode-scope artwork edit stored the user's file there — episode-1 art showed on the season tile/page/wall card. Fix: MediaMetadata grows episodePosterFilePath; MetadataService._fromCache re-slots a userEdited episode row's `user_` posterFile into it and RECOVERS the season slot from the row's TMDB id (`tv_<id>_s<N>.jpg` on disk, shared per season — covers remove-artwork's posterFile=null too, which previously would have blanked the tile). Entry-level surfaces read new entryPosterImage (episode art beats season slot): episode DetailScreen header, per-entry poster cards, search episode-tile fallback; season/show surfaces keep posterImage and never see episode art. Editor previews rescoped: episode editor shows ONLY the episode's own artwork (placeholder when none — the slot its save replaces), season editor shows the true season slot. 521 Dart tests (+4: leak matrix incl. sibling isolation + recovery-after-remove + season-user-art-beats-recovered + movie-slot pin) + analyze clean. LIVE-VERIFIED on Xvfb :77 clean HOME (debug bundle + release .so, seeded 2-episode show w/ blue TMDB season art): red image picked into episode 1 via the editor → episode page red, season page header + show-page season tile + home wall card ALL stay blue (pre-fix they went red), season editor previews blue, survives restart, Remove artwork reverts the episode page to blue with the user_ file deleted and poster_file=null in sqlite (/tmp/wiart-*.png, esp. -8-epdetail-red, -10-showpage-after, -12-seasoneditor, -16-removed)
- [2026-08-25] EDIT DETAILS: SHOW/SEASON/EPISODE SCOPES (implements the scoping-bug fix analysed below, UNRELEASED — post-alpha.57): ParsedName grows showLookupKey (`tv:title:year`, == lookupKey for non-episodes) + seasonLookupKey (`…:sN`) — keys NO entry resolves under, so they're free for user rows. MetadataService composes: metadataFor(episode) overlays a userEdited show row (title/year/overview→showOverview/posterFile→show poster) and season row (overview→seasonOverview/posterFile→season-art slot) onto every episode via new cache-only `_overlayFor` (never TMDB-fetched; cleared in reset/notifyExternalSeed) — show pages/cards/season tiles keep reading first-episode metadata and now see user edits regardless of which episode is first. ShowScreen + SeasonScreen gain the app-bar pencil (scope show/season); EditDetailsScreen takes `scope` (entry|show|season): show scope = title/year/description/artwork under the show key, season scope = description/artwork only under the season key, and an EPISODE entry now edits "Episode name (optional) — SxxEyy" (saved as `SxxEyy · Name` via new saveUserDetails episodeLabel Value param, absent=preserve) + episode synopsis + artwork — the row's title keeps the stored show title instead of being overwritten (the original bug: episode edits rewrote the series master). Bundle export adds show/season keys to the metadata.json key set (import already accepts any key); player camera button unaffected. 517 tests (+9: key units, overlay matrix incl. sibling isolation, export keys, 3 scope widget flows, pencil pins) + analyze clean. LIVE-VERIFIED on Xvfb :97 clean HOME (debug bundle + release .so, catalog bundle imported through the UI): show pencil → renamed One Step Beyond → title updates on show page/season page/episode page/wall card while TMDB rating/genres/synopsis survive; season editor writes season synopsis (beats showOverview on the season page); episode editor prefills "The Bride Possessed" (episode name, NOT the show title) and a rename lands as `S01E01 · The Bride Renamed` with the show title untouched; sqlite shows exactly 3 userEdited rows under episode/show/season keys; all survive app restart (/tmp/wi-tvedit-*.png, esp. -9-showeditor, -12-seasoneditor, -15-epeditor, -16-eprenamed, -18-restart-show). KNOWN RESIDUAL (FIXED 2026-08-25, entry above): episode-1 user artwork leaked onto its season tile
- [2026-08-25] EDIT-DETAILS TV SCOPING BUG ANALYSED (user report, analysis only — NO code changes): pencil is missing from ShowScreen and SeasonScreen entirely (only DetailScreen has one, detail_screen.dart:512), so shows and seasons cannot be edited; AND the episode editor misleads — it prefills Title with meta.title which for episodes is the SHOW title (episode name lives in read-only episodeLabel), so editing an episode looks like editing the series. Root cause: there IS no show/season-level metadata row — every row is keyed per-episode (`tv:title:year:sN:eM`), and show card (poster_cards.dart:111), ShowScreen header (show_screen.dart:55), season tiles (:178) and SeasonScreen header (season_screen.dart:59) all read metadataFor(FIRST episode in library order), so a userEdited row on episode 1 leaks into show/season/card surfaces (title + posterFile=season-art slot) while the same edit on any other episode changes nothing outside its own page. PROPOSED FIX (3 parts, awaiting greenlight): (1) show-level user rows under the episode-less key `tv:title:year` + a compose step in MetadataService that overlays a userEdited show row's title/year/showOverview/show-poster onto every episode's metadata; (2) pencil on ShowScreen (show scope: title/year/description/artwork → show key) and SeasonScreen (season scope `tv:title:year:sN`: season artwork/overview); (3) rescope the episode editor to episode fields (episode name → episodeLabel after the SxxEyy marker, description → overview, artwork stays per-episode posterFile) and stop show-title prefill. Bundle export/import of the new keys works unchanged (metadata.json carries whole rows)
- [2026-08-25] DOCS REFRESH THROUGH ALPHA.57 (7f2b9db pushed, docs-only, no code/tests): all user-facing docs reviewed and caught up — they still described a CLI-only upload world and stopped at alpha.50. README: How-it-works step 1 now leads with the in-app Publish flow (ant CLI kept as the alternative), step 3 mentions Edit details + Publish auto-naming, intro + Status say "Android, Linux, and Windows" and cover alpha.51–.57 (Windows zip, Publish/wallet, tiers/ffmpeg, update check, Edit details). VISION: goal 3 reworded, NEW goal 7 "Publish from the app" (8/9 renumbered), problem statement notes "or uploader". ARCHITECTURE: NEW "Publishing — in-app uploads" section (wallet.rs keychain/BIP-39, upload.rs single-slot job, x-watchit-auth routes, tiers + bundled ffmpeg, NAMING output tags), library-entry paths "two ways"→"three ways" (publish added), Windows packaging row = shipped portable zip, diagram + repo-layout comment updated. ROADMAP: status header rewritten (2026-08-25, alpha.57, per-release summary .51–.57), NEW shipped section "Publish — in-app uploads" with open boxes (mobile Publish, external signer, true self-update), Phase 3 Windows box ticked. UI-DESIGN: Settings gains Publishing + About update-check lines, NEW "### 7. Publish (desktop)" screen, Built-so-far renamed alpha.39→.57 and extended. NAMING: NEW "Publishing from the app" section (real-output-height tags, .mp4, SxxEyy preserved → version-picker fold). BUNDLE-FORMAT: metadata.json row documents the alpha.57 userEdited flag semantics. SEED-CATALOG/BRAND/catalog-README/PLAN-* checked, accurate as-is (historical/spec)
- [2026-08-25] v0.1.0-alpha.57 "EDIT DETAILS" RELEASED (Latest: AppImage + Windows zip + APK; tag v0.1.0-alpha.57 on adcced6, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.57 — ships the Edit details feature (entry below). Local watchit-build APK+AppImage (ALL BUILDS DONE 19:34), Windows zip from CI run 32890130676 on the bump commit; verified versionCode 57/cert 79:11:66:99, zip complete (ffmpeg.exe+ffprobe.exe+av* DLLs, watchit_core.dll 27.4MB); clean-HOME Xvfb :97 smoke of the exact dist AppImage — connected · 5 peers, health ready stored_maps=2, and the shipped binary's detail-page pencil opens the full Edit details editor (From image file / From video frame / Remove artwork, title/year/description/Save) — /tmp/wi57-smoke{,-detail,-editor}.png. Release notes /tmp/wi57-release-notes.md (Latest; frame-picker desktop-only, player camera button all platforms).
- [2026-08-25] EDIT DETAILS: USER METADATA + ARTWORK FROM FILE OR VIDEO FRAME (5c92318, RELEASED in alpha.57 — entry above): detail pages grow an app-bar pencil → EditDetailsScreen (new app/lib/screens/edit_details_screen.dart): title/year/description written into the metadata cache under the entry's lookup key flagged userEdited (drift schema v9) — a found row short-circuits the TMDB fetch so user rows are NEVER overwritten by a later match; edits on top of a TMDB match keep its extras (rating/genres/stills); "Remove my edits" deletes the row so TMDB can re-match. Artwork: from image file (file_selector, 10MB cap), from video (frame-picker dialog samples 12 frames via bundled ffmpeg — new FfmpegService.extractFrame, keyframe fast-seek JPEG pipe, no upscaling, works on downloaded file OR the embedded client's stream URL; tap → re-grab at poster resolution; desktop-only), or the player's new camera button "use this frame" (mpv screenshot of the paused frame; all platforms). User posters are user_<key>_<hash>_<ts>.jpg (fresh name per save — Flutter image cache keys by path; old user files deleted, TMDB artwork untouched). Bundle exports carry userEdited in metadata.json, imports gap-fill it → .watch-list recipients see the same details offline. 508 tests (+14) + analyze clean; live-verified on Xvfb vs mainnet (NOTLD edit round trip incl. restart, image pick, frame picker on the real network stream).
- [2026-08-25] PUBLISH TAG = REAL OUTPUT RESOLUTION (3f26a5c pushed, UNRELEASED — post-alpha.56): sub-480p sources offered the fallback Low tier were still named `[480p]` (and the progress line said "Encoding 480p") though the encoder never upscales. NEW publish_plan.dart tierOutputHeight/tierLabel (tier cap, or source height snapped down to even exactly like encodeArgs' odd-dim snap; null/nominal-label fallback for Original and unprobed sources); tierOutputName now takes the probe (optional 3rd param — old call sites/tests compile) so a 360p source at Low → `Name [360p].mp4`, and buildQueue passes source.probe; publish_screen's "Encoding …" label uses tierLabel too. Global QUALITY checkbox still says "Low · 480p H.264" on purpose (it's the tier cap across many files). 494 Dart tests (+7: naming matrix incl. odd-height snap + unprobed fallback, tierOutputHeight/tierLabel units) + analyze clean; no GUI screenshot — the tag only surfaces on the paid queue/done pages, pinned by unit+widget tests instead
- [2026-08-25] v0.1.0-alpha.56 "PUBLISH, IN BULK" RELEASED (Latest: AppImage + Windows zip + APK — all three platforms back on one version; tag v0.1.0-alpha.56 on 38e2a2d, the version-bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.56 — ships the multi-file Publish + tiers + ffmpeg + update-check entry below unchanged; build/verify details in Current Status. Release notes (/tmp/wi56-release-notes.md) note the APK is functionally alpha.54 (Publish is desktop-only) + the standing SmartScreen and hot-wallet warnings. Release was finished across two tasks: the first bumped/pushed 38e2a2d and detached the builds but its reply was lost; this task verified all three artifacts, smoke-tested, tagged and published
- [2026-08-25] PUBLISH MULTI-FILE + QUALITY TIERS + BUNDLED FFMPEG + UPDATE CHECK (c235d68, RELEASED in alpha.56 — entry above; user addition this task: multi-file selection "so we can accommodate full series"): Publish screen REWRITTEN for batches — openFiles multi-pick + "Add more"/remove per file, per-file ffprobe verdict line ("1080p H.264 10-bit — many devices can't play this" / "480p H.264 — plays everywhere"), global QUALITY checkboxes (High 1080p/5Mbps · Medium 720p/2.5Mbps · Low 480p/1Mbps — H.264 High 8-bit + AAC stereo faststart MP4 — plus "Original files (as-is)") with per-file applicability (no upscaling; Original replaces the top tier when the source is already universal ≤1080p H.264/AAC mp4, default-unticked for non-universal sources), per-tier "applies to N of M · ≈size · ≈ANT" and a summed "N uploads · estimated cost ≈X ANT" scaled per-chunk (4MiB, 3-chunk floor) from ONE live /upload/estimate on the first file; sequential encode→upload queue (encode to a systemTemp dir with -progress %, upload via existing job polling; per-item retry/skip/stop on error, temp reused on retry, cleanup on finish/dispose) then batch done page (per-title address+copy, add-ALL-to-library via the list picker, save-ALL .datamaps via getDirectoryPath, totals incl. skipped/failed). NEW lib/services/publish_plan.dart (pure logic: tiers, offered/defaults, verdicts, [720p]-tag output naming per NAMING.md — resolution tags replaced, extension → .mp4, episodes keep SxxEyy so tiers fold into the alpha.49 version picker), lib/services/ffmpeg.dart (FfmpegService: binaries beside Platform.resolvedExecutable first — AppImage/win zip bundle them there — then PATH; ffprobe JSON parse with attached_pic cover-art guard; encode via encodeArgs incl. scale=-2:H only when downscaling + odd-dim snap, FfmpegException carries stderr tail; graceful degrade to as-is-only publishing with UI banner when ffmpeg missing), lib/services/update_check.dart (desktop-only ≤once/24h GET releases/latest, isNewerTag semver+alpha compare — stable beats alpha, silent failures which do NOT stamp the 24h so offline retries next launch; toggle pref update_check_enabled_v1 default ON) + Settings→About "Check for updates on startup" SwitchListTile + "Update available" row (url_launcher NEW dep) + main.dart startup snackbar with View action. Bundling: build_appimage.sh installs johnvansickle 7.0.2 STATIC ffmpeg+ffprobe into AppDir/usr/bin (fully static so NO soname/glibc interplay with the bundled libmpv libav* — a shared build there could collide; tarball cached ~/tools/ffmpeg-7.0.2-amd64-static.tar.xz, sha256 abda8d77… pinned, mismatch=fail loud; AppImage will grow ~+80MB); windows.yml bundles BtbN win64-gpl-SHARED (autobuild-2026-08-25-13-06, sha256 53c28bed… pinned) — ffmpeg.exe/ffprobe.exe + shared avcodec DLLs beside watchit.exe (~half the size of two static exes; no clash — libmpv-2.dll is self-contained; zip ~+65MB). Tests 487 (+48): publish_plan matrix, parser units, ffmpeg_live e2e encode (skips when no ffmpeg; CI-safe), multi-file/tier/skip/uncovered-warning widget flows (fake FfmpegService + multi-result fake server; GOTCHA: setup page overflows the 800px test viewport — tests set tester.view.physicalSize 900x2400, and the screen uses createTempSync/writeAsBytesSync because async dart:io hangs the fake-async zone), update-check gating, About toggle. LIVE-VERIFIED on Xvfb :77 clean HOME (debug bundle + release .so, PATH ffmpeg): imported a throwaway hardhat key (0xf39Fd6e5… derived correctly), picked 2 lavfi test episodes (1080p 10-bit mkv + 480p universal mp4) via GTK dialog + Add more, correct verdicts/tiers/"applies to N of 2"/5-uploads≈0.688581-ANT live-quote summary, rights tick enabled "Publish 5 uploads" (NOT clicked — real ANT), About shows the update toggle ON (/tmp/wi56-3-publish.png, -10-file1.png, -12-twofiles.png, -14-publish-enabled.png, -15-about.png). NEXT: version bump → release (AppImage + windows.yml zip now both carry ffmpeg)
- [2026-08-25] PUBLISH PAID UPLOAD CONFIRMED ON MAINNET (user test, no code changes): user ran the released alpha.55 AppImage and uploaded a small MP4 to the network successfully with a funded wallet — the paid-upload leg (the ONLY path never exercised pre-release, ant-core file_upload with real ANT) now verified end-to-end. alpha.55 fully verified: wallet ceremony, estimate, pay, upload all green on real mainnet. NEXT: alpha.56 (encode tiers + bundled ffmpeg + update check-and-notify — PLAN-alpha55.md sections 4+6 are the spec)
- [2026-08-25] v0.1.0-alpha.55 "PUBLISH" RELEASED (Latest: AppImage + Windows zip, first desktop-only release — no APK; tag v0.1.0-alpha.55 on 88bf042, the version-bump commit): see Current Status for build/smoke details. Ships the Publish feature below unchanged; release notes (/tmp/wi55-release-notes.md) carry the hot-wallet warning, SmartScreen note, and the alpha.56 tier/ffmpeg/update-check preview. USER: tested same day — paid upload confirmed working (entry above)
- [2026-08-25] PUBLISH: INTERNAL WALLET + PLAIN UPLOAD (f6f217a + plan rev 5 253a347 pushed, UNRELEASED — alpha.55 headline; user decisions: name "Publish", edition split — tiers/ffmpeg/update-check → alpha.56, no Windows installer, tag trigger now): Rust watchit_core grows wallet.rs (private key in OS keychain via keyring 3 — windows-native/apple-native/async-secret-service with **async-io NOT tokio zbus** + every keyring call on a dedicated plain thread, else zbus block_on PANICS on a tokio worker and killed the connect supervisor, caught live; mode-0600 wallet.key file fallback when no Secret Service, backend surfaced to UI; BIP-39 12-word generate/import at m/44'/60'/0'/0/0 via alloy-signer-local 1.8.3 mnemonic feature + coins-bip39 — MetaMask-compatible, hardhat vector pinned in tests; mnemonic NEVER stored) and upload.rs (single-slot job via ant-core file_upload_with_progress PaymentMode::Auto, UploadEvent channel → pollable phase/done/total, result root map stored in mapstore under derive_address — playable/exportable immediately; evmlib auto-approves allowance so no separate approve leg). Engine attaches wallet at connect (with_wallet is builder-only → wallet changes evict+kick the supervisor). NEW server routes GET/POST/DELETE /wallet, POST /wallet/generate, GET /wallet/balances (Arbitrum One RPC), POST /upload/estimate, POST /upload, GET /upload/{id} — ALL behind x-watchit-auth shared secret (random per start, Dart reads it via new FFI watchit_core_auth_token; localhost port is visible to every local process and these routes SPEND MONEY; tokenless curl → 401 verified; WATCHIT_AUTH_TOKEN env override for dev curl). Flutter: Settings → PUBLISHING → Wallet (create = full-screen 12-word ceremony, confirm 3 retyped words BEFORE anything persists, then import; import dialog auto-detects key vs phrase; address + live ANT/ETH balances + funding help; file-fallback warning; remove w/ confirm) + drawer "Publish" tile between Media and Settings (desktop-only via isDesktopPlatform in publish_screen.dart): pick file (file_selector) → live estimate (formatUnits 18-dec) → rights/permanence checkbox gate → publish → 1s-poll progress (encrypting/quoting/paying/storing) → done: address, add-to-library (list checkbox picker), save-.datamap (GET /datamap). NEW lib/services/publish_api.dart; fake_embedded_http.dart grew wallet/upload routes. 42 cargo (+13) + 439 Dart (+14) + analyze clean; live Xvfb verify (debug bundle + release .so, clean HOME): seed ceremony end-to-end (real words → typed confirm → wallet.key fallback with UI callout, address match), restart re-attach ("upload wallet attached" at connect), 2MB estimate vs mainnet 0.219036 ANT / 3 chunks / 7 quotes (/tmp/wi55-5-seedwords.png, -8-wallet-created.png, -13-estimate-done.png, -14-gate-open.png). NO paid upload exercised (real ANT — needs a funded wallet; the pay path is ant-core's own file_upload). GOTCHA: widget tests must use XFile.fromData(bytes, path:) — a path-backed XFile's length() does real IO that hangs the fake-async zone
- [2026-08-25] WINDOWS CONFIRMED WORKING + PLAN REV 4 (docs-only): user tested the fresh Windows zip on a real Windows box — WORKS → alpha.55's Windows release leg UNGATED, Windows zip joins this and future desktop releases (Decisions #6 in docs/PLAN-alpha55.md). Plan rev 4 adds section 6 (distribution): release mechanics (workflow_dispatch on bump commit + proposed `push: tags: v*` trigger on windows.yml; standing SmartScreen note in release notes — binaries unsigned, More info → Run anyway); RECOMMENDED no Windows installer during alpha (portable zip = AppImage analogue; unsigned installer trips SmartScreen identically — real fix is a code-signing cert ~$100/yr or Azure Trusted Signing ~$10/mo, revisit at beta/1.0 with Inno Setup per-user + .datamap/.watch-list file associations); auto-upgrade RECOMMENDED as check-and-notify only (once/24h GET github releases/latest, compare tag, snackbar + Settings→About badge, mandatory Settings toggle — app's only phone-home; default ON), true self-update DEFERRED (AppImageUpdate/zsync on Linux; Windows exe can't self-replace, needs helper/installer). New open questions #3 installer #4 update-check-in-55; both await user
- [2026-08-25] FRESH WINDOWS ZIP REBUILT FOR USER TEST (workflow run 32845632600 on main fafbdfd, green in 19m8s; no code changes this task): rebuilt from current HEAD so the zip now carries the alpha.54 codebase (version 54, cursor-hide — the Aug-11 zip predated both). Artifact verified complete (watchit.exe, watchit_core.dll 21.6MB, flutter_windows.dll, libmpv-2.dll, sqlite3 + plugin DLLs, data/flutter_assets, 44 files) and REPLACED ~/projects/Watch-It-windows-x64.zip (44.8MB, sha256 203053b8…, old 782141b6… gone). CONFIRMED WORKING by user on real Windows later the same day (entry above) — gate cleared
- [2026-08-12] v0.1.0-alpha.54 RELEASED (APK + AppImage, Latest, tag on ce5788e — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.54 — ships the player cursor-hide + Linux screen-lock inhibit (cdf7e15, entry below). Both artifacts functionally desktop-only changes (APK identical to alpha.53 aside from version bump — cursor-hide lives in the DESKTOP control themes only, release notes say so). Verified versionCode 54 / W@tch / cert 79:11:66:99, AppImage build 54 + 7 fallback audio libs; clean-HOME Xvfb smoke green (/tmp/wi54-smoke.png — connected · 5 peers, ready, stored_maps=2) + ScreenSaver D-Bus strings present in shipped libapp.so; CI green on the bump commit
- [2026-08-12] PLAYER: CURSOR HIDES WITH FADED CONTROLS + LINUX SCREEN-LOCK INHIBIT (cdf7e15, SHIPPED in alpha.54): desktop video controls set hideMouseOnControlsRemoval (vendored media_kit_video supported it, off by default) via new desktopControlsTheme() helper in player_screen.dart (normal + fullscreen themes). NEW lib/services/screen_wake.dart: while playback runs, holds an org.freedesktop.ScreenSaver Inhibit over D-Bus on Linux (released on pause/player close; serialized queue so fast pause/play can't leak a cookie; missing service swallowed) — needed because media_kit's built-in wakelock (Video widget, wakelock_plus, covers Android FLAG_KEEP_SCREEN_ON / Windows SetThreadExecutionState / macOS IOKit) only tries the xdg-desktop-portal Inhibit on Linux and silently swallows failures; the ScreenSaver iface is served directly by GNOME Shell/KDE/Cinnamon/MATE/XFCE. dbus promoted to a direct dep (^0.7.11, was transitive). PlayerScreen wires _player.stream.playing → ScreenWake.instance.setPlaying + setPlaying(false) in dispose. 425 Dart tests (+5) + analyze clean; LIVE-VERIFIED on Xvfb :98 with a private session bus + fake python3-dbus org.freedesktop.ScreenSaver service (simulates a portal-less session): NOTLD 480p streamed live (62MB fetched), log shows "Inhibit app=W@tch reason=Playing media cookie=100" the moment Play was pressed and "UnInhibit cookie=100" on player close (/tmp/wisw-3-playing.png). Cursor hiding pinned by test (Xvfb screenshots can't show cursors)
- [2026-08-11] v0.1.0-alpha.53 RELEASED (APK + AppImage, Latest, tag on b1bf83e — version bump commit): https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.53 — ships the Linux audio fix 16eefff (AppImage system-first audio libs with bundled fallback: all 7 audio client libs — libpulse/libpulsecommon/libjack/libpipewire/libasound/libsndio/libopenal — parked in usr/lib/fallback off the loader path, AppRun probes host via ldconfig cache + common lib dirs and symlinks only the MISSING ones into a runtime LD_LIBRARY_PATH dir; fixes AerynOS won't-start on missing libjack AND Pop!_OS silent audio from bundled-Pulse-client mismatch; build_appimage.sh packs with ~/tools/appimagetool-x86_64.AppImage since a 2nd linuxdeploy pass would re-bundle the parked libs). Verified: no-audio-libs ubuntu:24.04 container (alpha.52 dies at load, alpha.53 starts+connects), final dist artifact smoke on Xvfb :98 clean HOME (connected · 5 peers, health ready, /tmp/wi53-final-smoke.png) with /proc/PID/maps proving all audio libs loaded from host /usr/lib; APK versionCode 53 / cert 79:11:66:99 (functionally identical to alpha.52 aside from version bump). NOTE: app's localhost health port is DYNAMIC — find via `ss -tlnp | grep pid=$PID`, not a fixed port
- [2026-08-11] WINDOWS BUILD CI GREEN — ARTIFACT READY FOR USER TEST (d8aaad1 pushed; workflow run 31516584297 succeeded in 17m53s, UNRELEASED — untested on real Windows yet): first Windows desktop build. Commit adds the flutter-created windows/ runner (window title + version-resource product name "W@tch", multi-size app_icon.ico from the branding bucket), embedded_client.dart loading watchit_core.dll on Windows (exe-dir fallback), and manual workflow .github/workflows/windows.yml (workflow_dispatch: windows-latest, NASM for aws-lc/vendored OpenSSL, cargo build cdylib + flutter build windows, uploads zip artifact). Artifact Watch-It-windows-x64.zip (44.8MB, sha256 782141b6…) contains watchit.exe + watchit_core.dll (21.6MB Rust core) + flutter_windows.dll + libmpv-2.dll (media_kit playback) + sqlite3/plugins/data/flutter_assets — a complete ready-to-run bundle. Copied to ~/projects/Watch-It-windows-x64.zip for the user to scp to a Windows box (`scp ella:projects/Watch-It-windows-x64.zip .`), unzip, run watchit.exe. NEXT: user tests on Windows; if good, decide whether Windows zip joins future releases
- [2026-08-11] README HOME SCREENSHOT REGENERATED FROM THE CLEANED CATALOG (ef541e8 pushed, docs-only, no code/tests): old home-linux.jpg showed the pulled titles (Lady Vanishes, Hunchback 1939, Petticoat Junction "21 ep") — retaken at 1280x800 on Xvfb :98 with the alpha.52 AppImage on a clean HOME (transient units wirm-xvfb/wirm-app), importing the current 40-title catalog/Public Domain.watch-list through the UI (drawer → Media → Add to library, GTK picker via xdotool Ctrl+L, Merge on the seeded-Movies clash → Movies 9 + TV Shows 32 = 41 datamaps, "39 metadata entries, 44 posters" from the bundle): connected · 5 peers, PJ card now "Season 1 · 15 ep", OSB "2 seasons · 17 ep", no pulled titles anywhere (/tmp/wirm-8-home-clean.png); also fixed the stale "47 public-domain" count → 40 in README's catalog link paragraph. detail-linux.jpg + season-linux.jpg untouched (NOTLD and One Step Beyond both retained — no removed media shown). Raw round-trip sha256-identical (ee93bdd9…). Support-request draft's "current HEAD" line updated 000c3c4 → ef541e8 (~/projects/watchit-github-support-request.md, still unsubmitted)
- [2026-08-11] ALPHA.48–.50 RELEASE BINARIES DELETED FROM GITHUB (closes residual-exposure item 2 of the history rewrite, per user "For 2 delete those assets"; GitHub-only change, no commits): all 6 assets (APK + AppImage of v0.1.0-alpha.48/.49/.50) deleted via `gh release delete-asset` — these were the only releases whose binaries bundled the pulled titles (verified: alpha.47's rootmap_seeder.dart bundles ONLY kDefaultMovieAddress/NOTLD and 1 rootmap asset, the full 49-rootmap seed catalog first appears in the alpha.48 tag; alpha.51+ ship the trimmed NOTLD-only seed). Releases/tags themselves kept; a withdrawal note appended to each release body ("binaries withdrawn — bundled seed catalog included titles later removed after a copyright review, use v0.1.0-alpha.51+"); alpha.51/.52 assets untouched (2 each, verified post-delete). Remaining residual items: (1) old commit SHAs fetchable until GitHub server-side GC — only a GitHub Support sensitive-data-removal request purges them (user undecided); (3) user clearing local copies manually (in-app library, ~/Public domain/, ~/projects/Public Domain.watch-list); (4) /tmp/watchit-backup-pre-rewrite.git pre-rewrite mirror still present (44M, ella-owned, auto-gone on reboot) + any other clone/CI checkout of the repo still holds pre-rewrite history until it re-clones
- [2026-08-11] HUNCHBACK + PETTICOAT JUNCTION E16–21 PULLED, FULL GIT HISTORY REWRITE + FORCE-PUSH (completes the 2026-08-11 legal review; NOTLD kept — provenance confirmed good by user): (1) catalog pull (commit now 1411f2d post-rewrite): Hunchback of Notre Dame 1939 (single-CCE-search PD basis, unverifiable) + Petticoat Junction S01E16–E21 (1964 airdates → 1992 Renewal Act auto-renewal defeats the 1963 non-renewal basis; E01–E15 stand) removed from "catalog/Public Domain.watch-list" — 7 datamaps, 7 list.txt lines, 7 metadata entries, poster movie_31685.jpg + stills tv_3346_s1e16..21; 87 surviving members byte-identical; bundle now 40 titles / 41 datamaps; both READMEs recounted (8 films, PJ 1–15) with removal notes; ~/Public domain/README.md PD bases struck through with "Do NOT re-add". (2) HISTORY REWRITE via git-filter-repo v2.47 (script at /tmp/git-filter-repo; pip broken — ~/.local/bin/pip3 shim points at missing python3.14): stripped the 2 dirty catalog blobs + pre-trim seed_metadata/metadata.json blob, purged the 8 pulled rootmap assets (incl. Lady Vanishes dda9e302…) + movie_940.jpg + movie_31685.jpg + 6 PJ stills from ALL history, redacted the 8 pulled XOR addresses from every text blob (only seed_catalog.dart history had them); HEAD tree hash byte-identical pre/post (89dafab5); all 50 tags rewritten + force-pushed, releases stayed attached, CI green. VERIFIED: no pulled address/path in any remaining blob/commit; raw.githubusercontent bundle sha256-identical (893e517a…). RESIDUAL EXPOSURE (user to decide): old SHAs still fetchable on GitHub until their GC (support ticket = only full purge); alpha.48–.50 RELEASE BINARIES still bundle all 49 rootmaps incl. pulled titles (deleting those assets would close it); user's in-app library + ~/Public domain/ + ~/projects/Public Domain.watch-list still hold the titles locally — a re-export would re-add them. Local pre-rewrite mirror backup: /tmp/watchit-backup-pre-rewrite.git (gone on reboot)
- [2026-08-11] THE LADY VANISHES PULLED FROM THE PUBLISHED CATALOG (a32dae7 pushed, asset+docs only, no code/tests — implements item 1 of the 2026-08-11 legal review, per user 'Yes do it'): its datamap, list.txt line, metadata.json entry and poster (movie_940.jpg) removed from "catalog/Public Domain.watch-list" — the non-renewal PD basis is obsolete (URAA restored the US copyright of the UK work effective 1996; Carlton NIE effective 1998-01-30; Criterion licenses it; US copyright through 2033). Edited in place with `zip -d --no-wild` (GOTCHA: zip -d treats `[1080p]`/`{...}` as glob patterns — --no-wild required) + zip-update of list.txt/metadata.json; verified all other 48 datamaps + 52 posters byte-identical, round-trip counts 47 titles / Movies(10 incl. 2×NOTLD) / TV(38) / 47 metadata entries, raw.githubusercontent sha256-identical post-push. Both READMEs recounted (47 titles, 48 datamaps, 9 films) + removal notes for earlier importers; obsolete PD basis struck through in ~/Public domain/README.md ("Do NOT re-add to exports"). STILL OPEN from the review: Hunchback 1939 re-verify, Petticoat Junction S01E16–21 auto-renewal problem, NOTLD 1080p provenance; user's own app library still holds the title — a future re-export would re-add it unless deleted there first
- [2026-08-10] ANT-CORE 0.5.1 BUMP + HEART MOVED BESIDE DOWNLOAD (e2a6ea6 + 56f0b68 pushed, UNRELEASED — ready for alpha.52): watchit_core's ant-core pin bumped 629b87f (0.4.0, 2026-07-17) → 81a0a24 (0.5.1, rc-2026.7.4 — typed NotFound errors for absent records, single-node payment/quote fixes, merkle payment parity, saorsa-core 0.26.4); drop-in, self_encryption stays 0.36 / xor_name 5, no Rust source changes. Favourites heart moved OUT of DetailScreen's app bar into the action Wrap immediately right of the Download button (same per-address toggle/tooltips; new widget test pins placement — actions Wrap directly after Download, findsNothing in AppBar). 29 cargo + 420 Dart tests (+1) + analyze clean. Live-verified on clean-HOME Xvfb :98 with the REBUILT .so (GOTCHA x2: `flutter build linux` does NOT put libwatchit_core.so in the bundle — build_appimage.sh does that, cp it into bundle/lib/ for smokes; and `cargo test --release` does NOT relink the cdylib — `cargo build --release` first): /health state=ready peers=5 stored_maps=2 on ant-core 0.5.1, live NOTLD 480p range stream HTTP 206 ftyp isom 1MB/5.9s, heart right of Download (/tmp/wi52-detail-heart.png), fills blue (-detail-hearted.png), Favourites row on home (-home-favrow.png)
- [2026-08-10] README SCREENSHOTS TWO-UP (3438eda pushed, docs-only): user-supplied One Step Beyond Season 1 page shot (season poster, 5.7/10, Download season button, episode grid with stills/dates/descriptions, 1496x1838 JPEG 347K) committed as docs/screenshots/season-linux.jpg; README's second screenshot block became an HTML `<p>` two-up — detail-linux.jpg + season-linux.jpg side by side at width="49%" each (GitHub markdown can't size images, hence raw img tags) — with a combined Left/Right caption; home-linux.jpg untouched, still full width. Verified live: raw season-linux.jpg sha256-identical, width="49%" markup on raw README
- [2026-08-10] README DETAIL-PAGE SCREENSHOT ADDED (7e559ed pushed, docs-only): user-supplied NOTLD detail-page shot (poster, 7.6/10 rating, 5.29GB version dropdown, Play/Download, filled favourites heart — the alpha.49 picker + alpha.51 heart in one image, 1502x1850 progressive JPEG 143K) committed as docs/screenshots/detail-linux.jpg; README shows it as a SECOND screenshot below the "Want this exact library?" paragraph with caption covering version picker/stream/download/favourite; home-linux.jpg untouched — verified live via raw.githubusercontent.com (sha256-identical)
- [2026-08-10] PUBLIC-DOMAIN .WATCH-LIST PUBLISHED (8439b19 pushed, docs+asset only, no code/tests): user's 1.8MB export of the full alpha.48–.50 seed catalog committed as "catalog/Public Domain.watch-list" (spec-v2 bundle: 49 datamaps incl. both NOTLD versions, list.txt Movies(11)+TV Shows(38) — 21 Petticoat Junction + 17 One Step Beyond, library.json, metadata.json + 53 posters → imports display fully offline, no TMDB key) with NEW catalog/README.md: download link (github.com/…/raw/main/catalog/Public%20Domain.watch-list), contents table, import steps (☰ → Media → ＋ Add to library; merge dedupes by address so seeded installs don't duplicate; Android .bin rename harmless — importer sniffs content), US-PD caveat. Main README links it in two places: under the hero screenshot ("Want this exact library?" — it IS that library) and in the Bundled catalog section. Verified: raw URL round-trip sha256-identical (b2f7534b…) + unzips to all 49 datamaps
- [2026-08-10] README SCREENSHOT REPLACED (4e708af pushed, docs-only): user supplied a better shot — the populated-library home screen (Recently Added + Movies rows full of PD titles with posters, alpha.50 app bar) — committed as docs/screenshots/home-linux.jpg, old home-linux.png (single-NOTLD alpha.51 fresh-install wi51 smoke) deleted; caption reworded from "fresh install" to "browsing a library of public-domain films and shows" since fresh installs now seed only NOTLD — verified live via raw.githubusercontent.com (byte-identical)
- [2026-08-09] FAVOURITES SHIPPED (de8f750, UNRELEASED — ready for alpha.51; user dropped the companion playlist idea, favourites only): heart IconButton in DetailScreen's app bar toggles the CURRENTLY SHOWN VERSION (per-address on purpose — with two uploads of one title the user hearts the copy they want) into a new Favourites home row. Row is the FOURTH SPECIAL ROW (kSectionFavourites in home_sections.dart, default order continue→favourites→downloads→recent; customized home_sections_v1 blobs gain it appended via the existing missing-specials reconcile) — hidden while empty, reorder/hide on Settings → Home screen for free, works in BOTH arrangement modes, exempt from auto-mode hidden-list filtering like Downloads. NEW lib/services/favourites.dart: FavouritesStore ChangeNotifier (favourites_v1 StringList in prefs, normalized addrs, ensureLoaded MERGES not replaces so a toggle racing the initial read survives) + favouriteItems() mirroring downloadedItems (enabled lists, library order, dedup, groupShows fold). Deliberately NOT a MediaList: exports/Media page/auto split untouched; address gone from library just stops rendering, heart returns on re-import. 419 Dart tests (+17, incl. 6 home_sections repins) + analyze clean; live Xvfb drive on clean HOME: heart fills blue (/tmp/wi-fav-2-hearted.png), Favourites row tops the wall showing the hearted 480p version (-3-home.png), survives restart (-4-restart.png), un-heart removes the row (-5-unhearted.png), Settings → Home screen lists it with checkbox+drag handle (-8-homelayout.png)
