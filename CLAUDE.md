# Watch-It

## Description
Cross-platform media player (Android, Android TV, iOS, Linux, Windows, macOS) for the Autonomi network. Client-only — no server side at all, and no Plex/Emby/Jellyfin server compatibility (stripped by design). Plex-style poster-wall UI.

## Tech Stack
Flutter 3.44.6 (SDK at ~/flutter) + media_kit (libmpv); SQLite via drift (media lists; codegen: `dart run build_runner build` in app/); Riverpod planned; Autonomi access via embedded Rust library `native/watchit_core` (ant-core, dart:ffi + in-process localhost streaming server) — no gateway/sidecar. `media_kit_video` is vendored at `app/third_party/media_kit_video` (pubspec dependency_overrides) carrying a Linux H/W-rendering patch — see Architecture Notes; drop when upstream fixes media-kit/media-kit#1404.

## Test Commands
In `app/`: `flutter analyze`, `flutter test` (PATH needs `~/flutter/bin`; `source ~/Android/env.sh` for Android builds). Rust: `cargo test` in `native/watchit_core/` (needs `~/.cargo/bin` in PATH). Before `flutter build apk`: run `native/build-android.sh` (cargo-ndk → jniLibs). Host streaming harness: `cargo run --release --bin devserver` then curl `/health` and `/xor/<addr>`.

## Dev Server
`flutter run -d linux` in `app/` — Linux desktop toolchain installed (clang/ninja/libgtk-3-dev/libmpv-dev); Android also fully working. AppImage: `scripts/build_appimage.sh` (needs linuxdeploy at ~/tools/, output in dist/, gitignored).

## Current Status
Phase 0 done (v0.1.0-alpha.22 released, Latest: APK + AppImage — ships the drift/SQLite media-lists migration with one-time legacy import; APK from cb94403, AppImage from 3b094e4 which adds only the libsqlite3.so AppImage packaging fix. AppImage carries all alpha.17–20 H/W-rendering fixes + alpha.21 prefetch/cache). Default movie is now the H.264 8-bit 1080p mp4 at XOR 66cacd06… (5.3GB, faststart, Plex/Jellyfin name) — replaces the 3.8GB AV1 10-bit webm that choked phones/old desktops; ensureDefaults migrates both stale addresses (seeded key bumped to v3). File naming convention documented in docs/NAMING.md. Still open from the Mint-box report: hardware-decoding toggle + pre-first-frame error grace period (proposed for a later alpha; the H.264 default may already resolve it — awaiting user retest). Cold first byte ~20-25s even on fiber. CI live: GitHub Actions runs flutter analyze + test on push/PR (.github/workflows/ci.yml, Flutter 3.44.6 pinned, ~2min). Lists persist in SQLite via drift (watchit.sqlite in the app support dir; one-time import of the legacy shared_preferences blob), shipped in alpha.22. Next: filename→TMDB metadata scraping for list entries. Release signing: keystore ~/keystores/watchit-release.jks + password in ~/keystores/watchit-key.properties (copy to app/android/key.properties, gitignored). gh CLI authenticated as aautonomicc; publish APKs via `gh release create`.

## Architecture Notes
- Library = user-held **lists**; each entry is `{XOR public file address, file name}`. Multiple lists; import/export; later publish/subscribe lists on Autonomi
- Lists live in SQLite via drift: lib/db/app_database.dart (tables media_lists/media_entries, position columns keep order; generated code checked in) behind the unchanged LibraryStore load/save/ensureDefaults API. DB file `watchit.sqlite` pinned to getApplicationSupportDirectory (drift_flutter's documents-dir default degrades to $HOME on headless Linux). First open imports the legacy `media_lists_v1` shared_preferences JSON blob into an empty DB, then removes the key; `defaults_seeded_v3` flag stays in shared_preferences so upgraded installs don't re-seed. Tests inject `AppDatabase.forTesting(NativeDatabase.memory())` via LibraryStore.useForTesting
- Metadata matcher (alpha.23, lib/services/metadata_service.dart + tmdb_client.dart): file name → parseMediaName (title/year/imdb-id/SxxEyy + 1x02 markers) → TMDB (exact /find by IMDb id, else title/year search; TV gets show + episode details) → artwork/description/category, cached in SQLite (metadata_cache table, keyed by ParsedName.lookupKey so renames share rows; misses cached with 7-day TTL, transport errors never cached) with w342 posters as files in <support>/posters/. BYO TMDB key: Settings → Metadata accepts v3 api key (query param) or v4 read token (dots → Bearer header); `--dart-define=TMDB_API_KEY` bundles a default. Screens call the sync MetadataService.instance.metadataFor inside a ListenableBuilder; fallback chain memory → bundled NOTLD catalog → parsed name. Key change calls reset() (clears misses). schemaVersion 2 (onUpgrade creates the table)
- Playback via libmpv (media_kit) everywhere; streams `http://127.0.0.1:<port>/xor/<addr>` from the embedded client — seek works via HTTP Range
- Linux H/W video rendering (alpha.18 + alpha.20): vendored media_kit_video patch — upstream reads Flutter's EGL display with eglGetCurrentDisplay() on the platform thread, which returns EGL_NO_DISPLAY since Flutter 3.38, so every Linux box silently fell back to CPU mpv rendering + pixel-buffer copies (the Mint jerky-video/janky-spinner report). Fix: init stays EAGER on the platform thread (upstream flow) but derives Flutter's EGLDisplay exactly like the embedder's fl_opengl_manager.cc does — eglGetPlatformDisplayEXT(EGL_PLATFORM_WAYLAND/X11_EXT, gdk native display) returns the identical EGLDisplay handle; Flutter's *context* is never needed since the mpv render context lives in an isolated unshared context (only the display must match, for the display-scoped EGLImage frame sharing). Two hard-won invariants: (1) do NOT defer init to the first FlTextureGL populate — deadlock: Video widget mounts the Texture only after mpv reports dimensions, mpv reports dimensions only after the render context exists, the render context was only created in populate, and populate only runs when the Texture is composited (alpha.17's black-screen/stalled-playback bug); (2) after init on the platform thread, RELEASE the isolated context (eglMakeCurrent(dpy, NO_SURFACE, NO_SURFACE, EGL_NO_CONTEXT)) — restoring the saved EGL_NO_DISPLAY binding is an error that leaves our context current there and every raster-thread eglMakeCurrent then fails EGL_BAD_ACCESS; (3, alpha.20) on real-GPU X11 boxes GTK presents FlView through a GDK **GLX** context and leaves it current on the platform thread after painting — EGL cannot see a GLX binding (eglGetCurrentContext()==EGL_NO_CONTEXT) but Mesa refuses to bind an EGL context over it (EGL_BAD_ACCESS 0x3002 → S/W fallback, the Mint alpha.19 report), so video_output_release_foreign_gl_binding() calls gdk_gl_context_clear_current() before binding the isolated context (init + dispose); GDK re-binds on its next paint. xvfb NEVER exercises the GLX case (no DRI3 → no GDK-GL) — real-GPU testing needs the harness's REPRO_GDK_GL=1 runner hook that pins a GdkX11GLContext current before plugin init. Files: linux/video_output.cc (video_output_init_hw, video_output_get_flutter_egl_display, video_output_release_foreign_gl_binding), linux/texture_gl.cc (pure upstream populate). Verified via /tmp/mk_repro harness under xvfb (raw Texture + stock Video widget + upstream control + REPRO_GDK_GL both-ways test)
- Embedded client: `native/watchit_core` (Rust cdylib, ant-core pinned rev 629b87f) — tokio runtime + axum server on 127.0.0.1, `GET /xor/<addr>` (Range via self_encryption streaming_decrypt/get_range), `GET /health` for the Settings status tile; started once from main() over dart:ffi (lib/services/embedded_client.dart). APK is arm64-v8a only (abiFilters + jniLibs excludes); jniLibs/ and target/ are gitignored build artifacts
- Chunk cache + keep-ahead prefetch (post-alpha.20): all chunk traffic goes through a process-wide byte-bounded LRU (`cache.rs`, 256 MiB desktop / 128 MiB Android, single-flight claims so serving+prefetch never double-download; waiters poll 50ms — deliberate, avoids Notify lost-wakeup). Each `stream_range` spawns `prefetch_ahead` (engine.rs): serving loop reports last-served byte via a watch channel, prefetcher keeps PREFETCH_AHEAD_CHUNKS=16 (~64 MiB ≈ 60-70s at 7-8 Mbps) warm at PREFETCH_CONCURRENCY=4 (< FETCH_CONCURRENCY=8 so foreground batches win). Prefetch caps at the request's `end` — fine because mpv sends open-ended ranges. Prefetcher exits when serving drops the watch sender; chunk offsets rebuilt from DataMap::infos() cumulative src_size (same mapping get_range uses). /health gained cache_hit_chunks + cache_bytes
- Android $HOME shim (alpha.7): `watchit_core_start(peers_csv, data_dir)` — Dart resolves getApplicationSupportDirectory() in main() (awaited before runApp so no lazy start can race it) and the native side sets HOME/XDG_DATA_HOME/XDG_CONFIG_HOME to it before any ant-core code runs (temp-dir fallback when NULL). Needed because ant-core's config::data_dir unwraps the home-dir lookup → HomeDirNotFound panic on Android; peer cache persists in the app dir as a side benefit
- Connect hardening (alpha.6): each `Client::connect` attempt runs in its own task (panic→error) with 60s timeout; saorsa-core returns Ok with 0 peers when all bootstrap dials fail — engine treats that as failure; background retry loop 2s→60s backoff; `/health` while connecting = `{state, attempts, message}`; panics routed to tracing (logcat tag `watchit_core`). Devserver takes `WATCHIT_PEERS=ip:port` to test the failure path
- Download manager for offline: downloaded item = same entry with local path, full library UX offline
- Watch state/resume points local-only in SQLite; no accounts, no telemetry
- Branding follows the etchit.io family (fetch>it/etch/it): ink/bone/copper tokens, system fonts, mono for XOR addresses — see docs/BRAND.md (mirrors etchit-io/fetchit docs/BRAND.md)
- Repo: github.com/aautonomicc/Watch-It (MIT)

## Recent Changes
- [2026-07-21] Live TMDB metadata matching implemented (unreleased — needs next alpha build): filename parser now extracts SxxEyy/1x02 episode markers; new TmdbClient (find/search/details/episode, v3-key + v4-token auth) + MetadataService (async resolve, SQLite metadata_cache @ schema v2, poster files on disk, ListenableBuilder UI updates); TMDB API key field in Settings → Metadata (BYO key — none bundled; --dart-define=TMDB_API_KEY hook exists). 44 tests pass (15 new), analyze clean, xvfb debug-build smoke test OK. NOT yet verified against live TMDB — needs a real API key (user must create one at themoviedb.org)
- [2026-07-21] v0.1.0-alpha.22 released (APK + AppImage, marked Latest): ships the SQLite media-lists migration (commit 0f7217b). APK versionCode 22, release-signed (cert verified), 48MB. Pre-release smoke test caught a real bug: package:sqlite3 3.x dlopens bare `libsqlite3.so` via the native-assets manifest (NativeAssetsManifest.json maps to an unqualified name; executable RUNPATH doesn't apply, dev box only worked via system sqlite3) → AppImage crashed on first DB use on clean boxes. No Dart-side hook exists (sqlite3 3.x dropped open.overrideFor; @Native/DefaultAsset resolution is VM-side) — fixed in packaging: build_appimage.sh now copies the bundle's libsqlite3.so into AppDir/usr/lib, already on the custom AppRun's LD_LIBRARY_PATH (commit 3b094e4). Verified: clean-HOME xvfb smoke test creates watchit.sqlite, no sqlite errors, connects to bootstrap peers; upload sizes byte-exact. 29 tests pass, analyze clean
- [2026-07-20] Media lists migrated from shared_preferences to SQLite via drift (unreleased — needs next alpha build). New lib/db/app_database.dart + rewritten LibraryStore (same static API, screens untouched); one-time legacy-blob import verified live on the dev box (real Test Movies list moved into watchit.sqlite, pref key removed, survives restart). deps: drift/drift_flutter + drift_dev/build_runner/sqlite3(dev). 29 Flutter tests pass (3 new: ordering + import + corrupt blob), analyze clean; debug APK assembles with lib/arm64-v8a/libsqlite3.so bundled
- [2026-07-20] Mint judder investigation RESOLVED: switching the Mint box to its dedicated GPU makes playback much better. Root cause was the iGPU's frame presentation/pacing — not the app (standalone mpv and Watch-It were identical), not the file (verified clean CFR 23.976), not streaming (alpha.21 prefetch + buffer/pause tests), and not purely 24fps→60Hz cadence (a dGPU doesn't change cadence). No app changes needed; mpv video-sync=display-resample/interpolation and xrandr 23.976Hz follow-ups moot
- [2026-07-20] v0.1.0-alpha.21 released (APK + AppImage, marked Latest): first build shipping the keep-ahead prefetch + LRU chunk cache. Version bump 0.1.0+21 (commit 1cf5fd3). APK: arm64 cargo-ndk rebuild, release-signed (cert verified), versionCode 21, 47MB. AppImage: 115MB, xvfb smoke test connects to bootstrap peers; cache_hit_chunks/prefetch strings verified in bundled libwatchit_core.so and the isolated-EGL patch string in libmedia_kit_video_plugin.so. Upload sizes verified byte-exact against local files. 26 Flutter + 10 Rust tests pass, analyze clean
- [2026-07-20] Rust core: keep-ahead prefetch + LRU chunk cache (the "reactive server" stutter fix, unreleased — needs next alpha build). New cache.rs (byte-bounded LRU, single-flight, 5 unit tests) + prefetch_ahead in engine.rs; fetch_chunk_batch now cache-backed. Verified live via devserver: 90s throttled 1MB/s stream → prefetch ran ~30MB ahead, 46 serving-path cache hits (incl. boundary-chunk re-reads that previously refetched); rewind of first 16MB = 0.14s, 0 network fetches; mid-file seek to 2.5GB = 7s; all prefetchers exit cleanly (counters flat at idle), no panics. 10 Rust tests pass. No Dart changes (health JSON additions only)
- [2026-07-20] v0.1.0-alpha.20 released (AppImage only, APK unchanged): fixed the remaining Linux S/W-rendering fallback on real-GPU X11 boxes (Mint log: `Failed to make isolated EGL context current. Error: 0x3002`). Root cause: GTK presents FlView through a GDK GLX context left current on the platform thread after painting; EGL can't see GLX bindings so alpha.18's save/restore thought the thread was free, but Mesa fails eglMakeCurrent with EGL_BAD_ACCESS over a GLX binding. Fix: gdk_gl_context_clear_current() before binding the isolated context (init + dispose). Reproduced both ways: minimal C repro (/tmp/egl_bad_access_repro.c) + mk_repro harness with new REPRO_GDK_GL=1 runner hook (unpatched → 0x3002+S/W, patched → H/W, no-hook regression → H/W). xvfb alone can't catch this class (no DRI3 → no GDK-GL). Bundled .so verified to import gdk_gl_context_clear_current; smoke test connects (7 peers). 26 tests pass, analyze clean
- [2026-07-20] v0.1.0-alpha.19 released (APK + AppImage): resync release, no code changes — version bump to 0.1.0+19 only. Brings the Android APK (stuck at alpha.16) level with the Linux alpha.17/18 fixes; both artifacts built from commit fedd756. APK: arm64 cargo-ndk rebuild, signed (release cert verified), versionCode 19, 47MB. AppImage: 110MB, xvfb smoke test connects to 7 bootstrap peers, patched "H/W rendering with isolated EGL context" string verified in bundled plugin .so. Upload sizes verified against local files. 26 tests pass, analyze clean
- [2026-07-20] v0.1.0-alpha.18 (AppImage only): fixed alpha.17 black-screen/stalled-playback regression. Root cause found by local repro (/tmp/mk_repro, minimal media_kit app under xvfb): deferring EGL/mpv-render-context init to the first FlTextureGL populate deadlocks — the Video widget only mounts the Texture once mpv reports video dimensions, which mpv can't do before the render context exists, so populate never ran, mpv never started (position stuck at 0, no dimensions, black screen, demuxer stalled → watchit logs stop). Fix: eager init on the platform thread again, deriving Flutter's EGLDisplay via eglGetPlatformDisplayEXT on the GDK display (same call the 3.44 embedder uses → identical handle; Flutter's context never needed) + release (not restore) the EGL binding after init. Verified under xvfb: stock Video widget plays H/W-rendered test video, position advances, frames change; upstream S/W control compared. AppImage rebuilt + smoke-tested (connects, UI renders); patched strings verified in .so. 26 tests pass, analyze clean
- [2026-07-20] v0.1.0-alpha.17 released (AppImage only, APK unchanged from alpha.16; user testing from the GitHub release — Mint confirmation still pending): Linux S/W-rendering fix: user log showed `EGL display or context is invalid → S/W rendering` — root cause is upstream media_kit_video calling eglGetCurrentDisplay() on the platform thread, EGL_NO_DISPLAY since Flutter 3.38, so mpv rendered on CPU with pixel-buffer copies (jerky video + janky spinner). Vendored media_kit_video 2.0.1 at app/third_party/ (dependency_overrides) with EGL/mpv-render-context init deferred to first FlTextureGL populate on the raster thread + 50ms mark-frame kick loop; dummy 1×1 texture until init lands; eager path kept for pre-3.38 embedders. Patched strings verified inside built .so. AppImage rebuilt (dist/Watch-It-0.1.0-x86_64.AppImage, 110MB, version 0.1.0+17). 26 tests pass, analyze clean. Mint test should show "H/W rendering (EGL init deferred to raster thread)" then "H/W rendering with isolated EGL context" in the log (and no "S/W rendering" line)
