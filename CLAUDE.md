# Watch-It

## Description
Cross-platform media player (Android, Android TV, iOS, Linux, Windows, macOS) for the Autonomi network. Client-only — no server side at all, and no Plex/Emby/Jellyfin server compatibility (stripped by design). Plex-style poster-wall UI.

## Tech Stack
Flutter 3.44.6 (SDK at ~/flutter) + media_kit (libmpv); SQLite via drift (media lists; codegen: `dart run build_runner build` in app/); Riverpod planned; Autonomi access via embedded Rust library `native/watchit_core` (ant-core, dart:ffi + in-process localhost streaming server) — no gateway/sidecar. `media_kit_video` is vendored at `app/third_party/media_kit_video` (pubspec dependency_overrides) carrying a Linux H/W-rendering patch — see Architecture Notes; drop when upstream fixes media-kit/media-kit#1404.

## Test Commands
In `app/`: `flutter analyze`, `flutter test` (PATH needs `~/flutter/bin`; `source ~/Android/env.sh` for Android builds). Live TMDB tests (test/tmdb_live_test.dart) need the credentials from the gitignored repo-root `.env`: `set -a; source ../.env; set +a; flutter test` — skipped automatically when TMDB_API_KEY is unset (CI needs no secrets). Dev build with the key bundled: `flutter run -d linux --dart-define-from-file=../.env`. Since alpha.24, release builds ALSO bundle the key (user decision 2026-07-21): pass `--dart-define=TMDB_API_KEY=$TMDB_API_KEY` sourced from the gitignored repo-root `.env` (build_appimage.sh forwards extra args to `flutter build linux`). Release builds: run `scripts/release_build.sh` — it re-execs itself as transient systemd user unit `watchit-build` (survives session death; linger is on), builds APK then AppImage with the key bundled, logs to /tmp/watchit-build.log, refuses to double-launch. Follow with `tail -f /tmp/watchit-build.log`, status via `systemctl --user status watchit-build`; "ALL BUILDS DONE" in the log = success. The key never enters git, but IS strings-extractable from published binaries — user accepted this trade-off; a user key in Settings → Metadata still overrides the bundled one. Rust: `cargo test` in `native/watchit_core/` (needs `~/.cargo/bin` in PATH). Before `flutter build apk`: run `native/build-android.sh` (cargo-ndk → jniLibs). Host streaming harness: `cargo run --release --bin devserver` then curl `/health` and `/xor/<addr>`.

## Dev Server
`flutter run -d linux` in `app/` — Linux desktop toolchain installed (clang/ninja/libgtk-3-dev/libmpv-dev); Android also fully working. AppImage: `scripts/build_appimage.sh` (needs linuxdeploy at ~/tools/, output in dist/, gitignored).

## Current Status
Phase 0 done, Phase 1 feature-complete, Phase 2 downloads shipped (v0.1.0-alpha.32 released, Latest: APK + AppImage from 493b1b3 — season download-all button, home Downloads row, queue multi-select/delete-all, home top-bar download meter; previously v0.1.0-alpha.31 from 0a5370f — download badges on cards, offline gating with ConnectivityMonitor, Up-next offline rule, per-list export; previously "alpha A" v0.1.0-alpha.30, Latest: APK + AppImage from b41d96c — download manager with queue/pause/resume, prompt-on-play with remembered choice, Settings → Downloads, local playback of finished downloads (drift schema v6); previously v0.1.0-alpha.29 — resume points via watch_states table (drift schema v5), Continue Watching + Recently Added home rows, Resume/Start over + Watched badge + Next episode on DetailScreen, end-of-episode Up-next auto-play overlay; previously v0.1.0-alpha.28 — root-map SQLite persistence + /resolve, prefetch-on-import with hideable/resumable PrefetchManager, tile-open data-map warm, show-level home grouping, size-on-disk + factory reset; v0.1.0-alpha.27 — big-artwork show/season/detail pages with TMDB ratings, air dates, episode screenshots (6ddb5dd, cache schema v4); v0.1.0-alpha.26 — season grouping on the home wall + SeasonScreen, TMDB season artwork, seeded list renamed 'Movies'; and v0.1.0-alpha.25 — import lists from file (local or XOR download), multi-list files, merge-or-new clash handling, 10MB cap, Media Lists management page, TMDB key hidden from Settings UI; and v0.1.0-alpha.24 — bundles the TMDB key at build time so metadata works out of the box, and v0.1.0-alpha.23, APK + AppImage from f684964 — ships live TMDB metadata matching on top of the alpha.22 SQLite media-lists migration; AppImage carries all alpha.17–20 H/W-rendering fixes + alpha.21 prefetch/cache). Live TMDB verified with the user's real key (6 live tests pass; credentials in gitignored repo-root .env — TMDB_API_KEY + TMDB_READ_ACCESS_TOKEN, `set -a; source ../.env` before `flutter test test/tmdb_live_test.dart`). Default movie is now the H.264 8-bit 1080p mp4 at XOR 66cacd06… (5.3GB, faststart, Plex/Jellyfin name) — replaces the 3.8GB AV1 10-bit webm that choked phones/old desktops; ensureDefaults migrates both stale addresses (seeded key bumped to v3). File naming convention documented in docs/NAMING.md. Still open from the Mint-box report: hardware-decoding toggle + pre-first-frame error grace period (proposed for a later alpha; the H.264 default may already resolve it — awaiting user retest). Cold first byte ~20-25s even on fiber. CI live: GitHub Actions runs flutter analyze + test on push/PR (.github/workflows/ci.yml, Flutter 3.44.6 pinned, ~2min). Lists persist in SQLite via drift (watchit.sqlite in the app support dir; one-time import of the legacy shared_preferences blob), shipped in alpha.22. Release signing: keystore ~/keystores/watchit-release.jks + password in ~/keystores/watchit-key.properties (copy to app/android/key.properties, gitignored). gh CLI authenticated as aautonomicc; publish APKs via `gh release create`. Nothing unreleased on main.

## Architecture Notes
- Library = user-held **lists**; each entry is `{XOR public file address, file name}`. Multiple lists; import/export; later publish/subscribe lists on Autonomi
- Lists live in SQLite via drift: lib/db/app_database.dart (tables media_lists/media_entries, position columns keep order; generated code checked in) behind the unchanged LibraryStore load/save/ensureDefaults API. media_lists.enabled (schema v3, default 1) drives per-list home-screen visibility; lists are managed on the dedicated Settings → Media Lists page (screens/media_lists_screen.dart: create, checkbox show/hide, rename/delete behind a 3-dot menu; tap opens ListEditScreen). DB file `watchit.sqlite` pinned to getApplicationSupportDirectory (drift_flutter's documents-dir default degrades to $HOME on headless Linux). First open imports the legacy `media_lists_v1` shared_preferences JSON blob into an empty DB, then removes the key; `defaults_seeded_v3` flag stays in shared_preferences so upgraded installs don't re-seed. Tests inject `AppDatabase.forTesting(NativeDatabase.memory())` via LibraryStore.useForTesting
- Metadata matcher (alpha.23, lib/services/metadata_service.dart + tmdb_client.dart): file name → parseMediaName (title/year/imdb-id/SxxEyy + 1x02 markers) → TMDB (exact /find by IMDb id, else title/year search; TV gets show + episode details) → artwork/description/category, cached in SQLite (metadata_cache table, keyed by ParsedName.lookupKey so renames share rows; misses cached with 7-day TTL, transport errors never cached) with w342 posters as files in <support>/posters/. BYO TMDB key: Settings → Metadata accepts v3 api key (query param) or v4 read token (dots → Bearer header); `--dart-define=TMDB_API_KEY` bundles a default. Screens call the sync MetadataService.instance.metadataFor inside a ListenableBuilder; fallback chain memory → bundled NOTLD catalog → parsed name. Key change calls reset() (clears misses). schemaVersion 2 (onUpgrade creates the table)
- Playback via libmpv (media_kit) everywhere; streams `http://127.0.0.1:<port>/xor/<addr>` from the embedded client — seek works via HTTP Range
- Linux H/W video rendering (alpha.18 + alpha.20): vendored media_kit_video patch — upstream reads Flutter's EGL display with eglGetCurrentDisplay() on the platform thread, which returns EGL_NO_DISPLAY since Flutter 3.38, so every Linux box silently fell back to CPU mpv rendering + pixel-buffer copies (the Mint jerky-video/janky-spinner report). Fix: init stays EAGER on the platform thread (upstream flow) but derives Flutter's EGLDisplay exactly like the embedder's fl_opengl_manager.cc does — eglGetPlatformDisplayEXT(EGL_PLATFORM_WAYLAND/X11_EXT, gdk native display) returns the identical EGLDisplay handle; Flutter's *context* is never needed since the mpv render context lives in an isolated unshared context (only the display must match, for the display-scoped EGLImage frame sharing). Two hard-won invariants: (1) do NOT defer init to the first FlTextureGL populate — deadlock: Video widget mounts the Texture only after mpv reports dimensions, mpv reports dimensions only after the render context exists, the render context was only created in populate, and populate only runs when the Texture is composited (alpha.17's black-screen/stalled-playback bug); (2) after init on the platform thread, RELEASE the isolated context (eglMakeCurrent(dpy, NO_SURFACE, NO_SURFACE, EGL_NO_CONTEXT)) — restoring the saved EGL_NO_DISPLAY binding is an error that leaves our context current there and every raster-thread eglMakeCurrent then fails EGL_BAD_ACCESS; (3, alpha.20) on real-GPU X11 boxes GTK presents FlView through a GDK **GLX** context and leaves it current on the platform thread after painting — EGL cannot see a GLX binding (eglGetCurrentContext()==EGL_NO_CONTEXT) but Mesa refuses to bind an EGL context over it (EGL_BAD_ACCESS 0x3002 → S/W fallback, the Mint alpha.19 report), so video_output_release_foreign_gl_binding() calls gdk_gl_context_clear_current() before binding the isolated context (init + dispose); GDK re-binds on its next paint. xvfb NEVER exercises the GLX case (no DRI3 → no GDK-GL) — real-GPU testing needs the harness's REPRO_GDK_GL=1 runner hook that pins a GdkX11GLContext current before plugin init. Files: linux/video_output.cc (video_output_init_hw, video_output_get_flutter_egl_display, video_output_release_foreign_gl_binding), linux/texture_gl.cc (pure upstream populate). Verified via /tmp/mk_repro harness under xvfb (raw Texture + stock Video widget + upstream control + REPRO_GDK_GL both-ways test)
- Embedded client: `native/watchit_core` (Rust cdylib, ant-core pinned rev 629b87f) — tokio runtime + axum server on 127.0.0.1, `GET /xor/<addr>` (Range via self_encryption streaming_decrypt/get_range), `GET /health` for the Settings status tile; started once from main() over dart:ffi (lib/services/embedded_client.dart). APK is arm64-v8a only (abiFilters + jniLibs excludes); jniLibs/ and target/ are gitignored build artifacts
- Chunk cache + keep-ahead prefetch (post-alpha.20): all chunk traffic goes through a process-wide byte-bounded LRU (`cache.rs`, 256 MiB desktop / 128 MiB Android, single-flight claims so serving+prefetch never double-download; waiters poll 50ms — deliberate, avoids Notify lost-wakeup). Each `stream_range` spawns `prefetch_ahead` (engine.rs): serving loop reports last-served byte via a watch channel, prefetcher keeps PREFETCH_AHEAD_CHUNKS=16 (~64 MiB ≈ 60-70s at 7-8 Mbps) warm at PREFETCH_CONCURRENCY=4 (< FETCH_CONCURRENCY=16 so foreground batches win). Prefetch caps at the request's `end` — fine because mpv sends open-ended ranges. Prefetcher exits when serving drops the watch sender; chunk offsets rebuilt from DataMap::infos() cumulative src_size (same mapping get_range uses). /health gained cache_hit_chunks + cache_bytes
- Root-map persistence + prefetch (post-alpha.27): resolved root data maps persist in SQLite (`mapstore.rs`, rusqlite bundled, `root_maps.sqlite` in the app data dir, addr BLOB → bincode DataMap via DataMap::to_bytes) — content-addressed and immutable so cached forever; `Engine::root_map` reads memory → disk → network and writes back both, so any title's map resolves over the network at most once per device (cold resolve of the 5.3GB movie ≈ 22-31s, disk hit ≈ 7ms, across app restarts). `GET /resolve/{addr}` resolves+persists without streaming (returns `{size, chunks}`); `/health` gained `stored_maps`. Flutter: after a list import the Media Lists page offers "Prefetch data maps?" (DataMapPrefetcher walks the imported entries sequentially through /resolve; progress dialog = spinner + `File X of N` + file name + Cancel; declining costs nothing — maps persist on first play anyway). `Engine::new`/`start()` take the data dir; devserver honours WATCHIT_DATA_DIR
- Android $HOME shim (alpha.7): `watchit_core_start(peers_csv, data_dir)` — Dart resolves getApplicationSupportDirectory() in main() (awaited before runApp so no lazy start can race it) and the native side sets HOME/XDG_DATA_HOME/XDG_CONFIG_HOME to it before any ant-core code runs (temp-dir fallback when NULL). Needed because ant-core's config::data_dir unwraps the home-dir lookup → HomeDirNotFound panic on Android; peer cache persists in the app dir as a side benefit
- Connect hardening (alpha.6): each `Client::connect` attempt runs in its own task (panic→error) with 60s timeout; saorsa-core returns Ok with 0 peers when all bootstrap dials fail — engine treats that as failure; background retry loop 2s→60s backoff; `/health` while connecting = `{state, attempts, message}`; panics routed to tracing (logcat tag `watchit_core`). Devserver takes `WATCHIT_PEERS=ip:port` to test the failure path
- Download manager (post-alpha.29, lib/services/download_manager.dart): pure Dart, no Rust changes — streams `GET /xor/<addr>` (deterministic decrypted bytes) to disk one task at a time, resuming partial files with `Range: bytes=<file length>-` (the file on disk, not the DB counter, is the resume offset; progress persists only after flush). Queue persists in the `downloads` drift table (schema v6, address PK); mid-flight rows re-queue on next launch, paused stay paused. Files land in `<support>/downloads/` (Android app-private, no permissions) or a user-chosen desktop folder (Settings → Downloads, file_selector, new downloads only); duplicate names get an addr-prefix. Total size pre-filled via `/resolve` at enqueue. UI: DetailScreen Download button cycles start/pause/resume/retry/remove with a progress bar; Settings → Downloads = queue + pause/resume-all + storage + behaviour. A finished download plays locally: DetailScreen resolves local-path-beats-streamUrl (also for Up-next chaining via PlayerScreen.sourceFor; isLocal skips the network buffering overlay + health poll). Streamed playback with active downloads applies PauseDownloadsOnPlay (ask/always/never, "remember my choice" writes the pref; chained episodes never re-prompt); pauseAllForPlayback tracks exactly what it paused so resumeAfterPlayback (player close, "Downloads resumed" snackbar) spares manual pauses. Race gotcha: resume() can land before an aborted transfer's error surfaces — the catch treats status queued like paused (keep bytes, let the pump retry), else it would mark the re-queued task error. Connection loss auto-pauses instead of erroring: a failed transfer probes `/health` and pauses (no error text) when the client is `connecting` or `ready` with 0 peers; a 2-minute no-data stall timeout on the body stream backstops streams that hang instead of erroring. Resume stays manual (auto-resume on reconnect not implemented)
- Watch state/resume points local-only in SQLite; no accounts, no telemetry
- Branding follows the etchit.io family (fetch>it/etch/it): ink/bone/copper tokens, system fonts, mono for XOR addresses — see docs/BRAND.md (mirrors etchit-io/fetchit docs/BRAND.md)
- Repo: github.com/aautonomicc/Watch-It (MIT)

## Recent Changes
- [2026-07-25] FINAL two open decisions USER-CONFIRMED — bundle plan is now FULLY LOCKED, all six decisions closed, ready to implement on request (planning only, no code yet): (2) library.json {enabled, position} applies ONLY to lists the import creates — existing lists are never reordered/hidden/re-enabled (fresh-device migration hits an empty library so full home state restores anyway); (3) bundle extension is **.watch-list** (user's pick, supersedes the proposed .witchlist everywhere in the entries below — cosmetic only, import routes on PK zip-magic sniff, not extension). Full locked spec: single Import button auto-detecting .txt vs .watch-list; single Export button w/ two-step dialog (List only .txt / Full bundle .watch-list → checkboxes history default OFF, root maps default ON); cancellable pre-resolve pass on export (Cancel keeps partial bundle); caps 10MB txt / 200MB bundle + per-member decompressed sanity limits; mobile bundle save via SAF (file_selector), share sheet for .txt only; TMDB notice+logo in Settings About/Metadata + attribution field in metadata.json; root maps verified offline via shrink/serialize/hash == entry address. Est ~2 days, shippable in slices
- [2026-07-25] Four of six open decisions USER-CONFIRMED (planning only, no code): (1) export pre-resolve = DataMapPrefetcher-style cancellable progress pass; Cancel keeps the partial bundle (missing metadata/maps degrade gracefully on import); (4) import caps: plain text stays 10MB, .witchlist bundles capped at 200MB with per-member sanity limits on decompressed sizes (zip-bomb guard); (5) root-maps export checkbox default ON confirmed; (6) mobile bundle export uses the SAF save-file dialog (file_selector getSaveLocation, same as desktop) instead of share_plus — reliable for ~200MB zips; plain .txt export keeps the share sheet. STILL OPEN (explained to user, awaiting confirmation): (2) library.json {enabled, position} on import into a non-empty library — recommended apply-to-newly-created-lists-only, never touch existing lists; (3) the .witchlist extension name itself (explained it's a cosmetic Watch-It-list pun over a zip; content sniff makes it functionally irrelevant)
- [2026-07-25] Attribution USER-CONFIRMED + final outstanding-decisions sweep (planning only, no code): user approved both attribution items — standard TMDB notice + logo in Settings About/Metadata section, attribution field in bundle metadata.json. Sweep of all plan entries found these still open before implementation: (1) root-map pre-resolve on export — un-played titles need a full 22-31s network resolve each, so a large unwatched list makes bundle export minutes-long; recommended: share the DataMapPrefetcher-style cancellable progress pass, Cancel exports whatever resolved so far (partial bundles degrade gracefully on import); (2) library-bundle import into a NON-empty library — does library.json {enabled, position} apply only to lists the import creates (recommended: never reorder/hide existing lists; fresh-device migration hits an empty library so full state restores anyway) or overwrite existing home state; (3) .witchlist extension still only "proposed" — confirm the name; (4) exact new import cap — 10MB → ~200MB proposed, alternative: keep 10MB for plain text + larger zip cap with per-member sanity limits; (5) root-maps checkbox default ON is recommended but not user-confirmed (history OFF is); (6) mobile bundle-export delivery — share_plus share sheet fine for small .txt, fragile for ~200MB zips; consider Android SAF save instead. Implicit accepts assumed unless vetoed: pure-Dart `archive` dep, metadata pre-resolve pass on export
- [2026-07-25] TMDB attribution GAP FLAGGED (planning only, no code): the bundle plan's "keep attribution" note was an intention, not a spec — nothing concrete existed, and the app itself currently shows NO TMDB attribution anywhere (grep confirms: no "not endorsed or certified by TMDB" text, no TMDB logo). TMDB terms require the notice wherever their data/images appear, so this is owed today independent of bundles. Decided: (1) add the standard notice + logo to Settings (About/Metadata section) in the app; (2) bundle's metadata.json gains a top-level attribution field ("Metadata and images © TMDB — this product uses the TMDB API but is not endorsed or certified by TMDB") so redistributed data carries the credit. Cost trivial, inside the existing ~2-day estimate. Estimate breakdown reconfirmed 2026-07-25: base bundle ~1 day (zip r/w + archive dep, export pre-resolve pass w/ progress UI, two-step dialog, sniff+seed import, cap raise, clean-keyless-profile round-trip test) + ~0.5 library/history + ~0.5 root maps (mostly Rust: 2 endpoints + offline shrink/serialize/hash verify); shippable in slices, metadata-only first
- [2026-07-25] Export/import UX DECIDED (planning only, no code): (1) Import remains ONE action and auto-detects format — the PK zip-magic sniff already planned for the bundle means plain list.txt and .witchlist bundles both go through the same Import button with zero user choice (file extension irrelevant; partial bundles fine — whatever's absent from the zip just isn't seeded). (2) Export — both the per-list 3-dot item and the library option — becomes ONE button with a two-step dialog: step 1 radio "List only (.txt, plain text — today's byte-identical export)" vs "Full bundle (.witchlist — metadata + posters + root maps)"; step 2 shown only when Full bundle is picked: checkboxes "Include watch history" (default OFF, user-confirmed 2026-07-25) and "Include root maps (instant play on import)" (default ON, no privacy delta vs list.txt). Supersedes the "separate 'Export with metadata' menu item vs replacing Export" open decision in the metadata-bundle entry below: neither — single Export entry + format chooser. No estimate change (dialog UI already inside the ~1 day)
- [2026-07-25] Root-map bundling in export PLANNED (planning only, no code) — supersedes the "root_maps.sqlite not migrated" clause below: bundles CAN carry root data maps securely because everything is content-addressed and self-encryption is deterministic. Import verifies each map fully OFFLINE: re-shrink the imported root map (self_encryption::shrink_data_map, deterministic convergent encryption), serialize the top-level map with rmp_serde (exactly what ant-core data_map_store does at pinned rev 629b87f — the list's XOR address IS content_hash of that serialized chunk), hash, require == entry address; mismatch → discard + fall back to normal network resolve (import never fails, tampered maps can't poison the cache). Saves the 22-31s cold resolve per movie-sized title on the importing device — imported lists are instantly play-fast with no prefetch pass. Export ONLY maps for bundled entries' addresses (never dump root_maps.sqlite wholesale — it holds every title ever played, would leak beyond the exported list); entries never played/prefetched have no map → same pre-resolve-on-export open decision as metadata (share one DataMapPrefetcher pass). Plumbing: root_maps.sqlite is Rust-owned, so embedded server gains GET /rootmap/{addr} (export; 404 = unresolved) + PUT /rootmap/{addr} (import: verify-then-store in Rust, 4xx on mismatch); zip gains rootmaps/<addr>.map in DataMap::to_bytes format (undecodable rows already degrade to re-resolve). Checkbox default ON recommended (unlike watch history there's no privacy delta — the addresses are already in list.txt); verification cost ~ms per map. Est +~half day
- [2026-07-25] Bundle export plan EXTENDED (planning only, no code): (1) "Export library" option beside per-list export — the plain-text format already supports multi-list files (ListName= markers, import parses them today), so library export = concatenate serializeMediaList over all media_lists in position order into one list.txt; bundle metadata/posters = union across all entries; include disabled lists (it's a backup) and add library.json to the zip carrying per-list {enabled, position} so migration restores home-screen state (plain-text importers just ignore it). (2) Optional watch-history export for device migration — watch_states rows are keyed by normalized XOR address (content address, device-independent) so they migrate with zero remapping: history.json = {address, positionMs, durationMs, completed, updatedAt} for exported entries' addresses (per-list) or all rows (library). Continue Watching / Watched badges / Up-next all derive at runtime from these rows + file-name parsing, so nothing else is needed. Import merge = newer updatedAt wins (never regresses local progress). Zip-only (plain text can't carry it); export checkbox "Include watch history" default OFF everywhere — per-list AND library export (user decided 2026-07-25: OFF for library too; shared lists shouldn't leak viewing habits, library backup opts in explicitly). Explicitly NOT migrated: downloads table + files (absolute local paths, re-download on new device); root_maps.sqlite originally excluded here but bundling it was planned 2026-07-25 with offline verification (see entry above). Est +~half day on top of the bundle's ~1 day
- [2026-07-25] Metadata-bundle export PLANNED (no code): zip container (proposed ext .witchlist) holding list.txt (current export format, unzip-and-import compatible), metadata.json (metadata_cache rows keyed by lookupKey), posters/ (the local JPGs, deduped). Import sniffs zip magic (PK) vs plain text — same sniff would let network list fetch carry bundles; seeding fills only missing cache rows/poster files (local found rows win), fetchedAt stamped at import — keyless importers then get full posters/overviews/episode names with zero TMDB calls. Open decisions: pre-resolve pass on export for un-browsed entries (recommended, progress dialog, misses degrade to name-only), separate "Export with metadata" menu item vs replacing Export, raise 10MB import cap for bundles (~200MB), new pure-Dart `archive` dep. TMDB redistribution terms noted as a gray area (keep attribution). Est ~1 day incl round-trip test into a clean keyless profile
- [2026-07-24] Linux taskbar/window icon (unreleased): the 192px launcher logo is embedded in the runner via GResource (linux/runner/watchit.gresource.xml + resources/watchit.png; runner CMakeLists compiles it with glib-compile-resources — project() needed `LANGUAGES C CXX` since the generated file is C) and set with gtk_window_set_icon/set_default_icon in my_application.cc, so the running AppImage shows the Watch-It logo in the taskbar (_NET_WM_ICON) instead of the generic executable icon. watchit.desktop gained StartupWMClass=io.github.aautonomicc.watchit for integrated-AppImage/Wayland matching. Verified under Xvfb: toplevel advertises a 192x192 _NET_WM_ICON that decodes to the logo (/tmp/wi-taskbar-icon.png). Note: GtkApplication maps two X windows — the group-leader carries WM_CLASS but no icon; xprop the second one. 219 tests pass, analyze clean
- [2026-07-24] v0.1.0-alpha.32 released (APK + AppImage, marked Latest, tag on 493b1b3): first build shipping season download-all + home Downloads row + queue multi-delete (f941528) and the home top-bar download meter (8b943ac), plus versionCode-32 bump 493b1b3. Built via scripts/release_build.sh as systemd unit watchit-build (ALL BUILDS DONE 21:30, ~6.5min CPU); note: the build session's poller never re-invoked after BUILD_OK — release completed in the next task. APK versionCode 32, release-signed (cert verified), 52MB; AppImage 117MB, artifacts hand-renamed as usual. TMDB key verified in both (repo-root .env, grep the bare 32-hex token; AppImage extracted first). Clean-HOME xvfb smoke: watchit.sqlite + root_maps.sqlite created, 5 bootstrap peers, no panics. https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.32
- [2026-07-24] Home top-bar download meter (RELEASED in alpha.32): DownloadsIndicator in the home AppBar left of the settings icon — copper circular percent ring + "x of y" finished count, visible only while something is queued or downloading; tap opens Settings → Downloads. Backed by DownloadManager.batchProgress: a `_batch` set accumulates every task active since the queue was last idle (finished members keep counting — that's what makes x-of-y possible), cleared when the queue goes idle (all paused/done/error → meter hides; next enqueue starts a fresh count); progress = mean per-file fraction (done=1, size-unknown=0). New `@visibleForTesting debugStageTask` stages in-memory tasks for widget tests (stubbed HttpClient can't run real transfers). 7 new tests → 219 pass, analyze clean. Live-verified under Xvfb with a seeded queue (BegBlag.mp3 + 5.3GB movie, real network, md5 verified; /tmp/wi-meter-*.png): meter showed 1 of 2 mid-batch, tap-through worked, Pause all hid it. Test-harness gotchas: waitFor(downloading) passes before the transfer reaches the server's gate — delay 100ms before swapping gate completers; debug Linux bundle lacks libwatchit_core.so (copy from native release target)
- [2026-07-24] Season download-all + home Downloads row + queue multi-delete (RELEASED in alpha.32): (1) SeasonScreen header button — "Download season" enqueues every episode, "Download remaining (N)" when some are done, disabled "Season downloaded" when all are; disabled offline (ConnectivityMonitor added to the screen's merged listenable); snackbar "N episodes added to downloads"; (2) home wall gains a Downloads row (below Continue Watching, above Recently Added) via downloadedItems() in home_rows.dart — done-status entries only, movies as poster cards, a show's downloaded episodes folded into one show card (full check badge; the show card opens ShowScreen with just the downloaded episodes); computed inside _posterWall so it live-updates on every DownloadManager notification; (3) Settings → Downloads — checkbox on every queue row + "Delete selected (N)" and always-present "Delete all" (both confirm, then DownloadManager.removeMany deletes rows + files; ticked set pruned against the live queue each build; action row became a Wrap). 14 new tests → 212 pass, analyze clean. Live-verified under Xvfb with a python-seeded DB (/tmp/wi-feat-*.png): row appears/updates/vanishes as downloads are deleted, button counts correct, multi-delete flow works. Widget-test gotcha reconfirmed: async dart:io started from a tap never completes under pumpAndSettle alone — poll with tester.runAsync + pump (settings_about_test pattern)
- [2026-07-24] Docs refreshed to alpha.31 state: README status blurb now covers downloads/offline/export (alpha.30/.31), ROADMAP Phase 2 marked feature-complete (all checkboxes closed with shipped details; deferred: auto-resume on reconnect, per-direction bandwidth knobs; upstream /health stale-peers limitation noted), ARCHITECTURE Downloads/offline section rewritten to the shipped pure-Dart design (queue/Range-resume/auto-pause/ConnectivityMonitor) + open question 4 closed for export, UI-DESIGN "Built so far" bumped to alpha.31. Docs-only, no code
- [2026-07-24] v0.1.0-alpha.31 released (APK + AppImage, marked Latest, tag on 0a5370f): first build shipping alpha B (aba07b4 — download badges, offline gating, Up-next offline rule, per-list export) + versionCode-31 bump 0a5370f. Built via scripts/release_build.sh as systemd unit watchit-build (ALL BUILDS DONE 19:43; ~16min CPU, longer than the usual ~6 — clean rebuild). APK versionCode 31, release-signed (cert verified), 52MB; AppImage 117MB, artifacts hand-renamed as usual. TMDB key verified in both (AppImage extracted first; note the .env value carries trailing whitespace — grep the bare 32-hex token or it false-negatives). Clean-HOME xvfb smoke: watchit.sqlite + root_maps.sqlite created, 5 bootstrap peers, no panics. https://github.com/aautonomicc/Watch-It/releases/tag/v0.1.0-alpha.31
- [2026-07-24] Alpha B implemented (commit aba07b4 pushed, unreleased): (1) badges — lib/widgets/download_badge.dart entryDownloadBadge (copper check = done, determinate ring = any queued/downloading/paused/error task) + groupDownloadBadge (done/total count on show/season cards once ANY episode downloaded, plain check when all) wired into _PosterCard/_ContinueCard/_ShowCard/_SeasonTile/_EpisodeTile (posters became Stacks; screens listen to DownloadManager); (2) offline gating — lib/services/connectivity.dart ConnectivityMonitor singleton polls /health (4s offline / 15s online; connecting-or-ready-with-0-peers rule; 'unavailable' stops polling and 'error' is deliberately NOT offline so tests/broken-core keep existing surfaces), DetailScreen disables Play/Resume/Start-over + hint for non-downloaded titles and network-needing download actions only; (3) Up-next chain — canChainInto live-probes at episode end, offline + non-downloaded next stops the chain, online falls back to streaming; (4) export — Media Lists 3-dot Export, serializeMediaList (marker form, concatenation-safe), desktop save dialog / share_plus share sheet on mobile (NEW DEP share_plus). 22 new tests, 198 pass, analyze clean. Live-verified under Xvfb (screenshots /tmp/wi-b-*.png): all badge forms, Downloaded detail, export file re-importable byte-exact. FINDING (upstream, out of scope): in a no-network netns ant-core reports ready with 28 "connected" peers (connected_peers counts unroutable peers) so the /health rule cannot detect total network absence (affects shipped auto-pause equally — VPN-cut/handshake-timeout loss is detectable, airplane-mode-at-OS-level may not be), and the embedded Rust core died silently after ~3-5 min with no network (only Dart VM service left listening; no panic in stderr) — needs an ant-core-level look before relying on offline UX hard
