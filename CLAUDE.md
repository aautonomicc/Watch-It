# Watch-It

## Description
Cross-platform media player (Android, Android TV, iOS, Linux, Windows, macOS) for the Autonomi network. Client-only — no server side at all, and no Plex/Emby/Jellyfin server compatibility (stripped by design). Plex-style poster-wall UI.

## Tech Stack
Flutter 3.44.6 (SDK at ~/flutter) + media_kit (libmpv); SQLite via drift planned; Riverpod planned; Autonomi access via embedded Rust library `native/watchit_core` (ant-core, dart:ffi + in-process localhost streaming server) — no gateway/sidecar.

## Test Commands
In `app/`: `flutter analyze`, `flutter test` (PATH needs `~/flutter/bin`; `source ~/Android/env.sh` for Android builds). Rust: `cargo test` in `native/watchit_core/` (needs `~/.cargo/bin` in PATH). Before `flutter build apk`: run `native/build-android.sh` (cargo-ndk → jniLibs). Host streaming harness: `cargo run --release --bin devserver` then curl `/health` and `/xor/<addr>`.

## Dev Server
`flutter run -d linux` in `app/` — blocked until clang/ninja/libgtk-3-dev are apt-installed (Linux desktop toolchain incomplete; Android is fully working).

## Current Status
Phase 0 nearly done: scaffold, media lists, detail screen/playback, embedded Autonomi client (v0.1.0-alpha.12 released). Docs (ROADMAP status/checkboxes, README status) refreshed 2026-07-19 to match. alpha.7 confirmed connected on device (5 peers) but Play sat on an endless spinner; host repro showed the pipeline works end-to-end (ffprobe/ffmpeg decode OK) but cold first byte takes ~20-25s even on fiber — alpha.8 adds buffering progress UI, mpv error surfacing, 300s network timeout, 1MB opening range step. Awaiting on-device result (no adb here). Note: default movie is 3.8GB AV1 10-bit 1440x1080 — heavy for phones (dav1d software decode where no AV1 hwdec); consider a smaller H.264 default if playback still struggles. Remaining Phase 0: on-device streaming smoke test. CI live: GitHub Actions runs flutter analyze + test on push/PR (.github/workflows/ci.yml, Flutter 3.44.6 pinned, ~2min). Lists persist via shared_preferences (stand-in until drift/SQLite). Release signing: keystore ~/keystores/watchit-release.jks + password in ~/keystores/watchit-key.properties (copy to app/android/key.properties, gitignored). gh CLI authenticated as aautonomicc; publish APKs via `gh release create`.

## Architecture Notes
- Library = user-held **lists**; each entry is `{XOR public file address, file name}`. Multiple lists; import/export; later publish/subscribe lists on Autonomi
- Metadata matcher parses file name → TMDB → artwork/description/category (same pipeline media servers use, but on-device); cached in SQLite
- Playback via libmpv (media_kit) everywhere; streams `http://127.0.0.1:<port>/xor/<addr>` from the embedded client — seek works via HTTP Range
- Embedded client: `native/watchit_core` (Rust cdylib, ant-core pinned rev 629b87f) — tokio runtime + axum server on 127.0.0.1, `GET /xor/<addr>` (Range via self_encryption streaming_decrypt/get_range), `GET /health` for the Settings status tile; started once from main() over dart:ffi (lib/services/embedded_client.dart). APK is arm64-v8a only (abiFilters + jniLibs excludes); jniLibs/ and target/ are gitignored build artifacts
- Android $HOME shim (alpha.7): `watchit_core_start(peers_csv, data_dir)` — Dart resolves getApplicationSupportDirectory() in main() (awaited before runApp so no lazy start can race it) and the native side sets HOME/XDG_DATA_HOME/XDG_CONFIG_HOME to it before any ant-core code runs (temp-dir fallback when NULL). Needed because ant-core's config::data_dir unwraps the home-dir lookup → HomeDirNotFound panic on Android; peer cache persists in the app dir as a side benefit
- Connect hardening (alpha.6): each `Client::connect` attempt runs in its own task (panic→error) with 60s timeout; saorsa-core returns Ok with 0 peers when all bootstrap dials fail — engine treats that as failure; background retry loop 2s→60s backoff; `/health` while connecting = `{state, attempts, message}`; panics routed to tracing (logcat tag `watchit_core`). Devserver takes `WATCHIT_PEERS=ip:port` to test the failure path
- Download manager for offline: downloaded item = same entry with local path, full library UX offline
- Watch state/resume points local-only in SQLite; no accounts, no telemetry
- Branding follows the etchit.io family (fetch>it/etch/it): ink/bone/copper tokens, system fonts, mono for XOR addresses — see docs/BRAND.md (mirrors etchit-io/fetchit docs/BRAND.md)
- Repo: github.com/aautonomicc/Watch-It (MIT)

## Recent Changes
- [2026-07-19] CI added: .github/workflows/ci.yml runs `flutter pub get` + `flutter analyze` + `flutter test` in app/ on push to main and PRs (subosito/flutter-action, Flutter 3.44.6 pinned, cache on). First run green in 2m13s; Phase 0 CI checkbox ticked in ROADMAP
- [2026-07-19] Docs refreshed for alpha.12: ROADMAP gains a dated status header, Phase 0 items checked off (CI + Linux desktop toolchain remain), Phase 1 items annotated with real partial progress (lists = shared_preferences stand-in, TMDB = stub, streaming = done, APK = done/AppImage pending); README Status replaced "Early design phase" with working-alpha summary + release link
- [2026-07-19] v0.1.0-alpha.12: home app bar wordmark now matches the launcher icon — copper `[>]` mark (TextSpan, t.copper) precedes the bone `watch-it`; lockup documented in docs/BRAND.md. 24 Flutter tests pass, analyze clean
- [2026-07-19] v0.1.0-alpha.11: player controls lifted clear of Android nav buttons (bottomButtonBarMargin/seekBarMargin bottom: 48 normal / 64 fullscreen) and seek bar doubled to 4.8px (stock 2.4) via MaterialVideoControlsTheme copyWith. 24 Flutter tests pass, analyze clean. Note: background builds must be launched with `setsid nohup` or they die with the session
- [2026-07-19] v0.1.0-alpha.10: Settings → Streaming gains Buffer size option (16/32/64/128/256 MB radio dialog, default 32) — stored in shared_preferences (new lib/services/app_settings.dart), passed DetailScreen→PlayerScreen→PlayerConfiguration.bufferSize which caps mpv demuxer-max-bytes AND demuxer-max-back-bytes (so worst-case RAM ≈ 2× setting); applies on next Play. Note: the ">100MB" the user saw was likely the cumulative fetched-bytes counter, not the mpv buffer. 24 Flutter tests pass, analyze clean
- [2026-07-19] v0.1.0-alpha.9: removed duplicate grey buffering spinner during playback — media_kit's Material controls always draw their own CircularProgressIndicator; blanked via bufferingIndicatorBuilder in MaterialVideoControlsTheme + MaterialDesktopVideoControlsTheme wrappers so only the branded copper overlay shows. 19 Flutter tests pass, analyze clean
- [2026-07-19] v0.1.0-alpha.8: endless play-spinner fix — buffering overlay with live "N MB retrieved" (new fetched_chunks/fetched_bytes counters in /health), mpv errors shown on screen, mpv network-timeout 300s, range responses open with 1MB step doubling to 8MB (faster first byte). Host-verified: ffprobe 24s cold / ffmpeg 5s decode OK / 1.7MB/s sustained (~2.3× the 5.7Mbps bitrate); ant-core caches chunks so seeks don't refetch. File is AV1 10-bit; bundled libmpv has dav1d + av1_mediacodec. 19 Flutter + 5 cargo tests
- [2026-07-19] v0.1.0-alpha.7: fixed on-device connect panic `HomeDirNotFound` — Android processes lack $HOME and ant-core unwraps the home-dir lookup; app data dir now passed over FFI and exported as HOME/XDG_* in the native lib before ant-core runs. Host-verified with HOME unset: ready/5 peers, Range 206. 18 Flutter + 5 cargo tests pass. Deps added: ffi, path_provider
- [2026-07-19] v0.1.0-alpha.6: fix for stuck “Connecting to the network…” on device — connect attempts now bounded (60s), run panic-safe in own task, zero-peer “success” treated as failure, background retry with 2s→60s backoff; /health exposes attempt count + last error; home bar & Settings tile (now auto-polling) display them. Host-verified: ready in ~7s/5 peers, Range 206; dead-peer path shows retrying error. 18 Flutter + 5 cargo tests pass
- [2026-07-19] v0.1.0-alpha.5: default movie re-pointed to new XOR cebd79...3988 (old ac855e...0ea0 dead on network — cause of the endless loading spinner); seeder migrates stale address in existing installs (defaults_seeded_v2); Settings gains ABOUT section (app blurb + version via package_info_plus ^10.2.1); home screen gains NetworkStatusBar (dot + "connected · N peers" from /health, polls 3s connecting / 15s ready, hidden when native lib absent). New address verified on host: 3.8GB webm, first KB + mid-file Range both 206. 18 tests pass
- [2026-07-19] v0.1.0-alpha.4: embedded Autonomi client (native/watchit_core Rust cdylib via cargo-ndk) replaces AntTP gateway; gateway setting removed, Settings shows live connection status; verified vs live network on host (full-file md5 exact in 11s, mid-file Range byte-exact; 15MB file). arm64-only APK 46.5MB, signed, on GitHub Release. No adb device for on-device test