# Secrets (TMDB credentials etc.) — never commit
.env
*.env
# Rust build output
native/watchit_core/target/
# Cross-compiled native libs are build artifacts (native/build-android.sh)
app/android/app/src/main/jniLibs/
# AppImage build output
dist/
